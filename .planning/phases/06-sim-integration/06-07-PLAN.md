---
phase: 06-sim-integration
plan: 07
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06]
files_modified: []
autonomous: false
requirements: [RINT-01, RINT-02, RINT-03, RINT-04, RINT-05, RINT-06]
tags: [phase-6, sim-integration, operator-gate, RINT-04, wave-4]
must_haves:
  truths:
    - "Operator runs the rover SITL flow end-to-end: sudo ./install.sh --rover (if not already done), ./sim/rover/start_rover_sim.sh, robot-follow --robot rover --input udp://0.0.0.0:5600 --config configs/rover_simulation.json --webui"
    - "Operator observes: (a) rover spawns in gz GUI; (b) walking actor visible; (c) controller acquires the largest person (AUTO mode pill in web UI flips to 'Following: ID N'); (d) rover physically drives toward / tracks the actor through the walk pattern; (e) no FATAL/ERROR in console scrollback"
    - "Bottom-edge slow-down (RINT-02): when actor is near the bottom of the frame (close to rover), forward velocity drops to 0 — rover stops, doesn't run into the actor. Confirmed by watching gz GUI + checking that twist.linear.x in `gz topic -e -t /cmd_vel` goes to 0 when actor is close"
    - "SIGINT clean shutdown (RINT-06): Ctrl+C on robot-follow stops the rover within ~1 s; `gz topic -e -t /cmd_vel` shows no further messages after Ctrl+C"
    - "Drone path NOT REGRESSED: separately, `robot-follow --robot drone ...` against PX4 SITL still follows a person (run the existing drone gate from Phase 3 03-14 — if it was deferred, this re-runs that gate too)"
    - "Approval recorded in .planning/phases/06-sim-integration/06-07-SUMMARY.md (this plan's SUMMARY) per the standard resume-signal contract (approved / approved-with-deferral / failed / failed-infra)"
  artifacts:
    - path: "documentation only — no code artifacts produced by this plan"
      provides: "Sign-off recorded in 06-07-SUMMARY.md; on approval, ROADMAP.md Phase 6 status flips to Complete and the v1.1 milestone is ready to merge"
  key_links: []
---

<objective>
Phase 6 operator gate. The five preceding plans (06-01..06-06) deliver all
the code + tests for RINT-01..06; this plan is the operator-witnessed
"does it actually work in the gz Garden sim?" check.

Mirrors the 03-14 and 04-05 deferred-operator-gate pattern. The operator
runs the full flow on their machine, fills the scorecard in this file,
records the resume signal.

The deterministic E2E test in 06-06 (TestRoverWalkAcrossThenApproach)
covers the CONTROLLER side of the gate — telemetry assertions on
detection rate + ID retention. This plan covers the things only an
operator can verify with eyes-on:
  * The rover physically moves in gz GUI
  * The walking actor is visible + tracked
  * Bottom-edge slow-down doesn't run the rover into the actor
  * SIGINT cleanly stops the rover (no zombie `/cmd_vel` messages)
  * The drone path didn't regress (separate run against PX4 SITL)
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-sim-integration/RESEARCH.md
@.planning/phases/06-sim-integration/06-01-PLAN.md
@.planning/phases/06-sim-integration/06-02-PLAN.md
@.planning/phases/06-sim-integration/06-03-PLAN.md
@.planning/phases/06-sim-integration/06-04-PLAN.md
@.planning/phases/06-sim-integration/06-05-PLAN.md
@.planning/phases/06-sim-integration/06-06-PLAN.md
@.planning/phases/05-rover-sim/05-VERIFICATION.md
@CLAUDE.md
@sim/rover/README.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → sim + rover-follow processes | Operator runs both terminals locally; gz sim + ros_gz_bridge + video_bridge are local subprocesses. |
| operator browser → robot-follow web UI | Same threat surface as Phase 3 03-14 / 04-05 — local-only, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Operator confusion | rover behavior at bbox bottom edge is non-obvious to a first-time operator | mitigate | Scorecard row 3 has explicit instructions: watch `gz topic -e -t /cmd_vel` for the moment twist.linear.x → 0 when actor approaches. |
| T-06-07-02 | Tampering | operator records false approval | accept | Solo-developer trust model (mirrors 03-14, 04-05). |
| T-06-07-03 | Denial of service | gz sim wedges; rover doesn't move | mitigate | failed-infra resume signal is explicit; verifier interprets as "fix env then re-run", not "Phase 6 regression". |

No code changes; no new dependencies; no ASVS issues.
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Operator-witnessed rover sim full follow gate</name>
  <files>(no code edits — operator runs the flow; sign-off recorded in 06-07-SUMMARY.md)</files>
  <read_first>
    - sim/rover/README.md (the smoke-test recipe)
    - .planning/phases/05-rover-sim/05-VERIFICATION.md (Phase 5 confirmed RSIM stack works on the operator's box)
    - .planning/phases/06-sim-integration/06-04-PLAN.md (the RINT-02 bottom-edge slow-down behavior the operator validates)
    - configs/rover_simulation.json (the rover-tuned ControllerConfig)
  </read_first>
  <action>
    See `<how-to-verify>` below for full operator instructions. This is a
    checkpoint task: the orchestrator pauses until the operator records
    approval/failure in `.planning/phases/06-sim-integration/06-07-SUMMARY.md`
    per the resume-signal contract.

    The flow is:
      1. Verify Phase 5 sim still works (gz topic -l shows /cmd_vel and /camera).
      2. In a fresh terminal: `source setup_env.sh` then `robot-follow --robot rover --input udp://0.0.0.0:5600 --config configs/rover_simulation.json --webui`.
      3. Open the web UI (default port 5001). Watch the AUTO follow.
      4. Walk the actor across the frame (the walk_across_then_approach world handles this automatically).
      5. As the actor approaches the rover, verify the rover slows + stops (RINT-02 bottom-edge).
      6. Ctrl+C drone-follow; verify clean shutdown (no further /cmd_vel messages).
      7. Fill the scorecard.

    Mark each scorecard row {✅, ❌, deferred, n/a} with evidence captured.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-sim-integration/06-07-SUMMARY.md && grep -qE '^(approved|approved-with-deferral|failed|failed-infra)' .planning/phases/06-sim-integration/06-07-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    | # | Criterion | Evidence the operator captures |
    |---|-----------|-------------------------------|
    | 1 | gz sim launches cleanly with `start_rover_sim.sh --world walk_across_then_approach` | gz GUI shows rover + walking actor; gz topic -l includes /cmd_vel, /camera, /model/rover/odometry |
    | 2 | `robot-follow --robot rover ...` connects and AUTO-acquires the largest person | Web UI status bar flips to `Following: ID <N>`; console shows `Now following detection ID: <N>` |
    | 3 | Rover physically drives + tracks the actor through the walk pattern | gz GUI: rover follows the actor's path; `gz topic -e -t /cmd_vel` shows non-zero linear.x and angular.z during follow |
    | 4 | **RINT-02 bottom-edge slow-down**: rover stops before colliding with the actor | When actor is in lowest ~15% of frame: twist.linear.x in `gz topic -e -t /cmd_vel` drops to 0.0 (while twist.angular.z may stay non-zero to keep recentering) |
    | 5 | **RINT-06 SIGINT shutdown**: Ctrl+C stops the rover within 1 s, no further /cmd_vel messages | After Ctrl+C: `gz topic -e -t /cmd_vel --duration 3` is silent; pgrep robot-follow returns empty within ~5 s |
    | 6 | No FATAL / ERROR in robot-follow console | grep -E "FATAL|ERROR" in the captured scrollback returns no entries beyond established pre-Phase-6 baseline |
    | 7 | **Drone path not regressed**: separately, `robot-follow --robot drone ... --connection udpin://0.0.0.0:14540` against PX4 SITL still follows a person | If the 03-14 SITL gate is also deferred, run it now too — same scorecard from 03-14-PLAN.md applies. Otherwise: brief sanity run + confirm drone follows. |
    | 8 | The RINT-04 deterministic test (06-06's TestRoverWalkAcrossThenApproach) passes on this machine | `python -m pytest robot_follow/tests/test_sim_worlds.py::TestRoverWalkAcrossThenApproach -v` reports PASSED (not SKIPPED, not FAILED) |

    All 8 rows MUST hold (or be deferred with explicit rationale) for the gate to pass.
  </acceptance_criteria>
  <done>
    Operator records approval/failure in 06-07-SUMMARY.md. On approval, Phase 6 closes and the v1.1 milestone is ready to merge.
  </done>
</task>

</tasks>

<verification>
No commits in this plan — operator records the resume signal in the SUMMARY file (which is part of the SUMMARY-commit workflow per execute-plan.md).

On approval, the orchestrator updates:
- `.planning/STATE.md` → Phase 6 complete
- `.planning/ROADMAP.md` → Phase 6 row Complete
- `.planning/REQUIREMENTS.md` → RINT-01..06 → Complete

The v1.1 milestone is then ready for merge to main.
</verification>

<success_criteria>
- [ ] gz sim launches cleanly
- [ ] robot-follow --robot rover connects + AUTO-acquires
- [ ] Rover physically follows the actor
- [ ] RINT-02 bottom-edge slow-down verified
- [ ] RINT-06 SIGINT clean shutdown verified
- [ ] No FATAL/ERROR in console
- [ ] Drone path not regressed (separate run)
- [ ] RINT-04 deterministic test passes locally
- [ ] Operator approval recorded
</success_criteria>

<output>
After the operator's verification, create / update `.planning/phases/06-sim-integration/06-07-SUMMARY.md` with the resume signal at line 1 (approved / approved-with-deferral / failed / failed-infra) followed by the filled scorecard and any captured evidence (console excerpts, gz topic output, web UI screenshots if relevant).
</output>
