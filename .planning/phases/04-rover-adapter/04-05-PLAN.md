---
phase: 04-rover-adapter
plan: 05
type: execute
wave: 4
depends_on: [04-04]
files_modified: []
autonomous: false
requirements: [ROVER-01, ROVER-02, ROVER-03, ROVER-04, ROVER-05, ROVER-06, ROVER-07, ROVER-08]
tags: [phase-4, rover, operator-gate, checkpoint, real-rclpy, ROVER-08, wave-4, gate]
must_haves:
  truths:
    - "Operator runs `drone-follow --robot rover --input udp://0.0.0.0:5600` on a Humble-sourced machine and confirms the adapter constructs cleanly (no Python tracebacks; no friendly RuntimeError when ROS IS sourced)"
    - "On a no-rclpy box (or with ROS NOT sourced), `drone-follow --robot rover --input udp://0.0.0.0:5600` exits cleanly with the ROVER-04 friendly RuntimeError logged at ERROR level naming `source /opt/ros/humble/setup.bash` — NOT a Python ImportError traceback"
    - "While the rover adapter is running, `ros2 topic echo /cmd_vel` (or alternative: `ros2 topic hz /cmd_vel`) shows one geometry_msgs/Twist message per control tick (~10-30 Hz); linear.x is in m/s and angular.z is in rad/s (ROVER-06 wire-format verified)"
    - "After `Ros2RoverAdapter.start_session()` returns, the operator can verify (e.g., by sending SIGINT or by inspection) that drone-follow's `on_signal` handler — NOT rclpy's default — handles SIGINT cleanly (ROVER-08 end-to-end against real rclpy)"
    - "`drone-follow --robot rover --help` shows `--cmd-vel-topic`, `--ros-namespace`, `--ros-domain-id` and no drone-only flags (ROVER-05 visibility re-verified on the operator's machine)"
    - "Drone path is NOT regressed: `drone-follow --robot drone --takeoff-landing` (or operator's preferred drone-flow command) runs identically to its pre-Phase-4 state (Plan 04-04 carry-through verification at the runtime layer)"
    - "Operator approval recorded in 04-05-SUMMARY.md per the resume-signal contract (approved / approved-with-deferral / failed / failed-infra)"
  artifacts:
    - path: "documentation only — no code artifacts produced by this plan"
      provides: "Sign-off recorded in .planning/phases/04-rover-adapter/04-05-SUMMARY.md; on approval, ROADMAP.md Phase 4 status flips to Complete and Phase 5 (Rover sim) was already unblocked by Phase 3 — but Phase 6 (Sim integration) cannot start until Phases 4 AND 5 close, so Phase 4 closure here is on the critical path for Phase 6"
  key_links: []
---

<objective>
Wave 4 — Operator-witnessed real-rclpy smoke gate. Closes Phase 4 end-to-end by exercising the wire path against an actual rclpy + geometry_msgs install (NOT the mocked test-suite path covered by Plans 04-01 / 04-03 / 04-04).

This is a CHECKPOINT plan: no automated tasks, no code edits. The operator (the user) runs `drone-follow --robot rover` on a machine with ROS 2 Humble sourced and verifies the wire-level contracts that the unit test suite cannot verify with rclpy mocked:

1. **ROVER-04 happy path**: with ROS sourced, the friendly RuntimeError does NOT fire and the adapter constructs cleanly.
2. **ROVER-04 sad path**: without ROS sourced, the friendly RuntimeError DOES fire with the documented message (NOT a Python ImportError traceback).
3. **ROVER-02 + ROVER-08 (real)**: after `start_session()`, drone-follow's `on_signal` handles SIGINT — not rclpy's default. Verified by sending SIGINT and observing the documented clean-shutdown path.
4. **ROVER-06 wire format (real)**: `ros2 topic echo /cmd_vel` shows geometry_msgs/Twist messages with `linear.x` in m/s and `angular.z` in rad/s.
5. **ROVER-03 spin-thread liveness**: the background spin_once loop does NOT hang shutdown; SIGINT terminates the process cleanly within a few seconds.

**Why this plan is `autonomous: false`:** the dev box where these plans were written has NO `/opt/ros/humble` and NO rclpy installed (RESEARCH § Environment Availability: row 2 "✗ on this dev box"). Plan 04-04's integration smoke locks every contract that CAN be verified with rclpy mocked. The contracts above can only be verified with the real C-extension loaded (e.g., `signal_handler_options=SignalHandlerOptions.NO` actually does or does not clobber SIGINT — mocks pass-through both cases as a no-op). The operator gate is the only honest way to close ROVER-04's environment-dependent path, ROVER-06's wire format against a real DDS bus, and the real ROVER-02/08 SIGINT contract.

**Why this plan, NOT an autonomous verifier:** an autonomous verifier on this dev box would need to install ROS Humble (multi-GB apt operation) — outside Phase 4 scope per RESEARCH § "Out of Scope" (RSIM-05 + the install dependency lives in Phase 5). An autonomous CI-side verifier on a separate Humble runner is possible but not yet wired (no CI infrastructure in this project — confirmed via `git ls-files | grep -E '^\\.github/workflows|/Jenkinsfile|/\\.circleci'` returning empty). The operator gate is the simplest path that delivers reliable closure of the runtime contracts.

Mirrors the structure of `.planning/phases/03-abstraction/03-14-PLAN.md` (Phase 3's operator gate for ABS-11).

Output: human approval recorded in 04-05-SUMMARY.md. On approval, Phase 4 closes; ROVER-01..08 → Complete in REQUIREMENTS.md traceability; ROADMAP.md Phase 4 status flips from "In progress" (or "Not started") to "Complete"; Phase 6 (Sim integration) unblocks once Phase 5 also closes.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rover-adapter/RESEARCH.md
@.planning/phases/04-rover-adapter/04-01-PLAN.md
@.planning/phases/04-rover-adapter/04-02-PLAN.md
@.planning/phases/04-rover-adapter/04-03-PLAN.md
@.planning/phases/04-rover-adapter/04-04-PLAN.md
@.planning/research/PITFALLS.md
@.planning/phases/03-abstraction/03-14-PLAN.md
@robot_follow/robot_api/adapters/ros2_rover.py
@robot_follow/robot_follow_app.py
@CLAUDE.md

<interfaces>
Build state at the start of this plan (post-04-04):
- `robot_follow/robot_api/adapters/ros2_rover.py` exists with the Ros2RoverAdapter class (Plan 04-03)
- `robot_follow/robot_follow_app.py::add_rover_args` registers --cmd-vel-topic, --ros-namespace, --ros-domain-id (Plan 04-02)
- `robot_follow/robot_follow_app.py::run_robot()` rover branch constructs Ros2RoverAdapter via lazy import + RuntimeError catch (Plan 04-04)
- Full pytest suite reports green: ~292 PASS baseline (Phase 3 final) + ~26 net new PASS from Phase 4 (~20 adapter unit tests + 6 CLI dispatch + 2 integration smoke) = ~318 PASS expected
- Zero xfail markers from Phase 4 plans remaining (all stripped by 04-02 / 04-03)
- `drone-follow --robot drone --help` and `drone-follow --robot rover --help` both exit 0 on this no-rclpy dev box
- mavsdk_drone.py is byte-identical to its pre-Phase-4 state across the whole phase (architectural lock verified at every plan boundary)

What's NEW in this plan vs everything before:
- Nothing CODE-WISE. This plan adds no commits, edits no files. Its sole deliverable is the 04-05-SUMMARY.md sign-off.

Operator's Humble machine prerequisites (per CLAUDE.md § Development Machine Setup + RESEARCH § "Environment Availability"):
- Ubuntu 22.04 with Python 3.10+
- ROS 2 Humble installed: `sudo apt install ros-humble-ros-base ros-humble-geometry-msgs`
- ROS sourced AFTER venv: `source setup_env.sh && source /opt/ros/humble/setup.bash` (per Phase 3 plan 03-09's `setup_env.sh` conditional ROS source block at lines 39-55 — if `--robot rover` is in argv, the venv setup script sources ROS for the operator)
- A way to drive `--input udp://0.0.0.0:5600` — either a real rover sim (Phase 5 territory, not yet shipped) or any UDP stream the pipeline can chew on (a dummy `gst-launch-1.0 videotestsrc ! ... ! udpsink host=127.0.0.1 port=5600` will suffice for verifying the wire path — the cmd_vel publish does NOT depend on detecting any actual person, only on the orchestrator calling send_command at least once with a non-target_lost SafetyContext)
- `ros2` CLI for verification: `ros2 topic echo /cmd_vel`, `ros2 topic hz /cmd_vel`, `ros2 node list`

How the scorecard rows map to ROVER requirements:
- Row 1 (cleanly imports + constructs with ROS sourced) → ROVER-01, ROVER-04 happy path
- Row 2 (friendly RuntimeError without ROS sourced) → ROVER-04 sad path
- Row 3 (rover --help shows 3 rover flags, no drone flags) → ROVER-05 (runtime re-verify; suite-level verify in 04-02)
- Row 4 (cmd_vel publishes Twist with correct units) → ROVER-06
- Row 5 (Ctrl+C handled by on_signal, not rclpy default) → ROVER-02, ROVER-08 real-world
- Row 6 (spin thread shuts down within 2-5 seconds) → ROVER-03
- Row 7 (drone path NOT regressed by Phase 4 plans) → carry-through verification at the runtime layer
- Row 8 (full pytest suite green) → composition-of-all-prior-plan-success-criteria; sanity-check run on the operator's machine

If Phase 5 has not landed yet (it should not — Phase 4 and Phase 5 are parallel per ROADMAP, no dependency), the operator does NOT need a working rover sim. A simple UDP videosource as described above is sufficient to drive the pipeline and exercise `send_command`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator machine ↔ Humble apt repos | Operator runs `sudo apt install ros-humble-*` on their machine. Out of scope for this plan; covered by ROS 2 supply-chain documentation (apt.ros.org). |
| Operator machine ↔ Hailo HW or webcam | Operator's local sensor stack — same as every other operator-gate plan in this repo (Phase 3 03-10, 03-12, 03-14). |
| Operator's SUMMARY.md sign-off ↔ project state | A `failed` recorded here blocks Phase 6; a `failed-infra` allows re-running once env is fixed; `approved` closes Phase 4. |
| Test suite already-green ↔ operator's machine | The operator runs `pytest robot_follow/tests --ignore=robot_follow/tests/test_sim_worlds.py -q` BEFORE the SITL flow as a sanity check. If the suite fails on their machine, that's an env issue (rclpy mocking issue at most; the test fixture works on no-rclpy boxes, so a green suite on the operator's machine confirms the test plumbing works there too). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Spoofing | Operator records `approved` to bypass a real failure | accept | Solo-developer trust model; no separation of duties required. Mirrors 03-14 disposition. |
| T-04-05-02 | Tampering | rclpy version drift between Humble's apt version and what RESEARCH expected (rclpy >= 2.0) | mitigate | Operator's preflight checklist (Step 0 below) includes `python -c "import rclpy; print(rclpy.__version__)"`. If version is < 2.0 → record `failed-infra: rclpy version too old`. |
| T-04-05-03 | Repudiation | The "friendly RuntimeError" without ROS sourced is fired but the operator misreads it as an unrelated env error | mitigate | Scorecard Row 2 requires the operator to PASTE the exact error message into 04-05-SUMMARY.md. The expected message contains the precise substrings `"ROS 2 not"` and `"source /opt/ros/humble/setup.bash"` (locked by Plan 04-03 + verified by `test_friendly_error_when_rclpy_missing`). |
| T-04-05-04 | Information Disclosure | Operator's run might log network details (DDS multicast announcements, ROS_DOMAIN_ID values) to console | accept | Operator runs in their own sim/lab environment; standard ROS verbosity. No PII or credentials. |
| T-04-05-05 | Denial of Service | `--robot rover` against a missing UDP source (no Phase 5 sim, no videotestsrc) → pipeline blocks forever waiting for first frame; SIGINT might not propagate cleanly | mitigate | The operator's verification step explicitly drives `--input udp://0.0.0.0:5600` from `gst-launch-1.0 videotestsrc ! ... udpsink port=5600` so the pipeline gets frames immediately. If they prefer to wait for Phase 5's rover sim, the gate is still meaningful: starts ros2_rover, ros2 topic echo confirms node + publisher, Ctrl+C confirms clean shutdown — without needing per-tick cmd_vel publish. |
| T-04-05-06 | Denial of Service | Phase 4 SIGINT-clobber regression (rclpy.init() called without SignalHandlerOptions.NO) escapes unit tests because mocks pass-through | mitigate | Scorecard Row 5 is the real-rclpy SIGINT smoke. Test pairing: TestSignalHandlerPreservation (Plan 04-03 unit) + TestCompositionRootIntegration (Plan 04-04 mocked end-to-end) + this gate (real rclpy end-to-end) = three independent locks on the contract. |
| T-04-05-07 | Elevation of Privilege | An operator could accidentally make code changes during the gate run and check them into the same commit | mitigate | This plan has NO files_modified frontmatter (empty list). The verify gate checks: `git status` is clean before recording `approved`. If there are uncommitted changes, the operator must either commit them under a separate plan (e.g., as 04-06 gap closure) or revert them before signing off. |
| T-04-05-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. The operator's apt install of ros-humble-* is documented as a prerequisite per CLAUDE.md, not as a plan task. |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Operator real-rclpy gate: rover adapter constructs against real rclpy, publishes Twist on /cmd_vel, preserves SIGINT, friendly error when ROS missing, drone path not regressed</name>
  <files>(no code edits — operator runs the flow; sign-off recorded in .planning/phases/04-rover-adapter/04-05-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/04-rover-adapter/RESEARCH.md (PRIMARY context — all 8 ROVER requirements + the rclpy lifecycle checklist + the friendly-error message contract)
    - .planning/phases/04-rover-adapter/04-04-SUMMARY.md (the wire-path landing summary — operator should know which file changes 04-04 made and confirm them on their machine via `git log --oneline`)
    - .planning/phases/04-rover-adapter/04-03-SUMMARY.md (the adapter implementation summary — the exact RuntimeError message string to grep for in Step 2 below)
    - .planning/phases/03-abstraction/03-14-PLAN.md § <how-to-verify> (operator-gate template this plan mirrors)
    - .planning/research/PITFALLS.md § Pitfall 1 + Pitfall 2 (the contracts the gate is verifying)
    - CLAUDE.md § Development Machine Setup (apt install + venv + sourcing order)
    - CLAUDE.md § Running (the drone-follow command shapes the operator will use)
  </read_first>
  <action>
    See <what-built> and <how-to-verify> below for the full operator instructions. This is a checkpoint task: the orchestrator pauses until the operator records approval/failure in `.planning/phases/04-rover-adapter/04-05-SUMMARY.md` per the resume-signal contract.

    The flow is short — Step 0 (preflight + suite green sanity), Step 1 (happy path with ROS sourced), Step 2 (sad path without ROS sourced), Step 3 (Ctrl+C cleanup), Step 4 (drone path not regressed). All five steps must pass for `approved`.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-rover-adapter/04-05-SUMMARY.md && grep -qE '^(approved|approved-with-deferral|failed|failed-infra)' .planning/phases/04-rover-adapter/04-05-SUMMARY.md && cd /home/guyz/code/guyz/hailo-drone-follow && git status --porcelain | (read R; test -z "$R" || (echo "REJECT: uncommitted changes in tree at sign-off time"; false))</automated>
  </verify>
  <acceptance_criteria>
    The scorecard MUST be filled out in 04-05-SUMMARY.md with one of {✅, ❌, deferred, n/a} per row. ALL 8 rows must hold (or be deferred with explicit rationale) for the gate to pass. Mirror the table shape from 03-14-SUMMARY.md.

    | # | Criterion | Evidence the operator captures |
    |---|-----------|-------------------------------|
    | 1 | **ROVER-01 + ROVER-04 happy path:** with ROS sourced, `drone-follow --robot rover --input udp://0.0.0.0:5600` constructs the adapter cleanly | Console banner shows no Python traceback. Operator confirms `import robot_follow.robot_api.adapters.ros2_rover` succeeds with `python -c "..."` against the same venv. Operator pastes the first ~10 console lines into the SUMMARY. |
    | 2 | **ROVER-04 sad path:** without ROS sourced (fresh terminal, no `source /opt/ros/humble/setup.bash`), `drone-follow --robot rover --input udp://0.0.0.0:5600` exits cleanly with the friendly RuntimeError | Operator pastes the LOGGER.error line from the console. It MUST contain both substrings `"ROS 2 not"` and `"source /opt/ros/humble/setup.bash"`. There MUST NOT be a raw Python `ImportError` traceback above this line (the `raise ... from e` chain is acceptable in the underlying-error tail — it's debug info, not the surfaced message). |
    | 3 | **ROVER-05 visibility:** `drone-follow --robot rover --help` shows `--cmd-vel-topic`, `--ros-namespace`, `--ros-domain-id`; `drone-follow --robot drone --help` shows ZERO rover flags | Operator pastes the relevant `grep` output (e.g., `drone-follow --robot rover --help 2>&1 \| grep -E 'cmd-vel\|ros-namespace\|ros-domain'`) showing all 3 rover flags present under rover and absent under drone. |
    | 4 | **ROVER-06 wire format:** `ros2 topic echo /cmd_vel` (or `ros2 topic hz /cmd_vel`) shows Twist messages while drone-follow --robot rover is running | Operator pastes a couple of lines from `ros2 topic echo /cmd_vel`. Expected shape: `linear: {x: <m/s>, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: <rad/s>}`. Critically: `linear.z` MUST be 0.0 (ROVER-07: no ALTITUDE) and `angular.z` MUST be in rad/s — typical reasonable values are roughly < 1.0 in absolute value during normal operation (NOT 90+ which would be deg/s leaking through). Note from RESEARCH § "Planner note for Phase 6": with default ControllerConfig (drone-tuned `max_yawspeed=90`), the published `angular.z` may spike to 90 rad/s — that's DOCUMENTED expected behavior for Phase 4 (Phase 6 RINT-01 ships rover-tuned rad/s config). If observed, mark this row `✅ (angular.z spikes to ~90 rad/s — drone-tuned controller; Phase 6 RINT-01 ships rover config)` rather than `❌`. |
    | 5 | **ROVER-02 + ROVER-08 real:** during a running rover session, Ctrl+C triggers drone-follow's `on_signal` (not rclpy's default) and the pipeline shuts down cleanly | Operator presses Ctrl+C; observes the console banner specific to drone-follow's `on_signal` (the "Shutdown initiated" message or whatever the project's on_signal logs; if no log, the absence of a Python KeyboardInterrupt traceback is the signal that on_signal handled it). Pipeline exits within a few seconds; no zombie Python processes (`pgrep -f robot_follow.robot_follow_app` returns empty after 5s). |
    | 6 | **ROVER-03 spin-thread liveness:** the background rclpy spin thread does NOT hang shutdown | Time-to-exit from Ctrl+C to shell prompt is < 5 seconds (operator times it informally; a stopwatch is not required). No "spin thread did not exit within 2s" WARNING (or if present, that's the documented fallback path — daemon=True ensures the process still exits). |
    | 7 | **Drone path not regressed by Phase 4:** `drone-follow --robot drone` (operator's preferred drone-flow command — typically with `--takeoff-landing` against SITL or USB) runs identically to its pre-Phase-4 behavior | Operator runs one of:  (a) `drone-follow --robot drone --input usb --webui` and confirms the web UI loads and the drone command path is unchanged; OR  (b) `bash sim/start_sim.sh --bridge --world walk_across_then_approach &` + `drone-follow --robot drone --input udp://0.0.0.0:5600 --takeoff-landing --webui` and confirms SITL drone follow still works as it did at the end of Phase 3 (03-14 approval state). If the operator skipped SITL, paste the web UI status bar reading from option (a). |
    | 8 | **Full pytest suite green on the operator's machine** | `pytest robot_follow/tests --ignore=robot_follow/tests/test_sim_worlds.py -q` reports ≥ 318 passed / 0 unexpected failures (baseline ~292 from Phase 3 + ~26 net new from Phase 4). Operator pastes the `tail -5` output. |

    **GATE:** if all 8 rows hold → record `approved` in 04-05-SUMMARY.md. Phase 4 closes; ROVER-01..08 → Complete in REQUIREMENTS.md traceability; ROADMAP.md Phase 4 status flips to "Complete"; Phase 6 unblocks once Phase 5 also closes.

    **If Row 4 shows `angular.z` spikes to ~90 rad/s** (the documented Phase 6 / RINT-01 caveat) → record `approved-with-deferral: rover yaw tuning deferred to Phase 6 RINT-01 (configs/rover_simulation.json)`. This is NOT a Phase 4 gate failure; Phase 4 ships the wire path; Phase 6 ships the tuning. RESEARCH § "Planner note for Phase 6" documents this expected behavior.

    **If Row 7 fails** (drone path regressed) → record `failed: drone-path regression from Phase 4 — <details>`. This would be a Phase 4 bug; the orchestrator will run `/gsd:plan-phase 4 --gaps` to generate a 04-06 gap closure plan. Plan 04-04 has explicit must_haves that the drone path is unchanged, so any regression here is a Phase 4 implementation bug, not a Phase 5/6 carry-through.

    **If Row 2 (sad-path RuntimeError) shows a raw `ImportError` traceback instead of the friendly message** → record `failed: ROVER-04 friendly error path broken — <paste-traceback>`. This is also a Phase 4 implementation bug (Plan 04-03 must_haves include "Defensive RuntimeError on missing rclpy contains both 'ROS 2 not' and 'source /opt/ros/humble/setup.bash' substrings").

    **If infrastructure fails** (operator's Humble machine won't boot ROS, rclpy version too old, network DDS issues, etc.) → record `failed-infra: <reason>`. Verifier interprets `failed-infra` as "fix env then re-run this gate", NOT "Phase 4 regression". The Phase 4 code can stay landed; only the gate sign-off awaits a working env.
  </acceptance_criteria>
  <what-built>
The completed Phase 4 wire path (sum of Plans 04-01 through 04-04):

- `robot_follow/robot_api/adapters/ros2_rover.py` (NEW per 04-03) — `Ros2RoverAdapter` class + `ROVER_CAPS` constant + defensive lazy rclpy import + signal-preserving connect() + Twist publisher + background spin thread + idempotent shutdown.
- `robot_follow/robot_follow_app.py::add_rover_args` (per 04-02) — registers `--cmd-vel-topic`, `--ros-namespace`, `--ros-domain-id` ROVER-05 flags.
- `robot_follow/robot_follow_app.py::run_robot` (per 04-04) — rover branch lazily imports `Ros2RoverAdapter` and constructs it; ROVER-04 RuntimeError is caught + logged + run_robot returns cleanly so the outer pipeline continues without robot control.
- `robot_follow/tests/test_ros2_rover_adapter.py` (NEW per 04-01 + 04-03 + 04-04) — ~22 PASSING tests across 7 classes covering ROVER-01..04 and 06-08 with rclpy mocked.
- `robot_follow/tests/test_cli_help_dispatch.py` (extended per 04-01 + 04-02) — 6 new PASS tests covering ROVER-05 visibility.

Architectural locks preserved across the whole phase (verified at every plan boundary):
- `robot_follow/robot_api/adapters/mavsdk_drone.py` byte-identical from start to end.
- `robot_follow/follow_api/*` untouched (axes-only Capabilities contract per `feedback_robot_abstraction_axes_only.md` honored).

Goal of this gate: verify the wire path against REAL rclpy (not the mocked-test-suite path) — the contracts that mocking cannot validate.
  </what-built>
  <how-to-verify>
**Prerequisites:**
- Operator has a Humble-capable machine: Ubuntu 22.04 with `sudo apt install ros-humble-ros-base ros-humble-geometry-msgs` completed at least once.
- This may be a separate machine from the dev box that ran Plans 04-01..04-04 (RESEARCH § "Environment Availability" notes the dev box has no rclpy). Operator pulls/checks out `feature/rover-support` HEAD on the Humble machine.
- `setup_env.sh` exists at the repo root (Phase 3 03-09 landed the conditional ROS-source block); operator confirms via `grep -n 'ros/humble' setup_env.sh` shows the conditional block at around lines 39-55.
- A way to drive `--input udp://0.0.0.0:5600` — either Phase 5's rover sim if it has landed, or a simple `gst-launch-1.0` videotestsrc as described in Step 1 below.

**Step 0 — Pre-run sanity (cheap, on the Humble machine):**
```bash
cd /path/to/hailo-drone-follow  # adjust path

# Confirm 04-04 commits are in place
git log --oneline -10

# Confirm rclpy is available + version (RESEARCH expected >= 2.0, Humble's default)
source /opt/ros/humble/setup.bash
python -c "import rclpy; print('rclpy:', rclpy.__version__)"
python -c "from rclpy.signals import SignalHandlerOptions; print('SignalHandlerOptions.NO:', SignalHandlerOptions.NO)"
python -c "from geometry_msgs.msg import Twist; t = Twist(); print('Twist default:', t.linear, t.angular)"

# Confirm full pytest suite green on the operator's machine
source setup_env.sh
python -m pytest robot_follow/tests --ignore=robot_follow/tests/test_sim_worlds.py -q
```
Expected: rclpy >= 2.0 (Humble ships 3.x as of 2026); SignalHandlerOptions.NO is a valid enum value; Twist default has zero linear + angular sub-fields; suite ≥ 318 passed / 0 unexpected failures.

If the suite fails on the operator's machine with rclpy errors during `test_ros2_rover_adapter.py` collection or execution, the `rclpy_mock` fixture has a real-rclpy interaction bug — record `failed-infra: rclpy_mock fixture incompatible with installed rclpy <version>` and report so a 04-06 gap closure can be planned.

**Step 1 — Happy path: rover adapter constructs and publishes Twist (Terminal 1):**
```bash
# Fresh terminal — ROS sourced
source /opt/ros/humble/setup.bash
source setup_env.sh

# Drive an input stream so the pipeline gets frames (skip if Phase 5 sim is running)
# Option A: lightweight test source
gst-launch-1.0 -q videotestsrc is-live=true ! \
    video/x-raw,width=640,height=480,framerate=30/1 ! \
    x264enc tune=zerolatency speed-preset=ultrafast ! \
    rtph264pay ! udpsink host=127.0.0.1 port=5600 &
GST_PID=$!

# Run drone-follow as a rover
drone-follow --robot rover --input udp://0.0.0.0:5600 --webui &
DF_PID=$!

# Give it a few seconds to construct + start_session
sleep 5
```

**Step 2 — Verify the Twist wire (Terminal 2, same Humble machine):**
```bash
source /opt/ros/humble/setup.bash

# Check the node is up
ros2 node list
# Expected: /robot_follow_rover  (or similar; whatever Plan 04-03 named the node — likely
# "robot_follow_rover" per RESEARCH § rclpy lifecycle ordering Step 4)

# Check the publisher
ros2 topic list
# Expected: /cmd_vel listed

# Verify publish frequency (should be ~10-30 Hz — matches the pipeline tick rate)
timeout 5 ros2 topic hz /cmd_vel

# Verify wire format
timeout 3 ros2 topic echo /cmd_vel
# Expected output shape:
#   ---
#   linear:
#     x: 0.0       <- forward in m/s (likely 0 because videotestsrc has no person)
#     y: 0.0
#     z: 0.0       <- NO ALTITUDE (ROVER-07)
#   angular:
#     x: 0.0
#     y: 0.0
#     z: 0.0       <- yaw_rate in rad/s (likely 0)
#   ---
```

**IMPORTANT:** with videotestsrc as the input, the pipeline detects no person → controller emits zero RobotCommand → published Twist is all-zero. That's STILL a valid wire-format verification — `ros2 topic echo` returning the correct Twist shape (all the right fields) proves the publisher + serialization work. To see non-zero values, the operator needs a real video source with people (Phase 5's rover sim, a usb camera with a human in frame, or any HEF-compatible source). Recording the all-zero echo is acceptable for Row 4 of the scorecard; non-zero is preferred.

If the operator HAS a real video source (e.g., Phase 5 rover sim or a USB webcam pointed at themselves), substitute that for `--input udp://...` and observe non-zero `linear.x` and `angular.z` as the rover would follow.

Note the Phase 6 caveat: with default `ControllerConfig` (drone-tuned `max_yawspeed=90`), if the controller is actively yawing, `angular.z` may spike to ~90 rad/s. That's documented expected behavior for Phase 4 (Phase 6 RINT-01 ships rover-tuned rad/s config); see scorecard Row 4 notes.

**Step 3 — Sad path: friendly error when ROS not sourced (Terminal 3):**
```bash
# Fresh terminal — ROS NOT sourced
# Use a NEW terminal that has not seen `source /opt/ros/humble/setup.bash`. Confirm:
echo $ROS_DISTRO   # expect empty

cd /path/to/hailo-drone-follow
# DELIBERATELY skip ROS source; activate ONLY the venv (which on this terminal
# has not yet auto-sourced ROS because --robot rover isn't in argv at venv-activate time)
source ./hailo-apps/venv_hailo_apps/bin/activate

# Run the rover
drone-follow --robot rover --input udp://0.0.0.0:5600
```
Expected console output (last few lines):
```
ERROR ... [rover] adapter construction failed: ROS 2 not available — the rover adapter requires rclpy + geometry_msgs.
Fix:
  1) sudo apt install ros-humble-ros-base ros-humble-geometry-msgs
  2) source /opt/ros/humble/setup.bash
  3) re-source setup_env.sh
Underlying error: ...
Pipeline continues without robot control.
```
Operator pastes this block into 04-05-SUMMARY.md for Row 2 evidence.

**CRITICAL:** If instead the operator sees a raw Python traceback ending in `ImportError: No module named 'rclpy'` (without the friendly RuntimeError above it), the ROVER-04 path is broken — record `failed: ROVER-04 friendly error path broken` per the acceptance criteria.

**Step 4 — Clean shutdown (Terminal 1, back to the running rover):**
```bash
# Time the shutdown
time kill -INT $DF_PID

# Verify cleanup
sleep 3
pgrep -f robot_follow.robot_follow_app
# Expected: empty
ros2 node list | grep robot_follow_rover
# Expected: empty (node destroyed before try_shutdown — PITFALLS "Looks Done But Isn't")

# Kill the gst test source
kill $GST_PID 2>/dev/null
```
Expected: shutdown takes < 5 seconds; no zombie processes; the rover node is no longer in `ros2 node list` output (Plan 04-03's shutdown() destroyed it). Row 5 + Row 6 evidence.

**Step 5 — Drone path not regressed (Terminal 4):**
Operator picks ONE of:
- (a) Quick smoke: `drone-follow --robot drone --input usb --webui` (USB camera path, no flight controller needed). Confirm the web UI loads, no Python tracebacks, status bar updates. This exercises the drone construction path + the run_robot dispatch.
- (b) Full SITL re-run (if operator has time/setup): `bash sim/start_sim.sh --bridge --world walk_across_then_approach` + `drone-follow --robot drone --input udp://0.0.0.0:5600 --takeoff-landing --webui`. Confirm behavior matches the Phase 3 03-14 approval state (drone arms, follows actor, status bar updates, clean Ctrl+C).
Row 7 evidence — paste the chosen confirmation.

**Step 6 — Record sign-off in 04-05-SUMMARY.md:**
- Create `.planning/phases/04-rover-adapter/04-05-SUMMARY.md`.
- First line MUST start with `approved` / `approved-with-deferral: <item>` / `failed: <reason>` / `failed-infra: <reason>` (the regex the `<verify><automated>` block matches).
- Below the sign-off line, paste the 8-row scorecard table with ✅/❌/deferred/n/a per row plus the evidence collected in Steps 0-5.
- Note: `git status --porcelain` must be empty when you record sign-off (the gate verify command rejects sign-off if there are uncommitted changes — no code edits are allowed inside this plan).

**If a NEW gap is discovered** (a contract Plan 04-01..04 didn't cover, e.g., DDS QoS mismatch, multi-publisher conflict, or a subtle rclpy version drift) → record `failed: <new-finding>` and the orchestrator will run `/gsd:plan-phase 4 --gaps` to generate a 04-06 gap closure plan. Mirrors Phase 3's 03-10 → 03-11 → 03-12 → 03-13 → 03-14 iteration cycle.
  </how-to-verify>
  <resume-signal>
After the verification flow completes, the operator records ONE of the following in `.planning/phases/04-rover-adapter/04-05-SUMMARY.md` (must be on the first non-comment line for the verify regex to match):

- **"approved"** — all 8 scorecard rows pass; the rover wire works against real rclpy AND the drone path is unchanged. Phase 4 closes; ROVER-01..08 → Complete in REQUIREMENTS.md traceability; ROADMAP Phase 4 status → "Complete"; Phase 6 unblocks (once Phase 5 also closes).
- **"approved-with-deferral: rover yaw tuning deferred to Phase 6 RINT-01"** — all rows pass EXCEPT Row 4 shows `angular.z` spiking to ~90 rad/s (drone-tuned controller); this is documented expected behavior for Phase 4 per RESEARCH § "Planner note for Phase 6". Phase 4 still closes; Phase 6 RINT-01 inherits the requirement to ship `configs/rover_simulation.json`.
- **"approved-with-deferral: SITL drone re-run skipped (operator confirmed pre-Phase-4 state via USB smoke per option a)"** — Row 7 used the lightweight option (a) instead of the full SITL re-run. Phase 4 still closes; the carry-through verification at the runtime layer is satisfied by the USB smoke + the test-suite-level evidence from Plan 04-04's TestCompositionRootIntegration.
- **"failed: <specific contract failure>"** — one of the must-haves did not hold (e.g., Row 2 showed an ImportError traceback instead of friendly RuntimeError; Row 5 showed a SIGINT clobber; Row 7 showed a drone regression). Orchestrator runs `/gsd:plan-phase 4 --gaps` to generate a 04-06 gap closure plan. Phase 4 stays Pending.
- **"failed-infra: <reason>"** — env didn't cooperate (rclpy version too old, Humble not installed, sim wouldn't boot, etc.). Phase 4 code can stay landed; operator fixes the env and re-runs this same gate later. ROADMAP Phase 4 status stays at "In Progress" (or whatever its pre-gate state was).

After recording, the orchestrator's `gsd-verifier` MUST keep `human_needed` raised against Phase 4 ROVER-01..08 until the SUMMARY records `approved` (or `approved-with-deferral`). Mirror the verifier contract from 03-14-SUMMARY.md (Phase 3's analogous gate).
  </resume-signal>
</task>

</tasks>

<verification>
After the operator completes the gate and records sign-off:

```bash
cd /home/guyz/code/guyz/hailo-drone-follow

# 04-05-SUMMARY.md exists and starts with one of the four resume signals
test -f .planning/phases/04-rover-adapter/04-05-SUMMARY.md && grep -qE '^(approved|approved-with-deferral|failed|failed-infra)' .planning/phases/04-rover-adapter/04-05-SUMMARY.md && echo "sign-off line present and well-formed"

# No accidental code changes during the gate (the plan has empty files_modified)
git status --porcelain | (read R; test -z "$R" && echo "no uncommitted changes — code-edit-free gate honored")

# Architectural locks across the entire phase (most expensive check; run last)
git log --oneline feature/rover-support -- robot_follow/robot_api/adapters/mavsdk_drone.py | (read R; echo "mavsdk_drone.py git log on this branch: ${R:-empty (no Phase 4 commits touched it)}")
git log --format='%h %s' -- robot_follow/robot_api/adapters/mavsdk_drone.py | head -5
# ^ visual check: the most recent commit listed should be Phase 3 territory, not Phase 4
```

Expected:
- 04-05-SUMMARY.md exists and the first non-comment line matches the resume-signal regex
- Working tree is clean (no uncommitted edits — operator did not accidentally check changes into this gate)
- mavsdk_drone.py's most recent commit is Phase 3 (architectural lock held across the whole Phase 4)

Commit shape: ZERO commits in this plan. The plan is a documentation-only checkpoint; its only artifact is `.planning/phases/04-rover-adapter/04-05-SUMMARY.md` and ROADMAP.md updates that the orchestrator (NOT this plan) makes after the verifier accepts the sign-off.

If the operator does NEED to make a code edit during the gate (e.g., a tiny tweak to clarify a log message they noticed during the run), that edit goes into a SEPARATE 04-06 gap closure plan with its own PLAN.md and SUMMARY.md. This plan stays code-edit-free.

Bisectability: this plan contributes no commits, so bisection is unaffected. The state of the tree at the end of this plan is byte-identical to the state at the end of Plan 04-04.

Parallel-wave hygiene: depends on 04-04 only (the wire path must be complete before the gate runs). No parallel siblings.
</verification>

<success_criteria>
- [ ] `.planning/phases/04-rover-adapter/04-05-SUMMARY.md` exists with the first non-comment line matching `^(approved|approved-with-deferral|failed|failed-infra)`
- [ ] If `approved`: all 8 scorecard rows hold (or Row 4 is `approved-with-deferral` per the Phase 6 RINT-01 caveat)
- [ ] If `approved-with-deferral`: the deferral is one of the documented patterns (Phase 6 yaw tuning OR SITL re-run skipped)
- [ ] If `failed`: the orchestrator's next step is to run `/gsd:plan-phase 4 --gaps` for a 04-06 gap closure plan
- [ ] If `failed-infra`: env fix + re-run this same gate; no Phase 4 code change required
- [ ] `git status --porcelain` returns empty at sign-off time (no uncommitted code edits in the operator's workspace — this is a documentation-only gate)
- [ ] Architectural lock: mavsdk_drone.py's most recent commit on `feature/rover-support` is Phase 3 territory (no Phase 4 commits touched it)
- [ ] Scorecard evidence captured: 8 rows of {✅, ❌, deferred, n/a} + verbatim console pastes for Rows 2, 4, 5, 7
</success_criteria>

<output>
The operator creates `.planning/phases/04-rover-adapter/04-05-SUMMARY.md` with the following shape (mirror the 03-14-SUMMARY.md structure):

```markdown
# Plan 04-05 — Operator real-rclpy gate — SUMMARY

approved   <!-- or one of: approved-with-deferral: <item>, failed: <reason>, failed-infra: <reason> -->

## Gate context
- Date run: YYYY-MM-DD
- Machine: <hostname or shorthand — e.g., "humble-laptop", "rover-test-rig">
- ROS distro confirmed: humble (`echo $ROS_DISTRO` → humble)
- rclpy version: <output of `python -c "import rclpy; print(rclpy.__version__)"`>
- Phase 4 commits in scope: `git log --oneline -10` paste

## Scorecard

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | ROVER-01 + ROVER-04 happy path: adapter constructs with ROS sourced | ✅ | <paste first ~10 console lines from Step 1> |
| 2 | ROVER-04 sad path: friendly RuntimeError without ROS sourced | ✅ | <paste the LOGGER.error block from Step 3> |
| 3 | ROVER-05 visibility: --robot rover --help shows 3 rover flags; --robot drone --help shows none | ✅ | <paste grep outputs> |
| 4 | ROVER-06 wire format: ros2 topic echo /cmd_vel shows Twist with correct units | ✅ | <paste a few lines from `ros2 topic echo /cmd_vel`> |
| 5 | ROVER-02 + ROVER-08 real: SIGINT handled by on_signal not rclpy default | ✅ | <observed clean shutdown; no zombie process> |
| 6 | ROVER-03 spin-thread liveness: < 5s from Ctrl+C to shell prompt | ✅ | <paste `time kill -INT` output> |
| 7 | Drone path not regressed: --robot drone runs identically to pre-Phase-4 | ✅ | <option (a) or (b) confirmation> |
| 8 | Full pytest suite green on operator's machine: ≥ 318 passed | ✅ | <paste `pytest tail -5`> |

## Notes / deferrals
- <e.g., "Row 4 showed angular.z spiking to 90 rad/s during a yaw — documented Phase 6 RINT-01 behavior; recording as approved-with-deferral">
- <any other observations>

## Sign-off
- Phase 4 ROVER-01..08 → Complete in REQUIREMENTS.md traceability (orchestrator updates this after verifier accepts)
- ROADMAP.md Phase 4 status → "Complete" (orchestrator updates this)
- Phase 5 was unblocked from start of Phase 4 (parallel); Phase 6 unblocks once Phase 5 ALSO closes
```
</output>
</content>
</invoke>
