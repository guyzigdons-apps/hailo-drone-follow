---
phase: 03-abstraction
plan: 12
type: execute
wave: 9
depends_on: [03-11]
files_modified: []
autonomous: false
gap_closure: true
requirements: [ABS-11]
tags: [operator-gate, SITL, checkpoint, ABS-11, re-run]
must_haves:
  truths:
    - "Operator re-runs `sim/start_sim.sh --bridge --world walk_across_then_approach` + `drone-follow --robot drone --input udp://0.0.0.0:5600 --takeoff-landing --webui` and confirms the drone follows the walking actor for the full walk pattern with the web UI status bar updating live"
    - "F1 closed: web UI status bar shows mode ∈ {AUTO, LOCKED, LOST, IDLE} with non-zero forward/yaw values during follow; updates at ≥5 Hz (observable visually as a smoothly-changing readout, not a frozen panel)"
    - "F2 closed: no `ctrl: bh=... factor=... filtered=...` INFO lines in the console during the run; same data may appear under DEBUG if `--debug` flag is set"
    - "All original 03-10 acceptance criteria still pass (drone arms + follows actor + altitude held + smooth motion + clean Ctrl+C shutdown + no new FATAL/ERROR)"
    - "Operator approval recorded in 03-12-SUMMARY.md per the resume-signal contract"
  artifacts:
    - path: "documentation only — no code artifacts"
      provides: "Sign-off recorded in 03-12-SUMMARY.md; 03-10-SUMMARY.md first line may also be updated to 'approved (re-verified in 03-12)' per the 03-10 protocol"
  key_links: []
---

<objective>
Wave 9 final — Operator-witnessed SITL drone follow gate, RE-RUN after gap closure 03-11. Closes ABS-11 (the gate that failed in 03-10).

Purpose: verify that the F1 + F2 fixes from 03-11 (orchestrator-level ui_state publish + INFO log demotion) actually resolve the observed gaps in the live SITL flow — AND that the original 03-10 acceptance criteria (drone arms / follows / altitude held / smooth motion / clean shutdown) still hold. The first SITL run found two gaps; this re-run must confirm BOTH are closed and NO new regression was introduced by the wire-through.

This plan has NO automated tasks. It is a CHECKPOINT plan: pauses for operator verification, then approval (or another failure) is recorded in 03-12-SUMMARY.md.

Output: human approval recorded in 03-12-SUMMARY.md. On approval, Phase 3 closes and rover work (Phase 4 + Phase 5) may begin.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-abstraction/03-CONTEXT.md
@.planning/phases/03-abstraction/03-10-PLAN.md
@.planning/phases/03-abstraction/03-10-SUMMARY.md
@.planning/phases/03-abstraction/03-11-PLAN.md
@CLAUDE.md
@.claude/skills/drone-follow-dev/SKILL.md

<interfaces>
SITL prerequisites (unchanged from 03-10):
- `sim/setup_sim.sh` already run (one-time PX4-Autopilot submodule init + build per CLAUDE.md § Simulation)
- Gazebo Garden installed (`gz-garden` package)
- `sim/worlds/walk_across_then_approach.sdf` present
- Hailo HW connected (for the AI pipeline) — operator-side hardware
- Phase 3 build state at start of this plan: 03-01..09 landed + 03-11 commits landed; full pytest suite green; `robot-follow --help` and `drone-follow --help` both exit 0

What's NEW in this re-run vs 03-10:
1. **F1 fix in flight (gap closure ABS-11 / 03-11):** `run_robot_loop` publishes `(forward, down, yawspeed, mode)` to `SharedUIState.update_velocity` after each tick branch. The web UI's status bar above the video feed will show mode + speeds that change every ~100ms during follow.
2. **F2 fix (log demotion):** `ctrl: bh=... factor=... filtered=...` lines no longer appear at INFO in the default console output. They are emitted at DEBUG (visible only with `--debug` or equivalent log-level override).

This SITL re-run exercises the SAME code path as 03-10 (MavsdkDroneAdapter + run_robot_loop + altitude P + retreat-from-tilt + smoothing) PLUS the new UI publish path. The adapter is unchanged — axes-only Capabilities contract preserved.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Operator SITL re-run gate: F1 + F2 closed AND original 03-10 acceptance still holds</name>
  <files>(no code edits — operator runs SITL flow; sign-off recorded in .planning/phases/03-abstraction/03-12-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/03-abstraction/03-10-SUMMARY.md (the failing report — operator should re-read F1 / F2 descriptions to know exactly what failed and what should now be fixed)
    - .planning/phases/03-abstraction/03-11-PLAN.md (the fix plan — operator should know what code changed)
    - .planning/phases/03-abstraction/03-10-PLAN.md § <how-to-verify> (the SITL flow operator runs — Step 1-5 are unchanged; only the gate condition expands)
    - CLAUDE.md § Simulation (sim setup commands)
    - .claude/skills/drone-follow-dev/SKILL.md (port hygiene + kill recipes — needed if the previous SITL run left zombies)
  </read_first>
  <action>
    See <what-built> and <how-to-verify> below for the full operator instructions. This is a checkpoint task: the orchestrator pauses until the operator records approval/failure in `.planning/phases/03-abstraction/03-12-SUMMARY.md` per the resume-signal contract.

    The SITL flow is IDENTICAL to 03-10 Step 1-5; the difference is the EXPANDED gate condition (acceptance criteria below). The operator should mentally diff against 03-10's observed behavior: (a) status bar updates during follow, (b) no per-tick INFO log spam, (c) drone follow quality unchanged.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-abstraction/03-12-SUMMARY.md && grep -qE '^(approved|approved-with-deferral|failed)' .planning/phases/03-abstraction/03-12-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    All of the following must hold (original 03-10 + new F1 + new F2):

    **Original 03-10 criteria (carry-over):**
    - ✅ Drone arms + takes off cleanly with `--takeoff-landing`
    - ✅ Drone faces and follows the actor for the full `walk_across_then_approach` pattern (target not lost during normal walk)
    - ✅ Altitude held within ±0.5 m of `--target-altitude` (3 m default)
    - ✅ Smooth motion (no oscillation/jerk; EMA smoothing visible as gradual velocity changes)
    - ✅ Clean Ctrl+C shutdown with land (no zombie `mavsdk_server` 5s after exit per `pgrep mavsdk_server`)
    - ✅ No new FATAL / ERROR log lines vs pre-Phase-3

    **F1 closure (new gate condition — the original failure):**
    - ✅ Web UI status bar above the video feed shows mode + velocity values that CHANGE during follow (NOT frozen at `IDLE | Fwd 0.00 | Down 0.00 | Yaw 0.0`)
    - ✅ Mode reflects orchestrator state: `AUTO` while following the largest person (no operator click), `LOCKED` if a person is clicked in the UI to lock-on (if LOCKED wiring landed in 03-11; otherwise `AUTO` is acceptable per 03-11's documented simplification), `LOST` if target leaves frame, `IDLE` on Ctrl+C shutdown
    - ✅ Status bar update rate is visually ≥5 Hz — the values should change smoothly rather than tick noticeably. Operator's subjective "the readout looks alive" is the gate (no automated rate counter required, but if the operator wants to capture evidence, browser devtools → Network → SSE stream shows event rate ≥5/s)
    - ✅ During the actor's "approach the drone" segment (back half of the walk), the published forward velocity goes NEGATIVE (retreat) and the operator sees this in the status bar — confirms pre-smoothing values are flowing through

    **F2 closure (log demotion):**
    - ✅ Default console output (no `--debug` flag, no log-level override) shows NO `ctrl: bh=... bottom=... target=... factor=... filtered=...` lines during the SITL run
    - ✅ (Optional) Re-running with `--debug` or an env var that enables DEBUG-level logging DOES show the `ctrl: bh=...` lines, confirming the data is preserved at the right level (not deleted outright)

    **Re-run-specific (new regression-check):**
    - ✅ Drone follow quality is INDISTINGUISHABLE from the 03-10 run (the only thing that changed is UI publish + log level — the adapter is unchanged; if the operator notices any new oscillation, drift, or stuck-at-zero behavior that wasn't present in the 03-10 attempt, THAT IS A NEW REGRESSION introduced by 03-11 and must be reported)
    - ✅ No new pytest failures introduced (operator runs `cd /home/<user>/code/<user>/hailo-drone-follow && hailo-apps/venv_hailo_apps/bin/pytest robot_follow/tests/ -x --ignore=robot_follow/tests/_reid_gate.py` as a final smoke check; suite still green)

    **GATE:** if all of the above hold → record `approved` in 03-12-SUMMARY.md; Phase 3 closes; rover work (Phase 4 + Phase 5) may begin in parallel.
  </acceptance_criteria>
  <what-built>
The 03-11 gap closure on top of the full Phase 3 abstraction stack:

03-11 deliverables (new since 03-10):
- `run_robot_loop` in `robot_api/orchestrator.py` gained an optional `ui_state` parameter and publishes `(forward, down, yawspeed, mode)` after each tick branch and in the shutdown finally
- Mode strings are robot-agnostic (`AUTO`, `LOCKED`, `LOST`, `IDLE`) — symmetric for Phase 4 rover reuse
- Values published are the controller's PRE-smoothing `cmd` (matches pre-Phase-3 behavior; documented inline; pinned by `test_publish_uses_pre_smoothing_cmd`)
- `MavsdkDroneAdapter` is UNCHANGED — axes-only Capabilities contract preserved (the adapter must never know about UI state)
- `hailo_drone_detection_manager.py:533` `LOGGER.info("ctrl: bh=...")` demoted to `LOGGER.debug`
- New test file `test_orchestrator_ui_update.py` with 5 passing tests (FakeRobot + SpyUIState pattern)
- `robot_follow_app.py::run_robot()` passes `ui_state=ui_state` into `run_robot_loop`

Goal: verify the gap closure resolves F1 + F2 in a live SITL run AND that no new regression was introduced.
  </what-built>
  <how-to-verify>
**Prerequisites (same as 03-10):**
- Operator has Hailo HW + Gazebo Garden + PX4 SITL setup per CLAUDE.md § Simulation
- `sim/setup_sim.sh` has been run at least once
- Optional: kill any stale processes from the 03-10 run via `.claude/skills/drone-follow-dev/SKILL.md` recipes

**Step 0 — Pre-run sanity (cheap, run on the dev box):**
```bash
cd /home/<user>/code/<user>/hailo-drone-follow

# Confirm 03-11 commits are in place
git log --oneline -5

# Confirm new test passes
hailo-apps/venv_hailo_apps/bin/pytest robot_follow/tests/test_orchestrator_ui_update.py -v

# Confirm full suite green
hailo-apps/venv_hailo_apps/bin/pytest robot_follow/tests/ -x --ignore=robot_follow/tests/_reid_gate.py
```
Expected: 5/5 new tests pass; full suite green; no new failures.

**Step 1 — Start SITL (Terminal 1):**
```bash
sim/start_sim.sh --bridge --world walk_across_then_approach
```
Expected: Gazebo opens, PX4 boots, walking actor visible, MAVLink relay on udp:14540.

**Step 2 — Start drone-follow (Terminal 2):**
```bash
source setup_env.sh
drone-follow --robot drone --input udp://0.0.0.0:5600 --takeoff-landing --webui
```

Expected at startup:
- Console banner shows offboard handshake messages (same as 03-10)
- Web UI accessible at http://localhost:5001 with live MJPEG stream
- Drone arms + takes off to target_altitude (3 m default)
- **NEW:** Console does NOT spam `ctrl: bh=...` INFO lines every tick (F2 closed)

**Step 3 — Observe target-follow AND web UI status bar (5+ minutes):**

This is the gap-closure-specific observation step. The actor walks across the field then approaches the drone. Operator must watch BOTH the SITL view AND the web UI status bar simultaneously.

3a. **Web UI status bar (NEW for F1):**
   - Status bar above the video feed should show: `<MODE> | Fwd <N.NN> m/s | Down <N.NN> m/s | Yaw <N.N> deg/s`
   - **At rest before takeoff or in IDLE:** `IDLE | Fwd 0.00 | Down 0.00 | Yaw 0.0` (frozen — same as 03-10)
   - **During follow (post-takeoff, actor walking):** mode shows `AUTO` (or `LOCKED` if explicitly clicked); forward/yaw values CHANGE smoothly several times per second. This is the F1 fix — values must NOT stay frozen.
   - **During approach segment (back half of the walk):** forward velocity should swing NEGATIVE as retreat-from-tilt + emergency-safety kicks in (because the actor's bbox grows past the target size). Operator sees a visible sign change. This is the pre-smoothing pin in action.
   - **If target lost (actor disappears, e.g. behind sim geometry):** mode flips to `LOST` within `search_enter_delay_s` (default ~0.5s); velocities go to 0.0.

3b. **Drone behavior (unchanged from 03-10):**
   - Drone faces the actor (yaw tracks position)
   - Drone maintains target bbox height (forward in / out as actor walks away / approach)
   - Altitude held at ~3 m (no significant drift)
   - Smooth motion — no jerky setpoints
   - No "stuck" behavior — drone doesn't hold zero when actor is in frame
   - No NaN / clamping artifacts

3c. **Console output (F2 check):**
   - In the default invocation (no `--debug`), `ctrl: bh=... factor=... filtered=...` lines should NOT appear in the console. The DEBUG band may still emit them if a log-level override is set.

**Step 4 — Compare to 03-10 baseline (subjective regression check):**
- Operator mentally diffs against the 03-10 observed behavior. The drone follow quality should be IDENTICAL. If the operator notices any NEW oscillation, drift, stuck-at-zero, or weird control behavior that 03-10 did not show, that is a REGRESSION introduced by 03-11 and must be reported as a failure.

**Step 5 — Ctrl+C clean shutdown (same as 03-10):**
- Press Ctrl+C in Terminal 2
- Expected: drone lands cleanly; MAVSDK server reaper fires; no zombie processes
- **NEW:** the web UI status bar's last visible mode should be `IDLE` (orchestrator finally-block publish)

**Step 6 — Post-run sanity:**
- Check the console scrollback for any FATAL / ERROR lines that weren't there in 03-10
- Check `pgrep mavsdk_server` is empty 5s after exit
- (Optional) re-run with `--debug` or LOG_LEVEL=DEBUG to confirm the F2 `ctrl: bh=...` lines reappear at DEBUG level (sanity: data is preserved, not deleted)

**If the SITL flow fails for INFRASTRUCTURE reasons** (sim won't boot, Hailo not detected, PX4 doesn't connect), that is NOT a Phase-3 regression — record `failed-infra: <reason>` in 03-12-SUMMARY.md instead of `failed` so the next planner knows to look outside the code.
  </what-built>
  <resume-signal>
After the SITL run completes, the operator records ONE of the following in `.planning/phases/03-abstraction/03-12-SUMMARY.md`:

- **"approved"** — F1 + F2 closed, original 03-10 acceptance still holds, no new regression. Phase 3 closes; rover work (Phase 4 + Phase 5) unblocks.
- **"approved-with-deferral: {item}"** — primary criteria met; one or more secondary items deferred (e.g., ROS env-leak step deferred to Phase 4 per the 03-10 precedent). Phase 3 closes; deferral noted in STATE.md.
- **"failed: {description}"** — F1 still broken OR F2 still spammy OR new regression observed. Phase 3 does NOT close; operator files a gap report describing what's still broken. `/gsd:plan-phase 3 --gaps` runs again to plan another fix.
- **"failed-infra: {reason}"** — SITL flow could not run due to environment issues (sim won't boot, hardware not available). NOT a Phase-3 regression; next step is to fix the infrastructure and re-run this gate.

Phase 1 precedent: same three-way (approved / approved-with-deferral / failed) signal pattern.

On approval, the operator (or the orchestrator) should also update `.planning/phases/03-abstraction/03-10-SUMMARY.md` first line to `approved (re-verified in 03-12 after gap closure 03-11)` per the 03-10 protocol's last paragraph.
  </resume-signal>
</task>

</tasks>

<verification>
This plan has no automated verification by design — it is a human checkpoint. The verifier (`/gsd:verify-work`) keeps `human_needed` raised against ABS-11 until 03-12-SUMMARY.md is created with one of the resume-signal strings.

No code commits in this plan. Operator may choose to commit the SUMMARY in version control (recommended):
```bash
git add -- .planning/phases/03-abstraction/03-12-SUMMARY.md .planning/phases/03-abstraction/03-10-SUMMARY.md
git commit -m "docs(03-12): operator SITL re-run approval — Phase 3 closed (ABS-11)"
```

Parallel-wave hygiene: serial dependency on 03-11 (gap fixes must land first); no parallel siblings.
</verification>

<success_criteria>
- [ ] Operator runs the SITL flow per Step 0-6 above
- [ ] F1 closed: web UI status bar updates with mode + non-zero velocities during follow
- [ ] F2 closed: no `ctrl: bh=...` INFO log spam in default console
- [ ] Original 03-10 criteria still hold (arm + follow + altitude + smooth + clean shutdown + no FATAL/ERROR)
- [ ] No new regression introduced by 03-11 (follow quality indistinguishable from 03-10)
- [ ] Full pytest suite still green
- [ ] `.planning/phases/03-abstraction/03-12-SUMMARY.md` exists with one of: `approved`, `approved-with-deferral: ...`, `failed: ...`, or `failed-infra: ...`
- [ ] If approved: ABS-11 marked Complete in REQUIREMENTS.md traceability; Phase 3 closes; 03-10-SUMMARY.md first line updated to `approved (re-verified in 03-12 after gap closure 03-11)`
- [ ] If failed: gap report filed; Phase 3 stays open until next gap-closure plan lands
</success_criteria>

<output>
After completion, create `.planning/phases/03-abstraction/03-12-SUMMARY.md` recording:
- Operator's approval signal (approved / approved-with-deferral / failed / failed-infra)
- World used (default: walk_across_then_approach)
- SITL run duration (target ≥5 minutes)
- F1 status: was the status bar live during follow? Did mode change as expected? Did forward velocity swing negative on approach?
- F2 status: was the console quiet (no `ctrl: bh=...` INFO spam)?
- Regression check: any new oscillation / drift / stuck-at-zero vs 03-10? (yes/no, with description if yes)
- Ctrl+C shutdown clean? (yes/no, with zombie process notes)
- Final mode shown in UI before exit (should be `IDLE`)
- Any other anomalies (with timestamps for log-correlation)
- Recommendation: proceed to `/gsd:verify-work` (Phase 3 closes; rover phases unblock) OR file another gap report

If approved, also append a one-line update to `.planning/phases/03-abstraction/03-10-SUMMARY.md` per the 03-10 protocol's last paragraph: replace the first line `failed: ...` with `approved (re-verified in 03-12 after gap closure 03-11)`.
</output>
