---
phase: 03-abstraction
plan: 14
type: execute
wave: 11
depends_on: [03-13]
files_modified: []
autonomous: false
gap_closure: true
requirements: [ABS-11]
tags: [operator-gate, SITL, checkpoint, ABS-11, re-run-2, F3]
must_haves:
  truths:
    - "Operator re-runs `sim/start_sim.sh --bridge --world walk_across_then_approach` + `drone-follow --robot drone --input udp://0.0.0.0:5600 --takeoff-landing --webui` and confirms the drone follows the walking actor for the full walk pattern with the web UI status bar updating live AND click-to-lock working on the visible person"
    - "F3 closed: clicking a person bbox in the web UI overlay locks the drone onto the visible person even when the click handler captured a stale ByteTracker id — the 03-13 server-side leniency widening triggers under the single-actor `walk_across_then_approach` scenario"
    - "F1 STAYS closed: web UI status bar shows mode ∈ {AUTO, LOCKED, LOST, IDLE} with non-zero forward/yaw values during follow; updates at ≥5 Hz (regression check vs 03-12-SUMMARY scorecard rows 7 + 8)"
    - "F2 STAYS closed: no `ctrl: bh=... factor=... filtered=...` INFO lines in the console during the run (regression check vs 03-12-SUMMARY scorecard row 9)"
    - "AUTO mode is NOT regressed by the F3 fix: when the operator does NOT click, the drone selects the largest visible person and follows it identically to the 03-10 / 03-12 AUTO behavior (the 03-13 leniency widening only fires on operator click)"
    - "All original 03-10 + 03-12 acceptance criteria still pass (drone arms + follows actor + altitude held + smooth motion + clean Ctrl+C shutdown + no new FATAL/ERROR)"
    - "Operator approval recorded in 03-14-SUMMARY.md per the resume-signal contract (approved / approved-with-deferral / failed / failed-infra)"
  artifacts:
    - path: "documentation only — no code artifacts produced by this plan"
      provides: "Sign-off recorded in .planning/phases/03-abstraction/03-14-SUMMARY.md; on approval, ROADMAP.md Phase 3 status flips to Complete and Phases 4+5 unblock"
  key_links: []
---

<objective>
Wave 11 final — Operator-witnessed SITL drone follow gate, THIRD re-run after gap closure 03-13. Closes ABS-11 (the gate that failed in 03-10 and 03-12).

Purpose: verify that the F3 fix from 03-13 (server-side leniency widening for stale ByteTracker ids when `len(available_ids) == 1`) resolves the observed click-to-lock failure in a live SITL run — AND that F1 + F2 stay closed AND no new regression was introduced. The first SITL gate (03-10) failed on F1 + F2; the second SITL gate (03-12) confirmed F1 + F2 fixed but surfaced F3. This third re-run must confirm F3 closed AND F1/F2 still closed.

This plan has NO automated tasks. It is a CHECKPOINT plan: pauses for operator verification, then approval (or another failure) is recorded in 03-14-SUMMARY.md.

**Verifier contract (per 03-12-SUMMARY.md lines 92-93):** `gsd-verifier` MUST keep `human_needed` raised against ABS-11 until this plan's SUMMARY records `approved` (or `approved-with-deferral`). Three SITL attempts on this gate is unusual but defensible: F1 was a Phase-3 regression (03-07's deletion of `live_control_loop`'s UI publish call site) and F3 was a pre-existing UX bug surfaced under operator scrutiny. The verifier MUST NOT auto-close based on test-suite green alone.

Output: human approval recorded in 03-14-SUMMARY.md. On approval, Phase 3 closes, ABS-11 → Complete in REQUIREMENTS.md traceability, and rover work (Phase 4 + Phase 5) unblocks in parallel.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-abstraction/03-CONTEXT.md
@.planning/phases/03-abstraction/03-10-PLAN.md
@.planning/phases/03-abstraction/03-10-SUMMARY.md
@.planning/phases/03-abstraction/03-11-SUMMARY.md
@.planning/phases/03-abstraction/03-12-PLAN.md
@.planning/phases/03-abstraction/03-12-SUMMARY.md
@.planning/phases/03-abstraction/03-13-PLAN.md
@CLAUDE.md
@.claude/skills/drone-follow-dev/SKILL.md

<interfaces>
SITL prerequisites (unchanged from 03-10 / 03-12):
- `sim/setup_sim.sh` already run (one-time PX4-Autopilot submodule init + build per CLAUDE.md § Simulation)
- Gazebo Garden installed (`gz-garden` package)
- `sim/worlds/walk_across_then_approach.sdf` present
- Hailo HW connected (for the AI pipeline) — operator-side hardware
- Phase 3 build state at start of this plan: 03-01..09 landed + 03-11 commits landed + 03-13 commits landed; full pytest suite green at 292 passed / 7 skipped (or higher); `robot-follow --help` and `drone-follow --help` both exit 0

What's NEW in this re-run vs 03-12 (post-rewrite — see 03-13/15/16 SUMMARYs):

1. **F3 closed at the CLIENT layer (gap closure ABS-11 / 03-13, commit `596ff63`):** `robot_follow/ui/src/App.jsx` now mirrors the latest SSE detections via a `detectionsRef`. On bbox click, `handleFollow` resolves the bbox's CURRENT id at click time via a nearest-center match (instead of the React closure's stale render-time id). The display id is unchanged; only the id sent to the server is fresh. `follow_server` is untouched by 03-13.

2. **F4a — server widening reverted (gap closure / 03-15, commit `5bd69b9`):** the earlier 03-13 draft that added `if len(available_ids) == 1` recovery in `do_POST` was REJECTED by the operator (wrong layer — see `~/.claude/projects/-home-guyz-code-guyz-hailo-drone-follow/memory/feedback_click_to_follow_id_resolution.md`). The widening is fully reverted; `do_POST` returns to its pre-72add07 strict contract (404 + JSON + INFO log on unavailable id). The `Stale detection ID …; locking onto single available …` log line no longer exists. A new test (`test_post_follow_stale_id_with_single_available_returns_404`) pins the strict-404 behavior so any future re-introduction is caught.

3. **F4b — bbox-from-body protocol (gap closure / 03-15, commit `4d03f69`):** `POST /follow/<id>` now accepts an optional JSON body `{ "bbox": { "h": <number> } }` carrying the bbox height of the client-resolved detection. `do_POST` reads the body (Content-Length-bounded ≤4096; JSON-safe; value-checked in (0, 1]) and applies it directly via `capture_bbox_setpoint_from_height(controller_config, h, source="CLICK")`, bypassing the `SharedUIState` ↔ `SharedDetectionState` race that produced `bbox height n/a` in 03-12 SITL. Body absent / malformed / out-of-range → fall back to the existing `_capture_bbox_for_id` path. The success-path INFO log line now reports the source: `Now following detection ID: 45 (bbox height 0.180, source: POST body)` (or `source: ui_state` for fallback, `source: n/a` only if both paths fail).

4. **F5 — server-side bbox-match recovery (03-16, commits `070550c` + `72f7ba3`):** the 03-14 first attempt (after 03-13 + 03-15) still surfaced `Detection ID 1 not found. Available: {45}` because the client lags the server — by the time the POST reaches the server, the pipeline has advanced past what the client knew at click time. 03-16 closes that race structurally:
   - `SharedDetectionState` now carries `{id: bbox}` atomically via `get_available_bboxes()`. Pipeline plumbs `filtered_tlwh_by_id` through.
   - Body schema is `{bbox: {x, y, w, h}}` (full geometry, not just `h`).
   - When `requested_id` is NOT in `available_ids` AND the body has a bbox, server computes IoU vs every available bbox; if best IoU ≥ 0.3 it locks that id, logging `Stale detection ID 1 → bbox-matched 45 (IoU=0.85, available=[45])`. Else strict 404 with enriched log.
   - Success-path INFO log adds `id_source: requested|bbox-match` so the operator can see in scrollback which path served the click.

5. **No code changes vs 03-12 beyond the four above.** F1 (orchestrator ui_state publish) and F2 (log demotion) from 03-11 are still in place. `MavsdkDroneAdapter` and the entire `robot_api/` subtree are byte-identical pre/post the 03-13 + 03-15 + 03-16 gap-closure cycle. The axes-only Capabilities contract is preserved.

This SITL re-run exercises the SAME code path as 03-12 (MavsdkDroneAdapter + run_robot_loop + altitude P + retreat-from-tilt + smoothing + UI publish at orchestrator boundary + demoted F2 log) PLUS the new client-side click-to-lock resolution AND the new body-aware bbox capture. The adapter remains unchanged — axes-only Capabilities contract preserved across the whole gap closure cycle.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → sim + drone-follow processes | Operator runs both terminals locally on a dev box; SITL is contained inside Gazebo + PX4 SITL subprocesses. No external network in scope for this plan. |
| operator browser → FollowServer/WebServer | Already exercised in 03-10/03-12 SITL runs; same threat surface as Phase 0 v1.0 baseline. No change introduced by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14-01 | Operator confusion (Repudiation) | operator click-to-lock auto-recovers to different id under 03-13 leniency | mitigate | Acceptance scorecard row 12 explicitly requires the operator to observe the recovery LOG line (`Stale detection ID X; locking onto single available Y`) in the console scrollback. If the recovery happened but the operator did not see it, that's a deferred-doc concern, not a gate failure. |
| T-03-14-02 | Tampering | operator records false `approved` to bypass a real failure | accept | Solo-developer trust model; no separation of duties required. Mirrors the 03-10 + 03-12 gate disposition. |
| T-03-14-03 | Denial of Service | sim won't boot / Hailo not detected / PX4 doesn't connect | mitigate | Resume signal includes `failed-infra: <reason>` as an explicit alternative; verifier interprets `failed-infra` as "fix env then re-run this gate", NOT "Phase 3 regression". |

No new code in this plan → no package legitimacy gate, no install tasks, no new dependencies. ASVS L1 considerations limited to operator-local trust boundary already established in prior Phase 0 baseline.
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Operator SITL re-run gate (round 3): F3 closed AND F1 + F2 stay closed AND original 03-10 / 03-12 acceptance still holds AND AUTO mode not regressed</name>
  <files>(no code edits — operator runs SITL flow; sign-off recorded in .planning/phases/03-abstraction/03-14-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/03-abstraction/03-12-SUMMARY.md (PRIMARY context — F3 description, file evidence, scorecard rows 7-12 the operator is re-checking)
    - .planning/phases/03-abstraction/03-13-PLAN.md (the fix plan — operator should know what code changed in follow_server.py + which tests were added)
    - .planning/phases/03-abstraction/03-11-SUMMARY.md (F1 + F2 closure context — these stay-closed checks need to know what the F1 publish-at-orchestrator-boundary + F2 log demotion delivered)
    - .planning/phases/03-abstraction/03-10-SUMMARY.md (original failing report — context on the F1 + F2 root causes)
    - .planning/phases/03-abstraction/03-12-PLAN.md § <how-to-verify> (the SITL flow operator runs — Step 0-6 are unchanged; only the acceptance scorecard expands by two rows)
    - CLAUDE.md § Simulation (sim setup commands — bundled PX4 SITL + Gazebo Garden, `sim/setup_sim.sh` / `sim/start_sim.sh --bridge --world <name>` invocations)
    - .claude/skills/drone-follow-dev/SKILL.md (sim + drone-follow run loop, port hygiene, kill recipes — needed if 03-12's SITL run left zombies)
  </read_first>
  <action>
    See <what-built> and <how-to-verify> below for the full operator instructions. This is a checkpoint task: the orchestrator pauses until the operator records approval/failure in `.planning/phases/03-abstraction/03-14-SUMMARY.md` per the resume-signal contract.

    The SITL flow is IDENTICAL to 03-12 Steps 0-6; the difference is the acceptance scorecard expands by two rows (row 12 = F3 closure, row 13 = AUTO-mode-not-regressed). The operator should mentally diff against 03-12's observed behavior: (a) status bar still updates during follow (F1 stays closed), (b) no per-tick INFO log spam (F2 stays closed), (c) click-to-lock works on the visible person even with a stale id (F3 closed), (d) AUTO mode (no click) follows identically to 03-12 (no regression from the F3 fix).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-abstraction/03-14-SUMMARY.md && grep -qE '^(approved|approved-with-deferral|failed|failed-infra)' .planning/phases/03-abstraction/03-14-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    The scorecard MUST be filled out in 03-14-SUMMARY.md with one of {✅, ❌, deferred, n/a} per row. ALL 13 rows must hold (or be deferred with explicit rationale) for the gate to pass. Mirror the table shape from 03-12-SUMMARY.md lines 60-74.

    | # | Criterion | Evidence the operator captures |
    |---|-----------|-------------------------------|
    | 1 | Drone arms + takes off cleanly with `--takeoff-landing` | Console banner shows offboard handshake + arm + takeoff sequence; no MAVSDK timeout errors |
    | 2 | Drone follows actor through full `walk_across_then_approach` pattern | Visual: drone tracks actor across the full walk + approach segments; target not lost during normal flow |
    | 3 | Altitude held within ±0.5 m of `--target-altitude` (3 m default) | Visual: drone stays at roughly head-height; no significant climb/descent during the walk |
    | 4 | Smooth motion (no oscillation/jerk) | Visual: EMA smoothing visible as gradual velocity changes; no twitch or jerk |
    | 5 | Clean Ctrl+C shutdown + land | Drone lands; `pgrep mavsdk_server` is empty 5s after Ctrl+C; no zombie processes |
    | 6 | No new FATAL / ERROR vs pre-Phase-3 | Console scrollback grep for FATAL/ERROR shows no entries beyond the established pre-Phase-3 baseline |
    | 7 | **F1 STAYS CLOSED:** status bar shows mode + non-zero velocity during follow at ≥5 Hz | Web UI status bar above MJPEG: values change visibly during follow; not frozen at `IDLE \| Fwd 0.00 \| Down 0.00 \| Yaw 0.0` |
    | 8 | **F1 STAYS CLOSED:** forward swings negative on actor approach (pre-smoothing pin) | Web UI: during the back half of the walk (actor approaching drone), forward velocity goes negative as retreat-from-tilt fires |
    | 9 | **F2 STAYS CLOSED:** no `ctrl: bh=... factor=... filtered=...` INFO log spam in default console | `grep -c "ctrl: bh=" <terminal-2-scrollback>` returns 0 for the default invocation (DEBUG-level may still show it; that's expected) |
    | 10 | No new regression in follow quality vs 03-10 / 03-12 | Subjective: drone behavior INDISTINGUISHABLE from prior runs; if operator notices NEW oscillation/drift/stuck-at-zero, that's a regression introduced by 03-13 (server-only change, should not affect follow quality at all) |
    | 11 | Full pytest suite green | `python -m pytest robot_follow/tests --ignore=robot_follow/tests/test_sim_worlds.py -x -q` reports ≥ 298 passed / 1 skipped after 03-13 + 03-15 + 03-16 land |
    | 12 | **F3 / F5 CLOSED:** click-to-lock works on the visible person, regardless of id drift between SSE render, click, or HTTP arrival | (a) Operator clicks the actor bbox in the web UI during the walk. (b) Server console shows `INFO | servers.follow_server | Now following detection ID: <id> (bbox height <h>, source: POST body, id_source: <requested|bbox-match>)`. EITHER `id_source: requested` (client and server agreed on the id — happy path) OR `id_source: bbox-match` (server recovered via IoU against the body bbox — proves 03-16 is firing). (c) Web UI follow-state pill flips to `Following: ID <id>` and the drone visibly continues to track that person. (d) The `Detection ID <X> not found. Available: …` 404 log should NOT fire from a normal UI click. If it does, capture the full log line (which now includes `best bbox IoU <N> vs <K> candidate(s)`) — that tells us whether the bbox was disjoint from every candidate or the body was missing entirely. |
    | 13 | **AUTO mode unaffected by 03-13 / 03-15 / 03-16:** when operator does NOT click, drone selects largest visible person and follows identically to 03-10 / 03-12 AUTO behavior | Operator runs the full walk WITHOUT clicking; AUTO follow quality identical to 03-12 (largest-person selection, same control behavior). `/follow/<id>` is never hit when there's no click, so neither the client resolver nor the body-aware capture nor the IoU recovery is exercised by AUTO. |
    | 14 | **F4b CLOSED:** the "bbox height n/a" log line from 03-12 no longer fires for UI clicks | (a) After clicking, the success-path INFO log line reports `source: POST body` — that's the body-aware bbox capture firing. (b) The log line MUST NOT report `source: n/a` for a normal browser click. If it does, either the client failed to send the body or the body's `bbox.h` was out of (0, 1] range; capture the log line for diagnosis. (c) `source: ui_state` is acceptable as a fallback (e.g., for curl-style POSTs or older clients) but a normal UI click should hit `POST body`. |
    | 15 | **F5 RECOVERY VISIBLE (NEW):** if ByteTracker re-IDs the actor between SSE render and click, the operator should see at least one `id_source: bbox-match` log line during the walk | This row is OPTIONAL — the recovery may not fire under all SITL scenarios. If it does fire, it proves 03-16's IoU branch is wired end-to-end. If it doesn't fire (all clicks hit `id_source: requested`), that's also acceptable — it means ByteTracker held the id stable long enough for the click to land. Mark this row `✅ (recovery branch fired N times)` or `✅ (happy path only — recovery branch not exercised)` with the rationale captured. |

    **GATE:** if all 13 rows hold (or row 12 holds in its softer "recovery branch not exercised" form with documented rationale) → record `approved` in 03-14-SUMMARY.md. Phase 3 closes; ABS-11 → Complete in REQUIREMENTS.md traceability; ROADMAP.md Phase 3 status flips from "In Progress (gate failed)" to "Complete"; rover work (Phase 4 + Phase 5) unblocks in parallel.

    **If row 12 (F3) does NOT hold** — e.g., the stale id was sent and `len(available_ids) >= 2` so the server correctly returned 404 but the operator UX is still impaired — that's NOT a Phase 3 gate failure; the 03-13 plan only covered the single-actor recovery case (per 03-12-SUMMARY-recommended fix shape #1). Multi-actor stale-id recovery is fix shape #2 (UI position fallback) explicitly deferred per 03-12-SUMMARY § "Suggested fix shapes". Operator should record `approved-with-deferral: F3-multi-actor (fix shape #2 deferred to v1.2 UX polish phase)` IF rows 1-11 + 13 all hold AND only the multi-actor F3 subcase fails. The single-actor case (the `walk_across_then_approach` world has exactly one actor → single-available branch fires) MUST work for `approved`.
  </acceptance_criteria>
  <what-built>
The 03-13 gap closure on top of the full Phase 3 abstraction stack + the 03-11 orchestrator UI publish + log demotion:

03-13 deliverables (new since 03-12):
- `robot_follow/servers/follow_server.py::do_POST` widens stale-id branch: when `len(available_ids) == 1` and the requested id is unknown, the server recovers onto the single available id, logs the recovery, and continues to the success path (200 + lock + capture bbox). Zero-available and multi-available stale-id requests still return 404 (preserved).
- `robot_follow/tests/test_follow_server.py` gains three new tests pinning: (a) stale + single-available → 200, (b) stale + zero-available → 404, (c) stale + multi-available → 404. Pre-plan baseline 290 passed / 7 skipped → post-plan ≥ 292 passed / 7 skipped.
- `MavsdkDroneAdapter` and all of `robot_follow/robot_api/` are UNTOUCHED — server-layer fix only; axes-only Capabilities contract preserved across the full gap closure cycle.

Goal: verify the F3 gap closure resolves operator click-to-lock in a live SITL run AND that F1 + F2 stay closed AND no new regression was introduced AND AUTO mode behaves identically to 03-12.
  </what-built>
  <how-to-verify>
**Prerequisites (same as 03-12):**
- Operator has Hailo HW + Gazebo Garden + PX4 SITL setup per CLAUDE.md § Simulation
- `sim/setup_sim.sh` has been run at least once
- 03-13's commits landed; full pytest suite reports ≥ 292 passed / 7 skipped
- Optional: kill any stale processes from the 03-12 run via the recipes in `.claude/skills/drone-follow-dev/SKILL.md`

**Step 0 — Pre-run sanity (cheap, run on the dev box):**
```bash
cd /home/<user>/code/<user>/hailo-drone-follow

# Confirm 03-13 commits are in place
git log --oneline -5

# Confirm new F3 tests pass
hailo-apps/venv_hailo_apps/bin/python -m pytest robot_follow/tests/test_follow_server.py -v

# Confirm full suite green
hailo-apps/venv_hailo_apps/bin/python -m pytest robot_follow/tests --ignore=robot_follow/tests/test_sim_worlds.py -x -q
```
Expected: F3 tests pass; full suite ≥ 292 passed / 7 skipped.

**Step 1 — Start SITL (Terminal 1):**
```bash
bash sim/start_sim.sh --bridge --world walk_across_then_approach
```
Expected: Gazebo opens, PX4 boots, walking actor visible, MAVLink relay on udp:14540, video bridge on udp:5600.

**Step 2 — Start drone-follow (Terminal 2):**
```bash
source setup_env.sh
drone-follow --robot drone --input udp://0.0.0.0:5600 --takeoff-landing --webui
```

Expected at startup:
- Console banner shows offboard handshake messages (same as 03-12)
- Web UI accessible at http://localhost:5001 with live MJPEG stream
- Drone arms + takes off to target_altitude (3 m default)
- Console does NOT spam `ctrl: bh=...` INFO lines every tick (F2 stays closed)

**Step 3 — Observe target-follow + web UI status bar + AUTO mode quality (~3-5 minutes):**

3a. **Web UI status bar (F1 stay-closed check, scorecard rows 7 + 8):**
   - At rest before takeoff or in IDLE: `IDLE | Fwd 0.00 | Down 0.00 | Yaw 0.0` (frozen — same as 03-12)
   - During AUTO follow (post-takeoff, actor walking, no click yet): mode shows `AUTO`; forward/yaw values change smoothly several times per second
   - During approach segment (back half of walk): forward velocity swings NEGATIVE — visible sign change in the status bar

3b. **Drone behavior (regression check vs 03-12, scorecard row 10):**
   - Drone faces the actor (yaw tracks position)
   - Drone maintains target bbox height
   - Altitude held at ~3 m
   - Smooth motion — no jerky setpoints
   - Identical to 03-12 — the 03-13 plan made ZERO changes to the control path

3c. **Console output (F2 stay-closed check, scorecard row 9):**
   - No `ctrl: bh=... factor=... filtered=...` INFO lines in the default invocation

3d. **AUTO mode (scorecard row 13 — NEW for this plan):**
   - WITHOUT clicking anywhere in the web UI, run the full walk pattern. Drone should follow the actor identically to 03-12. The 03-13 server change does NOT touch the AUTO code path (`/follow/<id>` is only POSTed on operator click). If AUTO behavior changes vs 03-12, that's a regression to flag.

**Step 4 — Exercise the F3 fix (scorecard row 12 — NEW for this plan):**

The `walk_across_then_approach` world has exactly ONE actor, so `available_ids` should be a single-element set throughout the run (modulo brief gaps when ByteTracker is between activations). This is the EXACT scenario the 03-13 single-available recovery branch was built for.

4a. **Happy path (no stale id):** click the actor bbox in the web UI during the walk. Expected console:
```
INFO | servers.follow_server | Now following detection ID: <N>
```
Expected web UI: follow-state pill flips to `Following: ID <N>`; drone visibly continues tracking. If `<N>` matches the current bbox, the click hit the live id — recovery branch was NOT exercised (this is fine; happy path proves the endpoint still works).

4b. **Stale-id recovery path (the gap closure scenario):** to trigger the recovery branch deliberately, do ONE of:
   - **Method A (natural):** wait for ByteTracker to lose + re-acquire the actor mid-walk (the actor briefly leaves frame or is occluded → on re-acquisition the new track_id is higher than the cached React closure id). Then click immediately. The closure's stale id will not match `available_ids = {new_id}` → 03-13 recovery branch fires.
   - **Method B (deterministic via curl, optional):** while the drone is following AUTO and `available_ids` is a single id (verify via `curl http://localhost:8080/status` and read `available_ids`), POST a definitely-stale id to the follow server:
     ```bash
     curl -X POST http://localhost:8080/follow/1
     ```
     If `1` is NOT the current available id, expect:
     ```
     INFO | servers.follow_server | Stale detection ID 1; locking onto single available <N>
     INFO | servers.follow_server | Now following detection ID: <N> (bbox height ...)
     ```
     And the response JSON: `{"status": "success", "following_id": <N>, "target_bbox_height": ...}`.

4c. If neither Method A nor Method B can be exercised in the available SITL run time, mark scorecard row 12 as `✅ (recovery branch not exercised — single-id happy path verified per 4a)` and capture rationale in the SUMMARY. The unit tests landed in 03-13 are the deterministic safety net; the SITL gate is the operator-experience check.

**Step 5 — Ctrl+C clean shutdown (same as 03-12):**
- Press Ctrl+C in Terminal 2
- Expected: drone lands cleanly; MAVSDK server reaper fires; no zombie processes
- Web UI status bar's last visible mode should be `IDLE` (orchestrator finally-block publish from 03-11)

**Step 6 — Post-run sanity:**
- Check Terminal 2 scrollback for any FATAL / ERROR lines beyond the established baseline
- `pgrep mavsdk_server` is empty 5s after exit
- Capture the relevant `INFO | servers.follow_server | ...` log lines from the scrollback into 03-14-SUMMARY.md as evidence for scorecard row 12

**If the SITL flow fails for INFRASTRUCTURE reasons** (sim won't boot, Hailo not detected, PX4 doesn't connect), that is NOT a Phase 3 regression — record `failed-infra: <reason>` in 03-14-SUMMARY.md instead of `failed`.

**If the SITL flow exposes a NEW gap (a fourth finding the prior gates missed)** — record `failed: <new-finding-description>` and the orchestrator will run `/gsd:plan-phase 3 --gaps` once more to generate 03-15 + 03-16. The verifier note from 03-12-SUMMARY (lines 92-93) accepts that this gate may take multiple SITL attempts.
  </how-to-verify>
  <resume-signal>
After the SITL run completes, the operator records ONE of the following in `.planning/phases/03-abstraction/03-14-SUMMARY.md`:

- **"approved"** — F3 closed (scorecard row 12); F1 + F2 stay closed (rows 7-9); original 03-10 + 03-12 acceptance still holds (rows 1-6, 10-11); AUTO mode not regressed (row 13). Phase 3 closes; ROADMAP Phase 3 → Complete; ABS-11 → Complete in REQUIREMENTS.md traceability; rover work (Phase 4 + Phase 5) unblocks.
- **"approved-with-deferral: {item}"** — primary criteria met; one or more secondary items deferred. Examples:
  - `approved-with-deferral: F3 recovery branch not exercised in SITL (single-id happy path verified)` — row 12 marked soft-pass per 4c rationale; deterministic 03-13 unit tests serve as the closure proof.
  - `approved-with-deferral: F3-multi-actor (fix shape #2 deferred to v1.2 UX polish phase per 03-12-SUMMARY)` — single-actor case (this world) works; multi-actor case is documented deferred.
  - `approved-with-deferral: ROS env-leak verification deferred to Phase 4` (carry-over from 03-10 deferral if not yet exercised).

  Phase 3 closes; deferral noted in STATE.md.
- **"failed: {description}"** — F3 still broken OR F1/F2 regressed OR new regression observed. Phase 3 does NOT close. The orchestrator runs `/gsd:plan-phase 3 --gaps` to plan another fix.
- **"failed-infra: {reason}"** — SITL flow could not run due to environment issues (sim won't boot, hardware not available). NOT a Phase 3 regression; next step is to fix the infrastructure and re-run this gate.

On approval, the operator (or the orchestrator) should:
1. Update `.planning/ROADMAP.md` Phase 3 row: `In Progress (gate failed)` → `Complete (YYYY-MM-DD)`.
2. Update `.planning/REQUIREMENTS.md` ABS-11 row to mark complete with the 03-14-SUMMARY.md backreference.
3. Optionally update `.planning/phases/03-abstraction/03-10-SUMMARY.md` first line to `approved (re-verified in 03-14 after gap closures 03-11 + 03-13)` per the 03-10 protocol's last paragraph.
4. Optionally update `.planning/phases/03-abstraction/03-12-SUMMARY.md` first line to `approved (F3 closed by 03-13; verified in 03-14)` per the same protocol.
  </resume-signal>
</task>

</tasks>

<verification>
This plan has no automated verification by design — it is a human checkpoint. The verifier (`/gsd:verify-work`) MUST keep `human_needed` raised against ABS-11 until 03-14-SUMMARY.md is created with one of the resume-signal strings, per the 03-12-SUMMARY.md verifier note (lines 92-93).

No code commits in this plan. Operator may choose to commit the SUMMARY in version control (recommended):
```bash
git add -- .planning/phases/03-abstraction/03-14-SUMMARY.md \
            .planning/phases/03-abstraction/03-12-SUMMARY.md \
            .planning/phases/03-abstraction/03-10-SUMMARY.md \
            .planning/ROADMAP.md \
            .planning/REQUIREMENTS.md
git commit -m "docs(03-14): operator SITL re-gate approved — Phase 3 closed (ABS-11)"
```

Parallel-wave hygiene: serial dependency on 03-13 (gap fix must land first); no parallel siblings. Wave 11 is terminal for Phase 3.
</verification>

<success_criteria>
- [ ] Operator runs the SITL flow per Step 0-6 above
- [ ] Scorecard rows 1-13 filled out in 03-14-SUMMARY.md with evidence
- [ ] F3 closed (row 12): click-to-lock works on visible person; recovery branch exercised OR single-id happy path verified
- [ ] F1 stays closed (rows 7, 8): web UI status bar updates with mode + non-zero velocities
- [ ] F2 stays closed (row 9): no `ctrl: bh=...` INFO log spam in default console
- [ ] AUTO mode not regressed by F3 fix (row 13): no-click follow behavior identical to 03-12
- [ ] Original 03-10 / 03-12 criteria still hold (rows 1-6, 10-11)
- [ ] Full pytest suite still green (≥ 292 passed / 7 skipped)
- [ ] `.planning/phases/03-abstraction/03-14-SUMMARY.md` exists with one of: `approved`, `approved-with-deferral: ...`, `failed: ...`, or `failed-infra: ...`
- [ ] If approved: ABS-11 marked Complete in REQUIREMENTS.md traceability; ROADMAP Phase 3 row updated to Complete; 03-10 + 03-12 SUMMARY first lines optionally updated per the protocol
- [ ] If failed: gap report filed; Phase 3 stays open until next gap-closure plan lands; `gsd-verifier` keeps `human_needed` raised
</success_criteria>

<output>
After completion, create `.planning/phases/03-abstraction/03-14-SUMMARY.md` recording:
- Operator's approval signal (approved / approved-with-deferral / failed / failed-infra)
- World used (default: walk_across_then_approach)
- SITL run duration (target ≥3-5 minutes)
- The 13-row scorecard table (mirror 03-12-SUMMARY.md scorecard format at lines 60-74) with ✅/❌/deferred per row + per-row evidence (log line, observation, or rationale)
- F3 closure evidence: which Method (4a happy path / 4b natural stale-id / 4b deterministic curl) was used; the exact `INFO | servers.follow_server | ...` log line(s) captured; whether the recovery branch fired
- F1 stay-closed evidence: subjective "status bar was alive during follow"; mode transitions observed (AUTO → IDLE → LOST → IDLE-on-shutdown); forward velocity sign change on approach
- F2 stay-closed evidence: console quiet (grep count for `ctrl: bh=` in scrollback)
- AUTO mode evidence: no-click run quality (identical to 03-12 / 03-10 baseline)
- Regression check: any new oscillation / drift / stuck-at-zero vs 03-12? (yes/no, with description if yes)
- Ctrl+C shutdown clean? (yes/no, with zombie process notes)
- Final mode shown in UI before exit (should be `IDLE`)
- Any other anomalies (with timestamps for log-correlation)
- Recommendation:
  - **If approved:** proceed to `/gsd:verify-work` to formally close Phase 3 (sets ABS-11 → Complete in REQUIREMENTS.md, updates ROADMAP, unblocks Phase 4 + Phase 5).
  - **If approved-with-deferral:** same as approved, plus the deferral string captured in STATE.md for the next milestone.
  - **If failed:** file the gap description; orchestrator runs `/gsd:plan-phase 3 --gaps` to generate 03-15 + 03-16.
  - **If failed-infra:** fix the infrastructure issue; re-run this gate.

On approval, optionally append a one-line update to `.planning/phases/03-abstraction/03-10-SUMMARY.md` and `.planning/phases/03-abstraction/03-12-SUMMARY.md` first lines per the 03-10 protocol's last paragraph (the carry-over re-verification pattern).
</output>
