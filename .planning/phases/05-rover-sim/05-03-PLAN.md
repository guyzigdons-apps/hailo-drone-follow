---
phase: 05-rover-sim
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - sim/rover/README.md
autonomous: true
requirements: [RSIM-07]
tags: [phase-5, rover-sim, documentation, garden-eol, harmonic-migration, RSIM-07, wave-1]
must_haves:
  truths:
    - "sim/rover/README.md exists and is well-formed Markdown (no trailing tabs / mixed tabs+spaces in code blocks)"
    - "README explicitly states Gazebo Garden reached EOL in November 2024 (RSIM-07)"
    - "README documents the Harmonic migration path: s/gzgarden/gzharmonic/ in install.sh + apt names; SDF gz:: prefixes already compatible"
    - "README documents the gz topic -l smoke-test step verifying /cmd_vel and /camera show up (RSIM-07)"
    - "README documents the gz topic -e -t /cmd_vel smoke-test step verifying DiffDrive plugin subscribed (and proves the cmd_vel topic override from RSIM-02 worked)"
    - "README documents the ros2 topic pub --once /cmd_vel geometry_msgs/Twist '...' command for manual rover motion test"
    - "README documents port 5600 conflict with PX4 SITL (cannot run both sims simultaneously) — references PITFALLS Integration Gotchas"
    - "README documents the implicit-display fallback: if neither --webui nor --openhd is passed, drone-follow opens an X11 display window by default — relevant to the rover sim quick-start flow"
    - "README has a 'Quick start' section showing exactly 3 commands: sudo ./install.sh --rover, sim/rover/start_rover_sim.sh, robot-follow --robot rover --input udp://0.0.0.0:5600"
    - "robot_follow/ tree is BYTE-IDENTICAL to HEAD; sim/bridge/video_bridge.py is BYTE-IDENTICAL to HEAD; install.sh, sim/start_sim.sh, sim/worlds/ are BYTE-IDENTICAL to HEAD; no Phase 3/4 file is touched"
  artifacts:
    - path: "sim/rover/README.md"
      provides: "RSIM-07 documentation — Garden EOL notice, Harmonic migration path, gz topic -l smoke-test step, port-5600 conflict notice"
      contains: "Gazebo Garden, EOL, November 2024, gz topic -l, gz topic -e, ros2 topic pub, ros-humble-ros-gzgarden-bridge, ros-humble-ros-gzharmonic-bridge"
      min_lines: 100
  key_links:
    - from: "sim/rover/README.md::Quick start section"
      to: "install.sh --rover (Plan 05-04) + sim/rover/start_rover_sim.sh (Plan 05-05)"
      via: "documented command sequence"
      pattern: "install.sh --rover"
    - from: "sim/rover/README.md::Smoke test section"
      to: "DiffDrive plugin load (Plan 05-01 rover.sdf cmd_vel override) + parameter_bridge (Plan 05-05 launcher)"
      via: "gz topic -l + gz topic -e + ros2 topic pub commands"
      pattern: "gz topic -l"
---

<objective>
Create `sim/rover/README.md` documenting the rover sim subsystem. Address RSIM-07 (Garden EOL notice, Harmonic migration path, `gz topic -l` smoke-test step) and document the Phase 5 quick-start flow that subsequent plans wire up.

Purpose: This README is the operator-facing entry point to the rover sim. It explains:
1. **What the rover sim is** — one-paragraph overview, references to Plan 05-01 (rover.sdf), Plan 05-02 (worlds), Plan 05-05 (launcher).
2. **Quick start** — exactly three commands to go from clean checkout to follow-the-person in Gazebo.
3. **Smoke test** — `gz topic -l` (proves topics exist), `gz topic -e -t /cmd_vel` (proves DiffDrive plugin subscribed = proves Pitfall 6 mitigation works), `ros2 topic pub --once /cmd_vel ...` (proves rover responds to externally-published Twist), `ffplay udp://0.0.0.0:5600` (proves the video_bridge.py reuse works).
4. **Gazebo Garden EOL notice** — November 2024, security debt acknowledged, migration path is mechanical (`s/gzgarden/gzharmonic/`).
5. **Port conflict** — PX4 SITL and rover sim both bind UDP 5600. Documented constraint, not a Phase 5 bug.
6. **Known gotchas** — link back to PITFALLS Pitfalls 5–7 by section name (don't re-quote — single source of truth).

This plan is autonomous, runs in Wave 1, and can run **in parallel** with Plan 05-01 (rover.sdf) and Plan 05-02 (worlds). All three touch disjoint files. No Python is added; the entire `robot_follow/` tree must remain byte-identical to HEAD.

Output: A single Markdown file at `sim/rover/README.md` (~150–220 lines including code-fence examples). Verification is structural (grep) — no `gz` or `shellcheck` requirement.

The README references commands from Plan 05-04 (`install.sh --rover`) and Plan 05-05 (`sim/rover/start_rover_sim.sh`) that don't exist YET. That's fine: this plan documents the contract; Plans 05-04 and 05-05 land in Wave 2 and consume the same contract.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/phases/05-rover-sim/RESEARCH.md

<interfaces>
Key context the executor needs. The README is freestanding — no codebase reading required, only RESEARCH.md and PITFALLS.md.

**Authoritative facts to include (from RESEARCH.md + PITFALLS.md):**

1. **Garden EOL date:** November 2024 (PITFALLS Pitfall 7).
2. **Harmonic migration path:** SDF `gz::` prefixes already work on Harmonic; only apt package names change (`ros-humble-ros-gzgarden-bridge` → `ros-humble-ros-gzharmonic-bridge`; same for any other `gzgarden` references).
3. **Apt packages installed by `install.sh --rover` (Plan 05-04):** `ros-humble-ros-base`, `ros-humble-geometry-msgs`, `ros-humble-ros-gzgarden-bridge`, `gz-garden`. NOT `ros-humble-ros-gz-bridge` (Fortress; wrong).
4. **Bridge syntax:** `parameter_bridge /cmd_vel@geometry_msgs/msg/Twist@gz.msgs.Twist` (two `@` separators, no trailing `]`). REQUIREMENTS.md RSIM-04 wording is a typo; document the verified upstream form.
5. **Camera path:** `video_bridge.py` (existing, UNCHANGED) subscribes to gz `/camera` and forwards H.264 RTP to UDP 5600. drone-follow consumes via `--input udp://0.0.0.0:5600`. NOT `ros_gz_image_bridge` (caps at ~15 Hz on Garden per `ros_gz#368`).
6. **Smoke commands** (from RESEARCH § "start_rover_sim.sh Flow" + RSIM-07):
   - `gz topic -l` → should list `/cmd_vel`, `/camera`, `/world/.../clock`. Proves both plugins loaded.
   - `gz topic -e -t /cmd_vel -n 1` → echoes one Twist message → proves DiffDrive plugin's topic-override (RSIM-02 / Pitfall 6) worked.
   - `ros2 topic pub --once /cmd_vel geometry_msgs/Twist '{linear: {x: 0.3}}'` → rover rolls forward → proves the bridge wiring is bidirectional.
   - `ffplay udp://0.0.0.0:5600` → camera feed visible → proves `video_bridge.py` reuse works.
7. **Port 5600 conflict with PX4 SITL** (PITFALLS Integration Gotchas table, last row): both sims use UDP 5600 for video. Cannot run simultaneously on same machine. Documented constraint; RINT-05 (Phase 6) covers it.
8. **Implicit-display fallback** (CLAUDE.md: "Implicit-display rule"): when neither `--openhd` nor `--webui` is set, drone-follow opens an X11 window by default. Relevant because the rover sim quick-start often runs without those flags.

**README section outline (planner-authored — executor follows verbatim):**

```
# Rover sim (Phase 5, v1.1)

Phase 5 of the drone-follow v1.1 milestone ships the Gazebo rover sim
infrastructure: a diff-drive SDF model with a forward camera, three
adapted actor worlds, a one-command launcher, and the apt-package
install path.  This README is the operator-facing entry point.

## What's here

- `rover.sdf` — diff-drive rover model with a forward camera
  (Garden plugin: `gz::sim::systems::DiffDrive`).
- `worlds/walk_across_then_approach.sdf` — primary actor world; the
  rover spawns at origin, actor walks across the frame at X=4 then
  approaches.  Phase 6 RINT-04 uses this as the E2E test target.
- `worlds/random_walk.sdf` — actor random-walk pattern.
- `worlds/circle_around.sdf` — actor circles around origin at r=2.5 m.
- `start_rover_sim.sh` — one-command launcher (Plan 05-05).

## Prerequisites

- Ubuntu 22.04 (Jammy)
- ROS 2 Humble installed at `/opt/ros/humble`
- Gazebo Garden installed (`gz` CLI available; `gz sim --version` should
  report 7.x)
- This repo's existing drone-sim deps (run `./install.sh` for the base
  hailo-apps venv).

> Note: this dev environment ships **Gazebo Harmonic** (`gz-sim8`) by
> default.  See "Garden vs Harmonic" below for the migration recipe.

## Quick start

Three commands from a clean checkout:

```bash
# 1. Install rover deps (apt: ros-humble-ros-base, ros-humble-geometry-msgs,
#    ros-humble-ros-gzgarden-bridge, gz-garden).  Idempotent.
sudo ./install.sh --rover

# 2. Launch sim (Gazebo + ros_gz_bridge cmd_vel + video_bridge.py).
sim/rover/start_rover_sim.sh                                    # default world
sim/rover/start_rover_sim.sh --world random_walk                # alternate world

# 3. In a second terminal, launch drone-follow against the sim:
robot-follow --robot rover --input udp://0.0.0.0:5600
```

The rover should follow the walking actor.  drone-follow opens an X11
display window by default; pass `--webui` for the browser UI on port
5001.

## Smoke test

Run from a second terminal after `start_rover_sim.sh` is up:

```bash
# 1. Topic discovery — should list /cmd_vel, /camera, /world/<name>/clock
gz topic -l | grep -E '(cmd_vel|camera|clock)'

# 2. Verify DiffDrive subscribed on /cmd_vel (proves <topic>cmd_vel</topic>
#    override worked — Pitfall 6 / RSIM-02).  Should print "publish" entries.
gz topic -i -t /cmd_vel

# 3. Drive the rover from ROS via the bridge.  Should roll forward 0.3 m/s.
ros2 topic pub --once /cmd_vel geometry_msgs/Twist \
    '{linear: {x: 0.3}, angular: {z: 0.0}}'

# 4. Verify camera frames reach UDP 5600 (independent of drone-follow).
ffplay -fflags nobuffer udp://0.0.0.0:5600
```

If `gz topic -l` does NOT show `/cmd_vel`, the DiffDrive plugin failed
to load — likely cause is `ignition::` prefix snuck back into
`rover.sdf` (silent load failure on Garden, see PITFALLS Pitfall 5).
Check `gz sim` stdout for `Loaded system [gz::sim::systems::DiffDrive]`.

## Gazebo Garden EOL notice

**Gazebo Garden reached end-of-life in November 2024.**  Binary
packages remain available from `packages.osrfoundation.org` but no
longer receive security updates.

We use Garden for v1.1 because:
- The bundled PX4 SITL drone sim (`sim/PX4-Autopilot/`) already
  uses Garden.  Pulling in a second Gazebo would conflict on shared
  libs.
- Migration cost is low (see next section).
- v1.1 is sim-only (no real rover hardware), so the security debt
  is acceptable.

### Migration to Harmonic (post-v1.1 cleanup)

The migration is mechanical:

```diff
# install.sh
- ros-humble-ros-gzgarden-bridge
+ ros-humble-ros-gzharmonic-bridge

- gz-garden
+ gz-harmonic
```

**SDF files require ZERO changes** — `gz::sim::systems::DiffDrive`,
`gz-sim-diff-drive-system`, `<topic>cmd_vel</topic>`, and the camera
sensor stanza are all forward-compatible with Harmonic.

The DiffDrive plugin's `gz-sim-diff-drive-system` filename resolves
against the runtime gz-sim version: gz-sim7 on Garden, gz-sim8 on
Harmonic.  Both ship the same plugin under the same logical name.

Trigger for migration: a CVE in Garden's apt graph, or a new Hailo
runtime that breaks on Garden's transitive deps.  Until then, hold.

## Port 5600 conflict with PX4 SITL

Both PX4 SITL (`sim/start_sim.sh --bridge`) and rover sim
(`sim/rover/start_rover_sim.sh`) bind `udp://127.0.0.1:5600` for the
camera feed.  **They cannot run simultaneously on the same machine.**

This is a documented constraint, not a bug.  If you need both sims
running for cross-validation, pick one host for the drone sim and
another for the rover sim, and point drone-follow's `--input` at
the appropriate UDP source on each.

Phase 6 RINT-05 covers this trade-off acceptance.  A future
`--openhd-port`-style flag for the rover sim camera could remap the
port if a future milestone requires it.

## Known gotchas

These are documented in detail in `.planning/research/PITFALLS.md` —
do not re-quote here; that file is the single source of truth.  Quick
links:

- **Pitfall 5 — `ignition::` prefix in SDF** is a silent load failure
  on Garden.  Use `gz::sim::systems::DiffDrive` and
  `gz-sim-diff-drive-system`.
- **Pitfall 6 — DiffDrive topic namespace.**  Without
  `<topic>cmd_vel</topic>` in the plugin block, DiffDrive subscribes
  on `/model/rover/cmd_vel` and the bridge mapping silently fails.
- **Pitfall 7 — Garden EOL.**  See "Garden EOL notice" above.

## Bridge syntax reference

The ros_gz_bridge `parameter_bridge` topic spec uses these directional
symbols:

| Symbol | Direction | Example |
|--------|-----------|---------|
| `@` (between types) | bidirectional | `/cmd_vel@geometry_msgs/msg/Twist@gz.msgs.Twist` |
| `[` (replaces 2nd `@`) | ROS → GZ only | `/cmd_vel@geometry_msgs/msg/Twist[gz.msgs.Twist` |
| `]` (replaces 2nd `@`) | GZ → ROS only | `/odom@nav_msgs/msg/Odometry]gz.msgs.Odometry` |

We use the bidirectional form (`@`) for `/cmd_vel`.  REQUIREMENTS.md
RSIM-04 wording with a trailing `]` is a typo — the symbol replaces
the second `@`, it does not trail the type.

The Gazebo message prefix is `gz.msgs.*` (Garden / Harmonic), NOT
`ignition.msgs.*` (Fortress).

## See also

- `.planning/REQUIREMENTS.md` § RSIM-01..07 — Phase 5 requirements.
- `.planning/research/PITFALLS.md` — Garden / ROS / venv pitfall catalog.
- `.planning/phases/05-rover-sim/RESEARCH.md` — Phase 5 research notes.
- `sim/start_sim.sh` — drone-sim launcher, shares cleanup patterns.
- `sim/bridge/video_bridge.py` — shared camera→UDP bridge (UNCHANGED
  between drone and rover sims per RSIM-06 / SUMMARY.md camera
  plumbing decision).
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md (file on disk) ↔ Future operator following the docs | Operator copy-pastes commands. If the docs lie (wrong package name, wrong port, wrong syntax), the operator hits an obscure failure. Documentation IS the contract for the operator-facing surface. |
| README links to RESEARCH.md / PITFALLS.md | README is a thin pointer; the canonical pitfall text lives in PITFALLS.md. Drift between the two is a documentation-bitrot risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | README documents wrong apt package name (e.g., `ros-humble-ros-gz-bridge` instead of `-gzgarden-bridge`) | mitigate | Verify step greps README for the exact correct name (`ros-humble-ros-gzgarden-bridge`) and asserts the Fortress-form (`ros-humble-ros-gz-bridge` without garden/harmonic suffix) is NOT present as a standalone install instruction. |
| T-05-03-02 | Tampering | README documents wrong bridge syntax (trailing `]` per REQUIREMENTS.md typo) | mitigate | Verify step greps for the corrected form `/cmd_vel@geometry_msgs/msg/Twist@gz.msgs.Twist` (two `@` separators); asserts the malformed `Twist]` typo is NOT present. |
| T-05-03-03 | Information Disclosure | README mentions credentials, secrets, or keys | accept | This is a sim-only README; no credentials or auth surface exists. Verify step: README MUST NOT contain `password`, `secret`, `api_key`, `token` (case-insensitive grep returns 0). |
| T-05-03-04 | Tampering | README accidentally documents `--clean-start` for openhd (footgun per CLAUDE.md Troubleshooting) | mitigate | Verify step: README MUST NOT mention `--clean-start` for any openhd context; rover sim does not interact with openhd at all. |
| T-05-03-05 | Elevation of Privilege | Subtle edit accidentally touches `robot_follow/`, `sim/bridge/`, `install.sh`, or any Phase 3/4 file | mitigate | Pathspec-only commit naming only `sim/rover/README.md`; verify step explicitly checks `git diff --staged --name-only` does not include those paths. |
| T-05-03-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. README is plain Markdown. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create sim/rover/README.md documenting Garden EOL, Harmonic migration, smoke-test commands, port conflict, and bridge syntax</name>
  <files>sim/rover/README.md</files>
  <action>
    Create `sim/rover/README.md` using the README outline from the `<interfaces>` block above.

    **Write the file VERBATIM as shown in the interfaces block.** The outline is the deliverable. The executor's job is faithful transcription plus the structural verifications below — not a creative rewrite.

    **Section order (mandatory — checker greps section headers):**
    1. `# Rover sim (Phase 5, v1.1)` (H1 title)
    2. `## What's here`
    3. `## Prerequisites`
    4. `## Quick start`
    5. `## Smoke test`
    6. `## Gazebo Garden EOL notice`
    7. `### Migration to Harmonic (post-v1.1 cleanup)` (subsection of #6)
    8. `## Port 5600 conflict with PX4 SITL`
    9. `## Known gotchas`
    10. `## Bridge syntax reference`
    11. `## See also`

    **DO:**
    - Use fenced code blocks (triple-backtick) for ALL command examples. Specify the language: `bash`, `diff`, etc.
    - Use the exact strings from `<interfaces>` for all factual claims (apt package names, port numbers, command syntax).
    - Mention all four smoke-test verifications: `gz topic -l`, `gz topic -i -t /cmd_vel`, `ros2 topic pub --once /cmd_vel`, `ffplay udp://0.0.0.0:5600`.
    - Mention November 2024 explicitly as the Garden EOL date.
    - Mention the bidirectional `@` bridge syntax explicitly; clarify that the trailing `]` form from REQUIREMENTS.md RSIM-04 is a typo.
    - Link to PITFALLS Pitfall 5, 6, 7 by section name (not by quote — single source of truth).
    - Cross-reference Phase 6 RINT-05 for port-conflict ownership.
    - End with a trailing newline.

    **DON'T:**
    - Do NOT include `--clean-start` anywhere (CLAUDE.md Troubleshooting documents that as a footgun for openhd; rover sim doesn't touch openhd).
    - Do NOT include `password`, `secret`, `api_key`, `token`, or any auth-related string. The rover sim has no auth surface.
    - Do NOT re-quote the body of PITFALLS Pitfalls 5–7 — link to them by section name. Single source of truth.
    - Do NOT use the wrong apt package name (`ros-humble-ros-gz-bridge` is Fortress; rover uses `ros-humble-ros-gzgarden-bridge`).
    - Do NOT include the malformed bridge syntax with trailing `]` (`...gz.msgs.Twist]`). The REQUIREMENTS.md RSIM-04 text has a typo; the README explicitly clarifies the corrected form per RESEARCH § "ros_gz_bridge syntax — current truth".
    - Do NOT mention `ros_gz_image_bridge` as the camera path — that's the WRONG path per SUMMARY.md camera-plumbing decision and PITFALLS Integration Gotchas. The correct path is `video_bridge.py` reuse (RSIM-06).
    - Do NOT touch ANY file outside `sim/rover/README.md`. Specifically: `robot_follow/`, `sim/bridge/`, `install.sh`, `sim/start_sim.sh`, `sim/worlds/`, and any Phase 3/4 file are OUT OF SCOPE.

    **Pathspec-only commit:**
    ```bash
    mkdir -p sim/rover
    # ... write sim/rover/README.md via the Write tool ...

    git status --porcelain
    # Expected: exactly one new file: "?? sim/rover/README.md"

    git add -- sim/rover/README.md
    git diff --staged --name-only -- robot_follow/ sim/bridge/ install.sh sim/start_sim.sh sim/worlds/
    # ^ MUST print nothing

    git commit -- sim/rover/README.md -m "docs(05-03): sim/rover/README.md — Garden EOL + smoke-test step (RSIM-07)"
    ```
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && test -f sim/rover/README.md && echo "file exists" && H1=$(grep -c '^# Rover sim (Phase 5, v1.1)$' sim/rover/README.md) && test "$H1" = "1" && echo "H1 title OK" && for SEC in "## What's here" "## Prerequisites" "## Quick start" "## Smoke test" "## Gazebo Garden EOL notice" "## Port 5600 conflict with PX4 SITL" "## Known gotchas" "## Bridge syntax reference" "## See also"; do N=$(grep -cF "$SEC" sim/rover/README.md); test "$N" -ge "1" && echo "section present: $SEC"; done && EOL=$(grep -ciE 'november 2024' sim/rover/README.md) && test "$EOL" -ge "1" && echo "EOL date 'November 2024' documented" && APT_OK=$(grep -c 'ros-humble-ros-gzgarden-bridge' sim/rover/README.md) && test "$APT_OK" -ge "1" && echo "correct apt name present (RSIM-05)" && BRIDGE_SYNTAX=$(grep -c '/cmd_vel@geometry_msgs/msg/Twist@gz.msgs.Twist' sim/rover/README.md) && test "$BRIDGE_SYNTAX" -ge "1" && echo "correct bridge syntax (bidirectional @) present" && BAD_BRIDGE=$(grep -c 'Twist\]' sim/rover/README.md) && test "$BAD_BRIDGE" = "0" && echo "no malformed trailing ] bridge syntax" && TL=$(grep -c 'gz topic -l' sim/rover/README.md) && test "$TL" -ge "1" && echo "gz topic -l smoke step (RSIM-07) present" && PUB=$(grep -c 'ros2 topic pub' sim/rover/README.md) && test "$PUB" -ge "1" && echo "ros2 topic pub smoke step present" && FF=$(grep -c 'udp://0.0.0.0:5600' sim/rover/README.md) && test "$FF" -ge "1" && echo "UDP 5600 port documented" && HARM=$(grep -c 'ros-humble-ros-gzharmonic-bridge' sim/rover/README.md) && test "$HARM" -ge "1" && echo "Harmonic migration path package name present" && NO_CLEAN_START=$(grep -ciE 'clean-start' sim/rover/README.md) && test "$NO_CLEAN_START" = "0" && echo "no --clean-start footgun mentioned" && NO_SECRETS=$(grep -ciE '(password|secret|api[_ ]key|\btoken\b)' sim/rover/README.md) && test "$NO_SECRETS" = "0" && echo "no credential/secret strings present" && NO_IMG_BRIDGE=$(grep -c 'ros_gz_image_bridge' sim/rover/README.md) && test "$NO_IMG_BRIDGE" = "0" && echo "no ros_gz_image_bridge (wrong camera path) mentioned" && LINES=$(wc -l < sim/rover/README.md) && test "$LINES" -ge "100" && echo "min_lines (100) OK: $LINES" && DIRTY=$(git status --porcelain | grep -vE '(sim/rover/README.md|sim/rover/rover.sdf|sim/rover/worlds/)' | grep -vE '^[?? AM]+ \\.planning/' || true) && test -z "$DIRTY" && echo "no out-of-scope files touched"</automated>
  </verify>
  <done>sim/rover/README.md exists; all 9 sections present; "November 2024" appears at least once; bidirectional bridge syntax present; malformed trailing-`]` form absent; smoke commands present (`gz topic -l`, `ros2 topic pub`, UDP 5600); Harmonic migration path documented; no `--clean-start` / credentials / `ros_gz_image_bridge` mentions; ≥100 lines; only `sim/rover/README.md` is dirty in git status; commit landed via pathspec.</done>
</task>

</tasks>

<verification>
After the task:

```bash
cd /home/guyz/code/guyz/hailo-drone-follow

# File and structure
test -f sim/rover/README.md
grep -c '^# Rover sim (Phase 5, v1.1)$' sim/rover/README.md     # expect 1 (H1 title)

# All 9 required sections present
for SEC in "## What's here" "## Prerequisites" "## Quick start" \
           "## Smoke test" "## Gazebo Garden EOL notice" \
           "## Port 5600 conflict with PX4 SITL" "## Known gotchas" \
           "## Bridge syntax reference" "## See also"; do
  grep -cF "$SEC" sim/rover/README.md                            # expect >=1 per section
done

# Content gates (RSIM-07 + research locks)
grep -ciE 'november 2024' sim/rover/README.md                    # expect >=1 (Garden EOL date)
grep -c 'ros-humble-ros-gzgarden-bridge' sim/rover/README.md     # expect >=1 (correct apt name)
grep -c 'ros-humble-ros-gzharmonic-bridge' sim/rover/README.md   # expect >=1 (Harmonic migration)
grep -c '/cmd_vel@geometry_msgs/msg/Twist@gz.msgs.Twist' sim/rover/README.md  # expect >=1 (bidirectional @)
grep -c 'gz topic -l' sim/rover/README.md                        # expect >=1 (RSIM-07 smoke step)
grep -c 'ros2 topic pub' sim/rover/README.md                     # expect >=1 (manual cmd_vel test)
grep -c 'udp://0.0.0.0:5600' sim/rover/README.md                 # expect >=1 (port doc)

# Anti-gates (must be absent)
grep -c 'Twist\]' sim/rover/README.md                            # expect 0 (REQUIREMENTS typo NOT propagated)
grep -ciE 'clean-start' sim/rover/README.md                      # expect 0 (footgun)
grep -ciE '(password|secret|api[_ ]key|\btoken\b)' sim/rover/README.md  # expect 0
grep -c 'ros_gz_image_bridge' sim/rover/README.md                # expect 0 (wrong camera path)

# Architectural locks
git diff --name-only HEAD~1 HEAD -- robot_follow/ sim/bridge/ install.sh sim/start_sim.sh sim/worlds/
# ^ MUST print nothing

git log -1 --name-only
# ^ MUST show only `sim/rover/README.md`
```

**Verification trade-off (explicit):** This is documentation. The verification is structural (section headers, content keywords, anti-patterns). Whether the README is *useful* to a future operator is a judgement call that cannot be automated. The structural gates ensure the canonical Phase 5 facts (apt name, port, bridge syntax, EOL date) are present and the documented anti-patterns (REQUIREMENTS typo, `--clean-start`, image bridge) are absent.

Commit shape: 1 commit via pathspec
- `docs(05-03): sim/rover/README.md — Garden EOL + smoke-test step (RSIM-07)`

Parallel-wave hygiene: this plan can run IN PARALLEL with 05-01 (rover.sdf) and 05-02 (worlds). All three touch disjoint files; pathspec commits prevent attribution drift per `feedback_parallel_wave_worktree_isolation.md`.

Bisectability: at HEAD of this task, the full Python suite remains green because no Python file was modified.
</verification>

<success_criteria>
- [ ] `sim/rover/README.md` exists, ≥100 lines
- [ ] H1 title `# Rover sim (Phase 5, v1.1)` present exactly once
- [ ] All 9 required `##` section headers present (`## What's here`, `## Prerequisites`, `## Quick start`, `## Smoke test`, `## Gazebo Garden EOL notice`, `## Port 5600 conflict with PX4 SITL`, `## Known gotchas`, `## Bridge syntax reference`, `## See also`)
- [ ] "November 2024" mentioned at least once (Garden EOL date — RSIM-07)
- [ ] `ros-humble-ros-gzgarden-bridge` mentioned (correct apt name — RSIM-05)
- [ ] `ros-humble-ros-gzharmonic-bridge` mentioned (Harmonic migration path — RSIM-07)
- [ ] `/cmd_vel@geometry_msgs/msg/Twist@gz.msgs.Twist` (bidirectional `@`) present at least once
- [ ] `gz topic -l` smoke command documented (RSIM-07)
- [ ] `ros2 topic pub` manual-test command documented
- [ ] `udp://0.0.0.0:5600` port documented
- [ ] `Twist\]` malformed bridge syntax: ZERO matches (REQUIREMENTS.md RSIM-04 typo NOT propagated)
- [ ] `clean-start` (case-insensitive): ZERO matches (CLAUDE.md footgun)
- [ ] `password|secret|api_key|\btoken\b` (case-insensitive): ZERO matches
- [ ] `ros_gz_image_bridge`: ZERO matches (wrong camera path per SUMMARY.md)
- [ ] `git diff --name-only HEAD~1 HEAD -- robot_follow/ sim/bridge/ install.sh sim/start_sim.sh sim/worlds/` prints nothing
- [ ] Single pathspec commit `docs(05-03): …`
</success_criteria>

<output>
After completion, create `.planning/phases/05-rover-sim/05-03-SUMMARY.md` recording:
- File path (`sim/rover/README.md`) and final line count
- Grep gate evidence (paste outputs of the verification block — section presence, content gates, anti-gates)
- Architectural locks evidence: `git diff --name-only HEAD~1 HEAD -- robot_follow/ sim/bridge/` empty
- Pathspec commit hash
- Note that Plan 05-04's `install.sh --rover` and Plan 05-05's `start_rover_sim.sh` will both implement the contracts documented here; if those plans drift, the README is the source of truth for the operator-facing surface
- Note that the dev box has Harmonic (gz-sim8); the Harmonic migration path documented in this README is consequently exercised by the next contributor who runs `install.sh --rover` on this dev box — they'll find the apt package missing locally and follow the README's Harmonic-substitution recipe
</output>
</content>
