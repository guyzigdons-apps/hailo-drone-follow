---
phase: 02-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["02-00"]
files_modified:
  - robot_follow/drone_api/mavsdk_drone.py
  - robot_follow/robot_follow_app.py
  - CLAUDE.md
autonomous: true
requirements:
  - CLEAN-04
  - CLEAN-05
  - CLEAN-08
must_haves:
  truths:
    - "`--mission-duration` has a `help=` string explaining auto-land/timeout semantics; CLAUDE.md mentions it in the Key CLI Flags section."
    - "`_resolve_serial_connection` reads `args.serial_baud` directly with no `getattr` fallback that lies."
    - "`run_live_drone` signature has no `shutdown_read_fd` parameter and no pipe-reader block."
  artifacts:
    - path: "robot_follow/drone_api/mavsdk_drone.py"
      provides: "Documented `--mission-duration` flag (help= string); `run_live_drone` clean signature (no shutdown_read_fd)"
      contains: "def run_live_drone"
    - path: "robot_follow/robot_follow_app.py"
      provides: "_resolve_serial_connection reads `args.serial_baud` directly"
      contains: "_resolve_serial_connection"
    - path: "CLAUDE.md"
      provides: "Documentation paragraph describing --mission-duration timeout behaviour"
      contains: "mission-duration"
  key_links:
    - from: "robot_follow/robot_follow_app.py _resolve_serial_connection"
      to: "args.serial_baud (registered at mavsdk_drone.py:41 with default 57600)"
      via: "Direct attribute access; no getattr fallback"
      pattern: "args\\.serial_baud(?!.*getattr)"
---

<objective>
Wave 1C — Three independent edits in the `mavsdk_drone.py` + `robot_follow_app.py` pair: CLEAN-04 (document `--mission-duration`, do NOT remove — research found it load-bearing at lines 767+799), CLEAN-05 (replace lying `getattr(args, "serial_baud", 115200)` with direct attribute access), CLEAN-08 (remove unreachable `shutdown_read_fd` pipe-reader block at mavsdk_drone.py:642-665).

Purpose: All three are independent of plan 02-01 (different files) and plan 02-02 (different files). They share two files internally, but at distinct line ranges (CLEAN-04 @ mavsdk_drone.py:47; CLEAN-08 @ mavsdk_drone.py:642-665; CLEAN-05 @ robot_follow_app.py:54 — no overlap). Land all three in this plan to keep the wave count manageable.

**Decision: CLEAN-04 is DOCUMENT, not REMOVE.** RESEARCH § CLEAN-04 confirms `--mission-duration` is load-bearing — read at `mavsdk_drone.py:767` (auto-land timeout in `--takeoff-landing` branch) and `:799` (control-loop iteration timeout in the no-takeoff-landing branch). Removing it would change runtime behaviour (silent infinite mission). The fix is to add a `help=` string and CLAUDE.md documentation.

Output: Three small surgical changes + one CLAUDE.md paragraph. Full pytest suite stays green.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cleanup/02-RESEARCH.md
@.planning/phases/02-cleanup/02-VALIDATION.md
@robot_follow/drone_api/mavsdk_drone.py
@robot_follow/robot_follow_app.py
@CLAUDE.md
@docs/control-architecture.md

<interfaces>
CLEAN-04 — `robot_follow/drone_api/mavsdk_drone.py:47`:
  CURRENT (help-text empty — operator surprise hazard):
    group.add_argument("--mission-duration", type=float, default=300.0)
  TARGET:
    group.add_argument("--mission-duration", type=float, default=300.0,
        help="Maximum mission duration in seconds. Default 300.0 (5 min). "
             "With --takeoff-landing: triggers an automatic land at expiry. "
             "Without --takeoff-landing: triggers a control-loop restart "
             "(the app keeps running; the loop reconnects). "
             "Set to a large value (e.g. 86400) for unbounded missions.")

  Read at: mavsdk_drone.py:767 and :799 (both `asyncio.sleep(args.mission_duration)` patterns). DO NOT REMOVE — it is load-bearing.

CLEAN-05 — `robot_follow/robot_follow_app.py:54`:
  CURRENT (lying fallback — registered default is 57600 but fallback returns 115200):
    baud = getattr(args, "serial_baud", 115200)
  TARGET:
    baud = args.serial_baud

  --serial-baud is unconditionally registered at mavsdk_drone.py:41 with default=57600. The parser builds before _resolve_serial_connection runs. Fallback unreachable.

CLEAN-08 — `robot_follow/drone_api/mavsdk_drone.py:642-665`:
  CURRENT:
    async def run_live_drone(args, shared_state, shutdown, shutdown_read_fd=None,
                             config=None, ui_state=None, target_state=None):
        ...
        if shutdown_read_fd is not None:
            loop = asyncio.get_running_loop()
            def _on_shutdown_pipe(): ...
            loop.add_reader(shutdown_read_fd, _on_shutdown_pipe)

  TARGET:
    async def run_live_drone(args, shared_state, shutdown,
                             config=None, ui_state=None, target_state=None):
        ...
        # (pipe-reader block deleted)

  Single caller at robot_follow_app.py:396 does NOT pass shutdown_read_fd= — entire block unreachable.

  Do NOT remove `import os` at the top of mavsdk_drone.py — it is used elsewhere (`os.getuid()`, env-var access).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document --mission-duration in argparse and CLAUDE.md (CLEAN-04)</name>
  <files>
    robot_follow/drone_api/mavsdk_drone.py,
    CLAUDE.md
  </files>
  <action>
    Step A — add help= string at mavsdk_drone.py:47. Locate the existing `group.add_argument("--mission-duration", ...)` call (around line 47). Add a `help=` keyword with the text:
      "Maximum mission duration in seconds. Default 300.0 (5 min). With --takeoff-landing: triggers an automatic land at expiry. Without --takeoff-landing: triggers a control-loop restart (the app keeps running; the loop reconnects). Set to a large value (e.g. 86400) for unbounded missions."

    Step B — verify via `drone-follow --help` shows the help text (grep for "mission-duration" within the help output and confirm it appears alongside the explanation, not just the bare flag name).

    Step C — add a one-paragraph note in CLAUDE.md under "## Key CLI Flags". Locate that section. Insert a new bullet (anywhere within that subgroup) reading:

      - `--mission-duration SECONDS` — Mission watchdog timeout (default: 300.0 = 5 min). With `--takeoff-landing`, the drone auto-lands at expiry. Without `--takeoff-landing`, the control loop restarts (process keeps running, MAVSDK reconnects). Pass a large value (e.g. `86400` for 24 h) to effectively disable the watchdog. Note: silent auto-land at 300 s is a surprise hazard — set explicitly for long flights.

    Step D — do NOT remove --mission-duration. RESEARCH confirmed it is load-bearing.

    Step E — git diff self-check before committing — both files (mavsdk_drone.py + CLAUDE.md) in the same commit.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && drone-follow --help 2>&amp;1 | grep -qE "mission-duration.*(auto-land|timeout|restart|watchdog)" && grep -q "mission-duration" CLAUDE.md && echo OK</automated>
  </verify>
  <done>
    `drone-follow --help` lists `--mission-duration` with a help string mentioning auto-land/timeout. CLAUDE.md has a bullet describing the flag. Full pytest suite green: `python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace getattr(args, serial_baud, 115200) with args.serial_baud (CLEAN-05)</name>
  <files>robot_follow/robot_follow_app.py</files>
  <action>
    Open `robot_follow/robot_follow_app.py`. Locate `_resolve_serial_connection` (around line ~40-60). Find the line containing the `getattr(args, "serial_baud", 115200)` call (line ~54).

    Replace:
      baud = getattr(args, "serial_baud", 115200)
    With:
      baud = args.serial_baud

    Leave the outer guard `if getattr(args, "serial", None) is not None:` unchanged (it's a different field — `serial`, not `serial_baud` — and that field does have a None-default, so the guard is real and needed).

    Sanity grep after edit:
      grep -n 'getattr(args, "serial_baud"' robot_follow/robot_follow_app.py
    Expected: 0 matches.

    Smoke that `--help` still parses (install_smoke test covers this):
      python -m pytest robot_follow/tests/test_install_smoke.py -v
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && grep -c 'getattr(args, .serial_baud.' robot_follow/robot_follow_app.py | grep -q "^0$" && python -m pytest robot_follow/tests/test_install_smoke.py -v</automated>
  </verify>
  <done>
    grep for `getattr(args, "serial_baud"` in `robot_follow_app.py` returns 0 matches. `_resolve_serial_connection` reads `args.serial_baud` directly. install_smoke tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove shutdown_read_fd param and pipe-reader block from run_live_drone (CLEAN-08)</name>
  <files>robot_follow/drone_api/mavsdk_drone.py</files>
  <action>
    Open `robot_follow/drone_api/mavsdk_drone.py`. Locate `run_live_drone` (around line 642).

    Step A — signature edit: remove `shutdown_read_fd=None,` from the parameter list. Keep all other parameters in their existing order: `args, shared_state, shutdown, config, ui_state, target_state`.

    Step B — body deletion: locate the block around lines ~652-665:
      if shutdown_read_fd is not None:
          loop = asyncio.get_running_loop()
          def _on_shutdown_pipe():
              ...
          loop.add_reader(shutdown_read_fd, _on_shutdown_pipe)
    Delete this entire `if` block, including the nested `_on_shutdown_pipe` function. Leave surrounding code unchanged.

    Step C — keep `import os` at the top of the file. It is used at multiple other sites (`os.getuid()`, `os.environ`). Do NOT remove it.

    Step D — verify the single caller is unchanged:
      grep -n "run_live_drone" robot_follow/robot_follow_app.py
    Expected: one call site at line ~396 with NO `shutdown_read_fd=` kwarg (unchanged from before).

    Step E — smoke import + inspect:
      python -c "import inspect; from robot_follow.drone_api.mavsdk_drone import run_live_drone; assert 'shutdown_read_fd' not in inspect.signature(run_live_drone).parameters; print('OK')"

    Step F — grep across the file:
      grep -c 'shutdown_read_fd' robot_follow/drone_api/mavsdk_drone.py
    Expected: 0.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && python -c "import inspect; from robot_follow.drone_api.mavsdk_drone import run_live_drone; assert 'shutdown_read_fd' not in inspect.signature(run_live_drone).parameters; print('OK')" && grep -c 'shutdown_read_fd' robot_follow/drone_api/mavsdk_drone.py | grep -q "^0$"</automated>
  </verify>
  <done>
    `inspect.signature(run_live_drone).parameters` has no `shutdown_read_fd` key. `grep -c 'shutdown_read_fd' robot_follow/drone_api/mavsdk_drone.py` returns 0. Full pytest suite green.
  </done>
</task>

</tasks>

<verification>
1. Per-task automated verifies above.
2. Quick suite per commit: `python -m pytest robot_follow/tests/test_install_smoke.py robot_follow/tests/test_shared_state.py robot_follow/tests/test_config_persistence.py robot_follow/tests/test_velocity_command_shape.py -x` — green.
3. Full suite end of plan: `python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py` — green.
</verification>

<success_criteria>
- CLEAN-04: `drone-follow --help` shows `--mission-duration` with help text; CLAUDE.md mentions it.
- CLEAN-05: `getattr(args, "serial_baud"` grep returns 0 lines in `robot_follow_app.py`.
- CLEAN-08: `run_live_drone` signature lacks `shutdown_read_fd`; whole-file grep for the name returns 0.
- Full pytest suite green.
</success_criteria>

<output>
Create `.planning/phases/02-cleanup/02-03-SUMMARY.md` recording the three commits and the post-edit grep gates.
</output>
