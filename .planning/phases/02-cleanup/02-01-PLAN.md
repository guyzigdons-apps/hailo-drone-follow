---
phase: 02-cleanup
plan: 01
type: execute
wave: 2
depends_on: ["02-00"]
files_modified:
  - sim/world_loader.py
  - scripts/bench_reid_callback.py
  - robot_follow/pipeline_adapter/vision_branches.py
  - robot_follow/pipeline_adapter/hailo_drone_detection_manager.py
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-06
  - CLEAN-10
must_haves:
  truths:
    - "`sim/world_loader.py` does not exist; grep for `world_loader` outside .git returns nothing."
    - "`scripts/bench_reid_callback.py` does not exist; grep for `bench_reid` outside .git returns nothing."
    - "Alias `strip_tiles_and_highlight_target` is gone from `vision_branches.py`."
    - "`create_app(controller_config=...)` kwarg is gone; the attribute is still attached via `app.user_data.controller_config = ...` at the existing site in `robot_follow_app.py:340`."
  artifacts:
    - path: "robot_follow/pipeline_adapter/vision_branches.py"
      provides: "Line 397 alias removed; `highlight_target` is the sole public name"
      contains: "def highlight_target"
    - path: "robot_follow/pipeline_adapter/hailo_drone_detection_manager.py"
      provides: "create_app signature does not include controller_config; DroneFollowUserData still initialises self.controller_config = None"
      contains: "def create_app"
  key_links:
    - from: "robot_follow/robot_follow_app.py:340"
      to: "robot_follow/pipeline_adapter/hailo_drone_detection_manager.py DroneFollowUserData.controller_config"
      via: "app.user_data.controller_config = controller_config (post-construction attach — unchanged by this plan)"
      pattern: "app\\.user_data\\.controller_config\\s*="
---

<objective>
Wave 1A — Four edit-isolated dead-code deletions: two file removals (CLEAN-01, CLEAN-02), one one-line alias deletion (CLEAN-06), one kwarg removal (CLEAN-10). Zero callers in each case (verified by RESEARCH grep gates).

Purpose: Close the easiest 4 of the 18 cleanup items in one autonomous plan. These edits do not touch shared files with Wave 1B/1C plans (the file-conflict matrix in RESEARCH § File-Conflict Matrix confirms no overlap with 02-02 or 02-03).
Output: Two files gone, two single-file edits. Full pytest suite stays green.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cleanup/02-RESEARCH.md
@.planning/phases/02-cleanup/02-VALIDATION.md
@robot_follow/pipeline_adapter/vision_branches.py
@robot_follow/pipeline_adapter/hailo_drone_detection_manager.py
@robot_follow/robot_follow_app.py

<interfaces>
<!-- Confirmed via RESEARCH grep — these are the SOLE callers / definition sites. -->

CLEAN-06 — `vision_branches.py:397`:
  CURRENT — delete this single line:
    `strip_tiles_and_highlight_target = highlight_target`
  Confirmed via grep for "strip_tiles_and_highlight_target" returns ONLY this definition line.
  Internal callers at line 419, and comment-only mentions in `hailo_drone_detection_manager.py:841, :1016` all use `highlight_target` directly.

CLEAN-10 — `hailo_drone_detection_manager.py` four sites:
- Line 695: `def create_app(... controller_config=None, ...)` -> remove the kwarg
- Line 731: `DroneFollowUserData.__init__(... controller_config=None, ...)` -> remove the kwarg from signature; KEEP `self.controller_config = None` so the attribute exists for `hailo_drone_detection_manager.py:278` which reads `config = user_data.controller_config`
- Line 739: `self.controller_config = controller_config` -> become `self.controller_config = None`
- Line 1285: `controller_config=controller_config` keyword in the call passing kwargs to `DroneFollowUserData(...)` -> delete this kwarg

Caller at `robot_follow/robot_follow_app.py:317-323` (CALLS `create_app`) does NOT pass `controller_config=`; `robot_follow_app.py:340` does `app.user_data.controller_config = controller_config` (attribute attach AFTER construction — unchanged by this plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete sim/world_loader.py and scripts/bench_reid_callback.py (CLEAN-01, CLEAN-02)</name>
  <files>
    sim/world_loader.py (deleted),
    scripts/bench_reid_callback.py (deleted)
  </files>
  <action>
    Run `git rm sim/world_loader.py` and `git rm scripts/bench_reid_callback.py`.

    RESEARCH confirmed:
    - `world_loader.py` has zero callers outside its own internal `tempfile` prefix string (`sim/world_loader.py:103`). `start_sim.sh` uses `PX4_GZ_WORLD` env var instead.
    - `bench_reid_callback.py` imports nonexistent `reid_worker` module (broken pre-milestone); zero external refs in README / CLAUDE.md / TROUBLESHOOTING.md / docs/.

    After deletion, run the grep gates to confirm no stragglers (exclude .planning/ — historical references in research docs are allowed):

      grep -rE "world_loader|from sim.world_loader|import world_loader" --include='*.py' --include='*.sh' --include='*.md' --include='*.json' --include='*.toml' --exclude-dir=.git --exclude-dir=.planning --exclude-dir=hailo-apps --exclude-dir=__pycache__ --exclude-dir=node_modules . | head -20
      # Expected: 0 lines.

      grep -rE "bench_reid|bench_reid_callback" --include='*.py' --include='*.sh' --include='*.md' --include='*.json' --include='*.toml' --exclude-dir=.git --exclude-dir=.planning --exclude-dir=hailo-apps --exclude-dir=__pycache__ --exclude-dir=node_modules . | head -20
      # Expected: 0 lines.

    If either grep finds a remaining reference, update or remove it; if the reference is in .planning/, it is allowed (historical record).
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && test ! -f sim/world_loader.py && test ! -f scripts/bench_reid_callback.py && [ -z "$(grep -rE 'world_loader|bench_reid' --include='*.py' --include='*.sh' --include='*.toml' --exclude-dir=.git --exclude-dir=.planning --exclude-dir=hailo-apps --exclude-dir=__pycache__ . 2>/dev/null)" ]</automated>
  </verify>
  <done>
    Both files removed from working tree and from git index. Grep gates for `world_loader` and `bench_reid` return 0 lines outside `.git/` and `.planning/`. Full pytest suite green: `python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove strip_tiles_and_highlight_target alias from vision_branches.py (CLEAN-06)</name>
  <files>robot_follow/pipeline_adapter/vision_branches.py</files>
  <action>
    Open `robot_follow/pipeline_adapter/vision_branches.py`. Locate line 397: `strip_tiles_and_highlight_target = highlight_target`.

    Delete that single line. If there's a comment above the alias explaining "legacy name for callers", delete the comment too. Do not modify `highlight_target` itself.

    Sanity: grep the WHOLE tree for `strip_tiles_and_highlight_target` and confirm 0 matches (RESEARCH already verified this — the alias has no external callers; internal callers at vision_branches.py:419 use the canonical name; hailo_drone_detection_manager.py:841 and :1016 mention `highlight_target` only in comments).

    Final grep should return 0:
      grep -rn "strip_tiles_and_highlight_target" --include='*.py' --exclude-dir=.git --exclude-dir=.planning .
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && [ "$(grep -rn 'strip_tiles_and_highlight_target' --include='*.py' --exclude-dir=.git --exclude-dir=.planning . | wc -l)" = "0" ]</automated>
  </verify>
  <done>
    Line 397 deleted. Grep returns 0 matches for the alias name. Full pytest suite green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove create_app(controller_config=...) kwarg (CLEAN-10)</name>
  <files>robot_follow/pipeline_adapter/hailo_drone_detection_manager.py</files>
  <action>
    Open `robot_follow/pipeline_adapter/hailo_drone_detection_manager.py`. Make the four edits per RESEARCH § CLEAN-10 (verified line numbers — re-check exact lines on current file since prior plans may have shifted them; semantics not lines are authoritative):

    1. **Line ~695** — `def create_app(...)`: remove the `controller_config=None` parameter from the signature (and from its docstring if mentioned).
    2. **Line ~731** — `DroneFollowUserData.__init__(...)`: remove `controller_config=None` from the signature.
    3. **Line ~739** — change `self.controller_config = controller_config` to `self.controller_config = None`. KEEP the attribute so `hailo_drone_detection_manager.py:278` (`config = user_data.controller_config`) still works; `robot_follow_app.py:340` attaches the real value post-construction (`app.user_data.controller_config = controller_config`) and that line is NOT touched by this plan.
    4. **Line ~1285** — in the call constructing `DroneFollowUserData(...)`, remove the `controller_config=controller_config` kwarg.

    Critical do-not-touch: `robot_follow/robot_follow_app.py:340` `app.user_data.controller_config = controller_config` is the OUT-OF-BAND attach path; it stays. This plan only removes the constructor kwarg path.

    Sanity-import after edit (this is a tiny smoke that catches obvious typos before pytest):
      python -c "import inspect; from robot_follow.pipeline_adapter.hailo_drone_detection_manager import create_app; assert 'controller_config' not in inspect.signature(create_app).parameters"
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && python -c "import inspect; from robot_follow.pipeline_adapter.hailo_drone_detection_manager import create_app; assert 'controller_config' not in inspect.signature(create_app).parameters, 'create_app still has controller_config param'; print('OK')"</automated>
  </verify>
  <done>
    `create_app` signature has no `controller_config` parameter. Inline `python -c` import-and-inspect smoke passes. `app.user_data.controller_config` still exists (initialised to None inside `DroneFollowUserData.__init__`). Full pytest suite green.
  </done>
</task>

</tasks>

<verification>
1. Per-task automated verifies (above).
2. Quick suite: `python -m pytest robot_follow/tests/test_install_smoke.py robot_follow/tests/test_shared_state.py robot_follow/tests/test_config_persistence.py robot_follow/tests/test_velocity_command_shape.py -x` — < 2 s, green.
3. Full suite: `python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py` — < 30 s, green.
4. CLEAN-04 doc-only item is handled in plan 02-03 (it lives in `mavsdk_drone.py` alongside CLEAN-08).
</verification>

<success_criteria>
- CLEAN-01: `test ! -f sim/world_loader.py` AND no grep hits outside .git/.planning.
- CLEAN-02: `test ! -f scripts/bench_reid_callback.py` AND no grep hits outside .git/.planning.
- CLEAN-06: `grep -n 'strip_tiles_and_highlight_target' robot_follow/pipeline_adapter/vision_branches.py` returns 0 lines.
- CLEAN-10: `python -c "...assert 'controller_config' not in inspect.signature(create_app).parameters"` exits 0.
- Full pytest suite green (xfail tests from 02-00 still expected-fail).
</success_criteria>

<output>
After completion, create `.planning/phases/02-cleanup/02-01-SUMMARY.md` documenting the 4 deletions, the post-edit grep gates, and the pytest counts (before/after).
</output>
