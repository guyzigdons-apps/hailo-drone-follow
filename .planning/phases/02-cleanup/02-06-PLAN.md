---
phase: 02-cleanup
plan: 06
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - robot_follow/follow_api/config.py
  - robot_follow/servers/web_server.py
  - robot_follow/servers/openhd_bridge.py
autonomous: true
requirements:
  - CLEAN-14
must_haves:
  truths:
    - "`ControllerConfig.tunable_fields()` classmethod exists and returns a dict mapping attr names to a `TunableField(py_type, mavlink_id)` schema (NamedTuple or frozen dataclass)."
    - "`web_server._CONFIG_FIELDS` symbol is gone; `web_server` iterates `ControllerConfig.tunable_fields()` directly."
    - "`openhd_bridge._CONFIG_PARAMS` symbol is gone; `openhd_bridge` iterates `ControllerConfig.tunable_fields()` directly and reads both `py_type` and `mavlink_id` per entry."
    - "Union of fields from the previous two dicts is exposed (web_server-only fields have `mavlink_id=None`); no fields silently disappear."
  artifacts:
    - path: "robot_follow/follow_api/config.py"
      provides: "ControllerConfig.tunable_fields() classmethod; TunableField helper type"
      contains: "def tunable_fields"
    - path: "robot_follow/servers/web_server.py"
      provides: "Iterates ControllerConfig.tunable_fields() in place of _CONFIG_FIELDS"
      contains: "tunable_fields"
    - path: "robot_follow/servers/openhd_bridge.py"
      provides: "Iterates ControllerConfig.tunable_fields() in place of _CONFIG_PARAMS, skipping entries where mavlink_id is None"
      contains: "tunable_fields"
  key_links:
    - from: "robot_follow/servers/web_server.py"
      to: "robot_follow.follow_api.config.ControllerConfig.tunable_fields"
      via: "classmethod call returning dict[str, TunableField]"
      pattern: "ControllerConfig\\.tunable_fields\\(\\)"
    - from: "robot_follow/servers/openhd_bridge.py"
      to: "robot_follow.follow_api.config.ControllerConfig.tunable_fields"
      via: "classmethod call; iteration filters None mavlink_id"
      pattern: "ControllerConfig\\.tunable_fields\\(\\)"
---

<objective>
Wave 2C — Single duplication merge: replace `web_server._CONFIG_FIELDS` (27 entries) and `openhd_bridge._CONFIG_PARAMS` (24 entries) with a single source-of-truth `ControllerConfig.tunable_fields()` classmethod returning `{attr_name: TunableField(py_type, mavlink_id_or_None)}`.

**Decision (per planning_guidance Open Question 1):** Unify the Python side only. The two existing dicts differ by ~3 keys (likely `top_margin_safety`, `bottom_margin_safety`, and one more). For Phase 2, the Python-side merged list MUST include the UNION of both — fields exposed only to web_server get `mavlink_id=None`; OpenHD's `_apply_param_change` and `_send_report` paths SKIP entries with `mavlink_id=None`. This means OpenHD's MAVLink schema remains the 24 entries it currently exposes — NO new MAVLink IDs are added in Phase 2. Adding MAVLink IDs would require an OpenHD C++ patch (see `OpenHD/HAILO_INTEGRATION.md`); that work is deferred to v1.2.

depends_on: 02-02 — Plan 02-02 already touched `web_server.py` (`_NULLABLE_FIELDS` removal at line ~325) and `openhd_bridge.py` (lines 68, 310, 359). 02-06 touches DIFFERENT BLOCKS (`_CONFIG_FIELDS` at web_server.py:259-286; `_CONFIG_PARAMS` at openhd_bridge.py:40-65) but sequencing keeps the file's history linear.

Output: Single source of truth; ROADMAP success criterion 4 closed.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cleanup/02-RESEARCH.md
@.planning/phases/02-cleanup/02-VALIDATION.md
@robot_follow/follow_api/config.py
@robot_follow/servers/web_server.py
@robot_follow/servers/openhd_bridge.py

<interfaces>
Add to `robot_follow/follow_api/config.py`:

  from typing import NamedTuple, Optional

  class TunableField(NamedTuple):
      """Schema entry for runtime-mutable ControllerConfig fields.

      py_type: the Python type used to coerce inbound values (web_server JSON or
        OpenHD MAVLink params).
      mavlink_id: short OpenHD param ID (<=16 chars); None means web-UI-only.
      """
      py_type: type
      mavlink_id: Optional[str]

  Inside class ControllerConfig:
      @classmethod
      def tunable_fields(cls) -> dict[str, TunableField]:
          """Source of truth for runtime-mutable fields.

          web_server reads .py_type for coercion; openhd_bridge reads both
          py_type and mavlink_id, skipping entries where mavlink_id is None.
          """
          return {
              # Drone-specific (have MAVLink IDs in current _CONFIG_PARAMS):
              "kp_yaw": TunableField(float, "DF_KP_YAW"),
              "kp_forward": TunableField(float, "DF_KP_FWD"),
              "kp_altitude": TunableField(float, "DF_KP_ALT"),
              # ... etc. RESEARCH says _CONFIG_PARAMS has 24 entries; copy all of
              # them here with their existing MAVLink IDs verbatim.

              # Web-UI-only (currently in _CONFIG_FIELDS only):
              "top_margin_safety": TunableField(float, None),
              "bottom_margin_safety": TunableField(float, None),
              # ... etc. Compute the set difference at implementation time:
              #   set(_CONFIG_FIELDS) - set(_CONFIG_PARAMS)
              # and add each missing entry with mavlink_id=None.
          }

  IMPORTANT: copy the EXACT entries and EXACT MAVLink ID strings from the existing dicts. Do not invent new MAVLink IDs in Phase 2.

Consumer migration — `robot_follow/servers/web_server.py`:
  At the previous _CONFIG_FIELDS site (around lines 259-286 + 293, 328-336): replace with:
    from robot_follow.follow_api.config import ControllerConfig
    for k, schema in ControllerConfig.tunable_fields().items():
        expected_type = schema.py_type
        ...

Consumer migration — `robot_follow/servers/openhd_bridge.py`:
  At the previous _CONFIG_PARAMS site (lines 40-65 + 176, 307, 357): replace with:
    from robot_follow.follow_api.config import ControllerConfig
    # Iterate only entries that have a MAVLink ID (web-UI-only fields are skipped):
    for attr_name, schema in ControllerConfig.tunable_fields().items():
        if schema.mavlink_id is None:
            continue
        # use schema.py_type and schema.mavlink_id where the dict tuple was used before

  All three openhd_bridge sites (176, 307, 357) iterate over either keys (`for k in _CONFIG_PARAMS:`) or items (`for k, (mid, t) in _CONFIG_PARAMS.items():`). Update each to use the tunable_fields() classmethod + the None-filter pattern.

Test surface:
  Add a small assertion in `robot_follow/tests/test_config_persistence.py` (or create a new tiny test file `test_tunable_fields.py`):
    def test_tunable_fields_source_of_truth():
        from robot_follow.follow_api.config import ControllerConfig, TunableField
        tf = ControllerConfig.tunable_fields()
        assert isinstance(tf, dict)
        assert len(tf) >= 24   # at least the openhd subset
        for k, v in tf.items():
            assert isinstance(v, TunableField)
            assert isinstance(v.py_type, type)
            assert v.mavlink_id is None or (isinstance(v.mavlink_id, str) and len(v.mavlink_id) <= 16)

Open question resolution (planning_guidance Q1):
  - Three-key diff between `_CONFIG_FIELDS` (27) and `_CONFIG_PARAMS` (24): record the exact deltas in the SUMMARY. Diff command:
      python -c "from robot_follow.servers.web_server import _CONFIG_FIELDS; from robot_follow.servers.openhd_bridge import _CONFIG_PARAMS; print('web-only:', set(_CONFIG_FIELDS) - set(_CONFIG_PARAMS))"
    (Run this BEFORE deleting the dicts; record output in SUMMARY for audit.)
  - Web-only fields get mavlink_id=None.
  - No new OpenHD MAVLink IDs in Phase 2 (would need OpenHD C++ patch — deferred).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the 3-key diff and add ControllerConfig.tunable_fields() classmethod</name>
  <files>
    robot_follow/follow_api/config.py,
    robot_follow/tests/test_config_persistence.py
  </files>
  <action>
    Step A — audit BEFORE deleting anything. Run this to capture the exact diff:
      python -c "
      from robot_follow.servers.web_server import _CONFIG_FIELDS
      from robot_follow.servers.openhd_bridge import _CONFIG_PARAMS
      web_only = set(_CONFIG_FIELDS) - set(_CONFIG_PARAMS)
      openhd_only = set(_CONFIG_PARAMS) - set(_CONFIG_FIELDS)
      shared = set(_CONFIG_FIELDS) & set(_CONFIG_PARAMS)
      print('shared count:', len(shared))
      print('web-only:', sorted(web_only))
      print('openhd-only:', sorted(openhd_only))
      "
    Save the output for the SUMMARY. Expected per RESEARCH: 24 shared, ~3 web-only, ~0 openhd-only — but verify on real code.

    Step B — open `robot_follow/follow_api/config.py`. Add at module level (after existing imports):

      from typing import NamedTuple, Optional

      class TunableField(NamedTuple):
          py_type: type
          mavlink_id: Optional[str]

    Step C — inside `class ControllerConfig`, add the classmethod:

      @classmethod
      def tunable_fields(cls) -> "dict[str, TunableField]":
          \"\"\"Source of truth for runtime-mutable fields.

          web_server reads .py_type for coercion; openhd_bridge reads both
          py_type and mavlink_id (skipping entries where mavlink_id is None).
          Web-UI-only fields have mavlink_id=None and are NOT exposed to OpenHD
          (extending the OpenHD MAVLink schema requires a C++ patch — deferred
          to v1.2).
          \"\"\"
          return {
              # COPY EVERY ENTRY FROM openhd_bridge._CONFIG_PARAMS HERE,
              # verbatim, with TunableField(py_type, mavlink_id) wrapping.
              # Then APPEND the web-only deltas with mavlink_id=None.
          }

    Step D — copy the entries:
      1. Open `robot_follow/servers/openhd_bridge.py` lines 40-65. For each entry `"attr_name": ("DF_XXXX", type)`, write to tunable_fields: `"attr_name": TunableField(type, "DF_XXXX"),`.
      2. From the Step A audit, take the web-only set. For each name, look up the type in `web_server._CONFIG_FIELDS` (it's the raw dict value, since web_server has just `{name: type}`). Write to tunable_fields: `"attr_name": TunableField(type, None),`.

    Step E — add a small test assertion. Append to `robot_follow/tests/test_config_persistence.py`:

      def test_tunable_fields_source_of_truth():
          from robot_follow.follow_api.config import ControllerConfig, TunableField
          tf = ControllerConfig.tunable_fields()
          assert isinstance(tf, dict)
          assert len(tf) >= 24, f"expected at least 24 tunable fields, got {len(tf)}"
          for k, v in tf.items():
              assert isinstance(v, TunableField), f"{k} schema is not TunableField: {v!r}"
              assert isinstance(v.py_type, type), f"{k} py_type is not a type"
              assert v.mavlink_id is None or (
                  isinstance(v.mavlink_id, str) and len(v.mavlink_id) <= 16
              ), f"{k} mavlink_id invalid: {v.mavlink_id!r}"

    Step F — verify:
      python -c "from robot_follow.follow_api.config import ControllerConfig; tf = ControllerConfig.tunable_fields(); print(len(tf), 'fields'); print(sorted(tf.keys())[:5])"
      python -m pytest robot_follow/tests/test_config_persistence.py -v
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && python -c "from robot_follow.follow_api.config import ControllerConfig, TunableField; tf = ControllerConfig.tunable_fields(); assert isinstance(tf, dict) and len(tf) >= 24; assert all(isinstance(v, TunableField) for v in tf.values()); print('OK', len(tf))" && python -m pytest robot_follow/tests/test_config_persistence.py -v</automated>
  </verify>
  <done>
    `ControllerConfig.tunable_fields()` returns a dict of ≥ 24 entries, all `TunableField` instances. The classmethod is a pure-data source of truth. Existing config-persistence tests still pass; new `test_tunable_fields_source_of_truth` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate web_server.py and openhd_bridge.py to consume tunable_fields(); delete _CONFIG_FIELDS / _CONFIG_PARAMS</name>
  <files>
    robot_follow/servers/web_server.py,
    robot_follow/servers/openhd_bridge.py
  </files>
  <action>
    Step A — `robot_follow/servers/web_server.py`:
      1. At the top of the file, add `from robot_follow.follow_api.config import ControllerConfig` if not already imported.
      2. Locate `_CONFIG_FIELDS = { ... }` (around lines 259-286). DELETE the entire dict literal.
      3. Locate the consumer at around line 293 (`for k in _CONFIG_FIELDS:`) — change to:
           for k in ControllerConfig.tunable_fields():
      4. Locate the consumer at around lines 328-336 (`expected = _CONFIG_FIELDS[key]`) — change to:
           expected = ControllerConfig.tunable_fields()[key].py_type
         (Cache the call: `_TUNABLE = ControllerConfig.tunable_fields()` at module load time if the iteration happens in a hot path — but classmethod cost is negligible; cache only if profiling demands it. Default: just call inline.)
      5. Verify no remaining references:
           grep -n "_CONFIG_FIELDS" robot_follow/servers/web_server.py
         Expected: 0.

    Step B — `robot_follow/servers/openhd_bridge.py`:
      1. Add `from robot_follow.follow_api.config import ControllerConfig` to imports if not present.
      2. Locate `_CONFIG_PARAMS = { ... }` (around lines 40-65). DELETE the entire dict literal.
      3. Locate the three consumers (lines 176, 307, 357). Each iterates `_CONFIG_PARAMS`. Update each to filter out entries where `mavlink_id is None`:

         Pattern for `for k, (mid, t) in _CONFIG_PARAMS.items():`
         → for attr_name, schema in ControllerConfig.tunable_fields().items():
               if schema.mavlink_id is None:
                   continue
               # Use schema.mavlink_id and schema.py_type instead of mid, t

         Pattern for `if x in _CONFIG_PARAMS:`
         → `tf = ControllerConfig.tunable_fields(); if x in tf and tf[x].mavlink_id is not None:`

         OR, equivalently, define a local cache at the top of the function:
         → `tunable = ControllerConfig.tunable_fields()`
         → `openhd_fields = {k: v for k, v in tunable.items() if v.mavlink_id is not None}`
         and then iterate `openhd_fields`.

      4. Verify no remaining references:
           grep -n "_CONFIG_PARAMS" robot_follow/servers/openhd_bridge.py
         Expected: 0.

    Step C — combined sanity:
      grep -rcE "_CONFIG_FIELDS\b|_CONFIG_PARAMS\b" robot_follow/servers/ --include='*.py'
    Expected: 0 lines.

    Step D — smoke imports:
      python -c "from robot_follow.servers.web_server import SharedUIState; from robot_follow.servers.openhd_bridge import OpenHDBridge; print('OK')"

    Step E — full pytest suite green:
      python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py

    Step F — (manual, defer to phase gate) smoke that POST /api/config still round-trips a field value via the web UI; QOpenHD config-set still applies a param.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && grep -rcE "_CONFIG_FIELDS\b|_CONFIG_PARAMS\b" robot_follow/servers/ --include='*.py' | grep -vE ":0$" | wc -l | grep -q "^0$" && python -c "from robot_follow.servers.web_server import SharedUIState; from robot_follow.servers.openhd_bridge import OpenHDBridge; print('OK')" && python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py</automated>
  </verify>
  <done>
    Neither `_CONFIG_FIELDS` nor `_CONFIG_PARAMS` is grep-findable in `robot_follow/servers/`. Both server modules import cleanly. Full pytest suite green.
  </done>
</task>

</tasks>

<verification>
1. Per-task automated verifies above.
2. Quick suite: `python -m pytest robot_follow/tests/test_config_persistence.py -v` — green; new `test_tunable_fields_source_of_truth` passes.
3. Full suite: `python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py` — green.
4. (Manual, phase gate) web UI POST /api/config round-trip; QOpenHD config-set application.
</verification>

<success_criteria>
- CLEAN-14: `ControllerConfig.tunable_fields()` returns dict with ≥ 24 entries; `_CONFIG_FIELDS` and `_CONFIG_PARAMS` symbols are gone from `robot_follow/servers/`.
- ROADMAP success criterion 4 closed: "A single `ControllerConfig.tunable_fields()` call drives both `web_server` and `openhd_bridge` field lists; no parallel altitude knob lists remain."
- Full pytest suite green.
</success_criteria>

<output>
Create `.planning/phases/02-cleanup/02-06-SUMMARY.md` documenting:
- Step A diff output (web-only / openhd-only / shared counts and names) — historical record for future MAVLink schema extension
- Schema decision (web-only fields get mavlink_id=None; no new MAVLink IDs in Phase 2)
- pytest counts before/after
</output>
