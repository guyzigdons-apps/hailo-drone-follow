---
phase: 02-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["02-00"]
files_modified:
  - robot_follow/follow_api/config.py
  - robot_follow/follow_api/state.py
  - robot_follow/tests/test_shared_state.py
  - robot_follow/servers/web_server.py
  - robot_follow/servers/openhd_bridge.py
autonomous: true
requirements:
  - CLEAN-03
  - CLEAN-07
  - CLEAN-09
must_haves:
  truths:
    - "`vfov` field, `--vfov` CLI flag, and `vfov=...` constructor wiring are all gone from `config.py`."
    - "Legacy `vfov` key in JSON configs no longer raises a strict-schema error; either tolerated (preferred) or stripped from shipped JSONs."
    - "`state.update(detection, available_ids)` requires both args; all 11 call sites in `test_shared_state.py` pass `available_ids=set()` (or explicit set); production callers unchanged."
    - "`_NULLABLE_FIELDS` is gone from both `web_server.py` and `openhd_bridge.py`; the 3 dead `if key in _NULLABLE_FIELDS` branches are collapsed."
  artifacts:
    - path: "robot_follow/follow_api/config.py"
      provides: "ControllerConfig with no `vfov` attribute; no `--vfov` argparse registration; no `vfov=` in `from_args` constructor"
      contains: "@dataclass"
    - path: "robot_follow/follow_api/state.py"
      provides: "SharedDetectionState.update(detection, available_ids) — no default value on `available_ids`"
      contains: "def update"
    - path: "robot_follow/tests/test_shared_state.py"
      provides: "All 11 `state.update(...)` call sites pass an explicit `available_ids=` argument"
      min_lines: 80
  key_links:
    - from: "robot_follow/follow_api/state.py update()"
      to: "robot_follow/pipeline_adapter/hailo_drone_detection_manager.py callers"
      via: "Production callers all already pass available_ids — no production change. Tests fix-up is the load-bearing part."
      pattern: "state\\.update\\(.+,\\s*available_ids\\s*="
---

<objective>
Wave 1B — Three dead-code groups: CLEAN-03 (delete `vfov` field + flag), CLEAN-07 (tighten `state.update()` signature; co-update 11 test call sites in same commit), CLEAN-09 (remove empty `_NULLABLE_FIELDS` set + 3 dead branches in servers).

Purpose: All three live in different files than plan 02-01 and from plan 02-03, so this plan runs in parallel with those. CLEAN-07 is the only one in this phase that requires a same-commit signature change + test update (atomic) per RESEARCH § CLEAN-07 — 11 test call sites at `test_shared_state.py:32, 40, 46, 47, 54, 55, 56, 63, 66, 82, 104` use single-arg `state.update(d)` form.
Output: Three small surgical changes; full pytest suite green on every intermediate commit.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cleanup/02-RESEARCH.md
@.planning/phases/02-cleanup/02-VALIDATION.md
@robot_follow/follow_api/config.py
@robot_follow/follow_api/state.py
@robot_follow/tests/test_shared_state.py
@robot_follow/servers/web_server.py
@robot_follow/servers/openhd_bridge.py

<interfaces>
<!-- Confirmed via RESEARCH grep — every line ref still resolves cleanly post-rename. -->

CLEAN-03 — `robot_follow/follow_api/config.py` three sites:
- Line ~21: dataclass field `vfov: float = 41.0` — DELETE
- Line ~172: argparse `group.add_argument("--vfov", ...)` — DELETE
- Line ~278: constructor wiring `vfov=_arg("vfov", default=defaults.vfov)` — DELETE
- Compare-against: `hfov` (the kept field) is read at `controller.py:147, :158`. `vfov` has ZERO `.vfov` / `config.vfov` / `cfg.vfov` readers.

JSON-config tolerance check (RESEARCH Open Question 5): before deleting, OPEN `config.py` and read `from_args` and `load_from_file` to confirm extra-key tolerance:
  - If `from_args` / `load_from_file` accept unknown keys (use `getattr` or filter on field names), the legacy `"vfov": 41.0` entry in `df_config.example.json:3`, `sim/configs/simulation.json:7`, `sim/configs/simulation_follow.json:11` will be silently ignored — PREFERRED outcome, no JSON edits needed.
  - If they reject unknown keys (strict schema), ALSO strip the `"vfov": 41.0` line from those three JSON files in the same commit.
  - The plan-author judgement: research's expectation is that loader is tolerant; if not, strip the JSONs.

CLEAN-07 — `robot_follow/follow_api/state.py:19`:
  CURRENT:
    def update(self, detection: Optional[Detection], available_ids: set = None):
        with self._lock:
            self._detection = detection
            self._frame_count += 1
            if available_ids is not None:
                self._available_ids = available_ids
  TARGET:
    def update(self, detection: Optional[Detection], available_ids: set):
        with self._lock:
            self._detection = detection
            self._frame_count += 1
            self._available_ids = available_ids

  Production callers (NO CHANGE NEEDED — all pass available_ids explicitly):
    - hailo_drone_detection_manager.py:283 (`available_ids=set()`)
    - hailo_drone_detection_manager.py:377, 437, 464, 472, 494, 501, 534 (all `available_ids=...`)

  Test callers (MUST UPDATE — 11 sites in `test_shared_state.py`):
    - Lines 32, 40, 46, 47, 54, 55, 56, 63, 66, 82, 104 — currently call `state.update(d)` or `state.update(None)`. Update each to `state.update(d, available_ids=set())` or `state.update(None, available_ids=set())`. The test bodies only assert `detection` and `frame_count`, never `available_ids`, so `set()` is a safe default.
    - test_follow_server.py:48, 64, 116, 154 — already pass `available_ids={...}` ✓ NO CHANGE.

  Atomicity gate: state.py + test_shared_state.py edits land in ONE commit so pytest stays green at every commit.

CLEAN-09 — five sites across two files:
  - `robot_follow/servers/web_server.py:325`: `_NULLABLE_FIELDS = set()` — DELETE
  - `robot_follow/servers/web_server.py:333`: `if key in _NULLABLE_FIELDS and (value is None or value == 0):` (dead branch) — collapse the `if x: do_a else: do_b` block to `do_b` (i.e., only the else-branch runs).
  - `robot_follow/servers/openhd_bridge.py:68`: module-level `_NULLABLE_FIELDS = set()` — DELETE
  - `robot_follow/servers/openhd_bridge.py:310`: `if param_name in _NULLABLE_FIELDS and value == 0:` (dead branch) — collapse.
  - `robot_follow/servers/openhd_bridge.py:359`: `if param_name in _NULLABLE_FIELDS and py_value is None:` (dead branch) — collapse.

  NOTE: CLEAN-09 touches the same files as CLEAN-14 (plan 02-06) but DIFFERENT BLOCKS (research § File-Conflict Matrix). web_server.py:325-336 is the CLEAN-09 block; web_server.py:259-286 (`_CONFIG_FIELDS`) is the CLEAN-14 block. Same file, different lines — safe to land CLEAN-09 in this plan before CLEAN-14 lands.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete vfov field/flag/wiring from config.py (CLEAN-03)</name>
  <files>
    robot_follow/follow_api/config.py,
    df_config.example.json,
    sim/configs/simulation.json,
    sim/configs/simulation_follow.json
  </files>
  <action>
    Step A — verify config loader tolerance BEFORE deleting:
      Open `robot_follow/follow_api/config.py`. Locate `from_args` (around line ~270) and `load_from_file` (search for `def load_from_file`). Determine whether unknown keys in input dicts are tolerated:
      - If they use `dataclasses.fields(cls)` + filter-by-name (or `**filter_dict(...)`): unknown keys are silently dropped — TOLERANT — no JSON edits needed.
      - If they `**raw` directly into the dataclass constructor: unknown keys raise `TypeError` — STRICT — must strip the JSONs.

    Step B — delete the three source sites:
      1. Line ~21: remove the dataclass field `vfov: float = 41.0` (delete the whole line, plus the trailing comment if any).
      2. Line ~172: remove the `group.add_argument("--vfov", ...)` block (the whole call, including any continuation lines).
      3. Line ~278: remove the `vfov=_arg("vfov", default=defaults.vfov)` line from the `from_args` constructor wiring.

    Step C — handle JSON configs based on Step A's finding:
      - If TOLERANT: leave `df_config.example.json:3`, `sim/configs/simulation.json:7`, `sim/configs/simulation_follow.json:11` unchanged; the legacy `"vfov": 41.0` becomes a silently-ignored key. Document in commit message that loader tolerance was confirmed.
      - If STRICT: open each JSON and delete the `"vfov": 41.0,` line (mind trailing-comma JSON formatting — the line above may need its trailing comma removed if `vfov` was the last entry of the object).

    Step D — confirm test coverage:
      Open `robot_follow/tests/test_config_persistence.py`. If there's a roundtrip test that asserts `cfg.vfov == 41.0` (unlikely per RESEARCH but verify), DELETE that assertion. If the test asserts the set of `fields(cls)`, update the expected count.

      Add one negative-assertion test (small) — at the bottom of `test_config_persistence.py`:
        def test_vfov_field_removed():
            from robot_follow.follow_api.config import ControllerConfig
            from dataclasses import fields
            assert "vfov" not in {f.name for f in fields(ControllerConfig)}

    Step E — `--help` smoke:
      drone-follow --help 2>&1 | grep -i "\-\-vfov"
      # Expected: 0 lines.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && python -m pytest robot_follow/tests/test_config_persistence.py -v && python -c "from dataclasses import fields; from robot_follow.follow_api.config import ControllerConfig; assert 'vfov' not in {f.name for f in fields(ControllerConfig)}, 'vfov field still present'; print('OK')" && ! drone-follow --help 2>&1 | grep -qE "^\s*--vfov\b"</automated>
  </verify>
  <done>
    `vfov` field absent from `ControllerConfig`; `--vfov` absent from `--help`; `from_args` no longer references `vfov`; `test_config_persistence.py` passes with the new negative assertion; full suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten state.update signature + fix 11 test call sites in one commit (CLEAN-07)</name>
  <files>
    robot_follow/follow_api/state.py,
    robot_follow/tests/test_shared_state.py
  </files>
  <action>
    ATOMIC EDIT — both files must land in one commit, otherwise pytest fails between the two edits.

    Step A — `robot_follow/follow_api/state.py:19`:
      Change the signature from `def update(self, detection: Optional[Detection], available_ids: set = None):` to `def update(self, detection: Optional[Detection], available_ids: set):` (remove the default). Inside the body, remove the `if available_ids is not None:` guard and unconditionally assign `self._available_ids = available_ids`.

    Step B — `robot_follow/tests/test_shared_state.py`: update each of the 11 call sites to pass `available_ids=set()` (or an explicit non-empty set if the test logic checks `available_ids`; per RESEARCH none of them do — they only assert `detection` and `frame_count`):

      Lines to update (1-based, verified per RESEARCH): 32, 40, 46, 47, 54, 55, 56, 63, 66, 82, 104.

      Pattern:
        state.update(d)            -> state.update(d, available_ids=set())
        state.update(None)         -> state.update(None, available_ids=set())
        state.update(detection)    -> state.update(detection, available_ids=set())

      Search-and-replace pattern that is safe (Python regex):
        state\.update\(([^,)]+)\)  -> state.update(\1, available_ids=set())
      But ONLY apply where there's exactly one argument; do not break call sites that already pass `available_ids=` (none exist in test_shared_state.py per RESEARCH; the 11 sites are all single-arg).

    Step C — verify both edits in one go BEFORE committing:
      python -m pytest robot_follow/tests/test_shared_state.py -v
      # Expected: all tests pass; no TypeError about missing required argument.

    Step D — broader pytest:
      python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py
      # Expected: green.

    Commit the two files together in one commit.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && python -m pytest robot_follow/tests/test_shared_state.py -v && python -c "import inspect; from robot_follow.follow_api.state import SharedDetectionState; sig = inspect.signature(SharedDetectionState.update); ai = sig.parameters.get('available_ids'); assert ai is not None and ai.default is inspect.Parameter.empty, 'available_ids must have no default'; print('OK')"</automated>
  </verify>
  <done>
    `state.py:19` signature has no default for `available_ids`. `test_shared_state.py` passes all 11+ tests. Both edits in one commit (verified via `git log --stat HEAD`). Full pytest suite green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove _NULLABLE_FIELDS and dead branches from servers (CLEAN-09)</name>
  <files>
    robot_follow/servers/web_server.py,
    robot_follow/servers/openhd_bridge.py
  </files>
  <action>
    Important: this plan touches `web_server.py` and `openhd_bridge.py` at LINES DIFFERENT FROM plan 02-06 (CLEAN-14). Specifically: CLEAN-09 is around web_server.py:325-336 and openhd_bridge.py:68, :310, :359 — none of these overlap with CLEAN-14's `_CONFIG_FIELDS` (web_server.py:259-286) / `_CONFIG_PARAMS` (openhd_bridge.py:40-65) blocks. If plan 02-06 has already landed before this one (depends_on says 02-00 only — both can land in any order; but file-conflict matrix says CLEAN-09 and CLEAN-14 are safe to interleave), inspect the current file state and find the equivalent blocks; the lines may have shifted.

    Step A — `robot_follow/servers/web_server.py`:
      1. Locate `_NULLABLE_FIELDS = set()` (around line 325). DELETE the line.
      2. Locate the `if key in _NULLABLE_FIELDS and (value is None or value == 0):` branch (around line 333). Identify the if/else block: this `if` gates a "set the field to None when value is sentinel-0" path; the `else` is the normal "use the value as-is" path. Since `_NULLABLE_FIELDS` is empty, the `if` is dead — collapse: replace `if ...: do_a` with `do_b` only (and remove the `if/else` wrapper).
      3. After the edit, run grep:
           grep -n "_NULLABLE_FIELDS" robot_follow/servers/web_server.py
         Expected: 0 matches.

    Step B — `robot_follow/servers/openhd_bridge.py`:
      1. Locate module-level `_NULLABLE_FIELDS = set()` (around line 68). DELETE the line.
      2. Locate two dead branches (around lines 310 and 359). Collapse each:
         - Line ~310: `if param_name in _NULLABLE_FIELDS and value == 0: ...` — delete the dead branch; the else-path remains.
         - Line ~359: `if param_name in _NULLABLE_FIELDS and py_value is None: ...` — delete the dead branch.
      3. After the edit, run grep:
           grep -n "_NULLABLE_FIELDS" robot_follow/servers/openhd_bridge.py
         Expected: 0 matches.

    Step C — Combined sanity:
      grep -rn "_NULLABLE_FIELDS" robot_follow/ --include='*.py' --exclude-dir=__pycache__
      # Expected: 0 matches.

      python -c "from robot_follow.servers.web_server import SharedUIState; print('OK')"
      python -c "from robot_follow.servers.openhd_bridge import OpenHDBridge; print('OK')"

    Step D — Smoke for web UI POST roundtrip is manual (browser + curl POST to /api/config); skip — relying on full pytest + later phase-gate manual smoke.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && [ "$(grep -rn '_NULLABLE_FIELDS' robot_follow/servers/ --include='*.py' | wc -l)" = "0" ] && python -c "from robot_follow.servers import web_server, openhd_bridge; print('OK')" && python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py</automated>
  </verify>
  <done>
    `grep -rn '_NULLABLE_FIELDS' robot_follow/servers/` returns 0 matches. Both modules import cleanly. Full pytest suite green.
  </done>
</task>

</tasks>

<verification>
1. Per-task verifies above.
2. Quick suite per commit: `python -m pytest robot_follow/tests/test_install_smoke.py robot_follow/tests/test_shared_state.py robot_follow/tests/test_config_persistence.py robot_follow/tests/test_velocity_command_shape.py -x` — green.
3. Full suite end of plan: `python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py` — green.
</verification>

<success_criteria>
- CLEAN-03: `vfov` field absent from `ControllerConfig.fields()`; `--vfov` absent from `--help`; `test_config_persistence.py` green.
- CLEAN-07: `state.update` has no default for `available_ids`; `test_shared_state.py` green; production callers untouched.
- CLEAN-09: `_NULLABLE_FIELDS` symbol gone from both `web_server.py` and `openhd_bridge.py`; dead branches collapsed; both modules import.
- Full pytest suite green (including the xfail tests from 02-00 — still expected-fail).
</success_criteria>

<output>
Create `.planning/phases/02-cleanup/02-02-SUMMARY.md` documenting:
- vfov deletion (and whether JSON configs were strict or tolerant — record the loader behaviour for posterity)
- state.update atomic commit hash
- _NULLABLE_FIELDS grep gate result
- pytest counts before/after
</output>
