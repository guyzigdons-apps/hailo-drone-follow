---
phase: 02-cleanup
plan: 05
type: execute
wave: 4
depends_on: ["02-00", "02-01", "02-03", "02-04"]
files_modified:
  - robot_follow/robot_follow_app.py
  - robot_follow/pipeline_adapter/vision_branches.py
  - robot_follow/pipeline_adapter/hailo_drone_detection_manager.py
  - robot_follow/tests/test_vision_branches.py
autonomous: true
requirements:
  - CLEAN-12
  - CLEAN-15
must_haves:
  truths:
    - "`robot_follow_app.py` has exactly one `argparse.ArgumentParser(add_help=False)` pre-parser (CLEAN-12)."
    - "`decide_branches(*, openhd, webui, display, record) -> BranchDecision` exists in `vision_branches.py` (CLEAN-15)."
    - "Implicit-display rule (`if not openhd and not webui: display = True`) appears exactly ONCE in the codebase — inside `decide_branches`. Removed from `robot_follow_app.py:226-227`, `:330-331`, and `hailo_drone_detection_manager.py:1166-1167`."
    - "`--openhd --webui` mutual-exclusion check exists exactly ONCE (inside `decide_branches`); the duplicate at `robot_follow_app.py:325-326` is gone."
    - "`test_vision_branches.py` xfail markers are stripped; all 21 tests pass."
  artifacts:
    - path: "robot_follow/pipeline_adapter/vision_branches.py"
      provides: "BranchDecision dataclass + decide_branches() helper"
      contains: "def decide_branches"
    - path: "robot_follow/robot_follow_app.py"
      provides: "Single pre-parser; calls decide_branches() once; uses returned BranchDecision throughout"
      contains: "decide_branches"
    - path: "robot_follow/tests/test_vision_branches.py"
      provides: "21 unit tests for decide_branches, all green (xfail markers removed)"
      contains: "decide_branches"
  key_links:
    - from: "robot_follow/robot_follow_app.py (pre-parse area)"
      to: "robot_follow/pipeline_adapter/vision_branches.decide_branches"
      via: "single call; result stored as BranchDecision; consumed throughout the function"
      pattern: "decide_branches\\("
    - from: "robot_follow/pipeline_adapter/hailo_drone_detection_manager.py _build_pipeline"
      to: "args.display (already authoritative after the pre-parser writes back)"
      via: "Reads args.display; no redundant implicit-display rule"
      pattern: "self\\.options_menu\\.display"
---

<objective>
Wave 2B — Two duplication merges with concurrency considerations: CLEAN-12 (collapse 3 throwaway pre-parsers in `robot_follow_app.py` into one) and CLEAN-15 (add `decide_branches()` helper to `vision_branches.py`; remove 3 implicit-display-rule duplications + 1 duplicate mutex check). Closes the xfail markers in `test_vision_branches.py` (landed by plan 02-00).

Purpose: Both touch `robot_follow_app.py` so they must serialise within this plan. The pre-parser collapse simplifies the call to `decide_branches`, so do CLEAN-12 first. CLEAN-15 then introduces the helper and removes the duplications.

depends_on:
  - 02-00 — landed the failing test scaffolds
  - 02-01 — touched vision_branches.py (CLEAN-06 alias removal — same file, different lines)
  - 02-03 — touched robot_follow_app.py (CLEAN-05 — same file, different lines)
  - 02-04 — touched robot_follow_app.py (CLEAN-11 reaper call — same file, different lines)

Output: One pre-parser, one helper, 4 sites of duplication removed, xfail markers flipped to green.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cleanup/02-RESEARCH.md
@.planning/phases/02-cleanup/02-VALIDATION.md
@robot_follow/robot_follow_app.py
@robot_follow/pipeline_adapter/vision_branches.py
@robot_follow/pipeline_adapter/hailo_drone_detection_manager.py
@robot_follow/tests/test_vision_branches.py

<interfaces>
CLEAN-12 — collapse 3 pre-parsers in `robot_follow/robot_follow_app.py`:

  CURRENT — three independent argparse.ArgumentParser(add_help=False) instances:
    - ui_pre (lines 212-220): --webui, --webui-port, --webui-fps, --openhd, --display, --record, --log-perf
    - reid_pre (lines 267-290): --reid-model, --no-reid, --update-interval, --reid-threshold, --reid-timeout, --reid-drift-threshold, --reid-duplicate-threshold, --reid-refresh-every, --reid-min-gallery-for-drift-check, --reid-bootstrap-consistency, --reid-overlap-skip-iou, --reid-dump-embeddings
    - tracker_pre (lines 312-314): --tracker

  TARGET — single pre-parser. Per RESEARCH § CLEAN-12 the three are independent (parse_known_args is idempotent on sys.argv):
    pre = argparse.ArgumentParser(add_help=False)
    # UI / output branches
    pre.add_argument("--webui", action="store_true")
    pre.add_argument("--webui-port", type=int, default=5001)
    pre.add_argument("--webui-fps", type=int, default=10)
    pre.add_argument("--openhd", action="store_true")
    pre.add_argument("--display", action="store_true")
    pre.add_argument("--record", action="store_true")
    pre.add_argument("--log-perf", action="store_true")
    # ReID
    pre.add_argument("--reid-model", type=str, default=_DEFAULT_REID_HEF)
    pre.add_argument("--no-reid", action="store_true")
    pre.add_argument("--update-interval", type=int, default=30)
    pre.add_argument("--reid-threshold", type=float, default=0.75)
    pre.add_argument("--reid-timeout", type=float, default=20.0)
    pre.add_argument("--reid-drift-threshold", type=float, default=0.6)
    pre.add_argument("--reid-duplicate-threshold", type=float, default=0.9)
    pre.add_argument("--reid-refresh-every", type=int, default=5)
    pre.add_argument("--reid-min-gallery-for-drift-check", type=int, default=6)
    pre.add_argument("--reid-bootstrap-consistency", type=int, default=3)
    pre.add_argument("--reid-overlap-skip-iou", type=float, default=0.5)
    pre.add_argument("--reid-dump-embeddings", action="store_true")
    # Tracker
    pre.add_argument("--tracker", default=DEFAULT_TRACKER, choices=TRACKER_CHOICES)
    pre_args, _ = pre.parse_known_args()

  All downstream references `ui_pre_args.X` / `reid_pre_args.X` / `tracker_pre_args.X` → `pre_args.X`.

  IMPORTANT: copy the EXACT defaults from the existing add_argument calls. The values above are from RESEARCH and may differ subtly from current defaults — re-read each existing add_argument before writing the consolidated parser.

  TRACKER_CHOICES / DEFAULT_TRACKER import (currently late at line 311): move to top-of-function with other imports, or keep where it is — either works since the module is already imported unconditionally per RESEARCH. Mechanical rename.

CLEAN-15 — `decide_branches` helper + remove duplications:

  Add to `robot_follow/pipeline_adapter/vision_branches.py`:
    from dataclasses import dataclass

    @dataclass(frozen=True)
    class BranchDecision:
        display: bool
        record_branch_enabled: bool
        webui: bool
        openhd: bool

    def decide_branches(*, openhd: bool, webui: bool, display: bool, record: bool) -> BranchDecision:
        """Single source of truth for the display/record/webui/openhd branch decision.

        Applies the implicit-display rule (no UI flag set -> display defaults on) and
        the record-branch gating rule (record auto-on when webui or openhd is on).
        Raises ValueError when openhd and webui are both set (mutually exclusive
        network encoders).
        """
        if openhd and webui:
            raise ValueError(
                "--openhd and --webui are mutually exclusive "
                "(only one network encoder may run at a time)"
            )
        if not openhd and not webui:
            display = True
        record_branch_enabled = record or webui or openhd
        return BranchDecision(
            display=display,
            record_branch_enabled=record_branch_enabled,
            webui=webui,
            openhd=openhd,
        )

  Removals (3 implicit-display duplications + 1 duplicate mutex check):
    1. robot_follow_app.py:222-224 — KEEP the mutex check at pre-parse time (raises early before heavy parser builds); IT WILL NOW DELEGATE to decide_branches() — see "wiring" below.
    2. robot_follow_app.py:226-227 (implicit-display) — REMOVE; supplanted by decide_branches.
    3. robot_follow_app.py:240-244 (record_branch_enabled = record or webui or openhd) — REMOVE; replace with `decision.record_branch_enabled`.
    4. robot_follow_app.py:325-326 (DUPLICATE mutex check after full parse) — REMOVE entirely.
    5. robot_follow_app.py:330-331 (implicit-display AGAIN after full parse) — REMOVE; the pre-parser already wrote back to `args` via `args.display = decision.display`.
    6. hailo_drone_detection_manager.py:1166-1167 (third implicit-display site inside _build_pipeline) — REMOVE; by the time _build_pipeline runs, args.display is authoritative.

  Wiring in robot_follow_app.py (after the single pre-parse from Task 1):
    from robot_follow.pipeline_adapter.vision_branches import decide_branches
    decision = decide_branches(
        openhd=pre_args.openhd,
        webui=pre_args.webui,
        display=pre_args.display,
        record=pre_args.record,
    )
    # Write back to pre_args so downstream args.display / args.record_branch_enabled is right
    pre_args.display = decision.display
    pre_args.record_branch_enabled = decision.record_branch_enabled  # new attribute, used at the previous lines 240-244 site

  AFTER the full parser builds and parses to `args`, propagate the same writeback:
    args.display = decision.display
  (decision is captured from the closure; no second decide_branches call needed.)

  Test xfail flip:
    In `robot_follow/tests/test_vision_branches.py`, remove every `@pytest.mark.xfail(...)` decorator. All 21 tests should now pass (`python -m pytest robot_follow/tests/test_vision_branches.py -v`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse 3 pre-parsers into one (CLEAN-12)</name>
  <files>robot_follow/robot_follow_app.py</files>
  <action>
    Step A — read `robot_follow/robot_follow_app.py` lines 200-320 to map the three pre-parsers exactly. Note the EXACT default values for every add_argument; record them.

    Step B — replace the three blocks (ui_pre @ 212-220, reid_pre @ 267-290, tracker_pre @ 312-314) with a single `pre = argparse.ArgumentParser(add_help=False)` containing the union of all flags. Use the defaults you extracted in Step A (do not blindly copy the example in the <interfaces> block — those came from RESEARCH and may have drifted).

    Step C — replace all downstream references:
      - `ui_pre_args.X` → `pre_args.X`
      - `reid_pre_args.X` → `pre_args.X`
      - `tracker_pre_args.X` → `pre_args.X`
    Use a global find-replace across the function body.

    Step D — verify only ONE `ArgumentParser(add_help=False)` remains:
      grep -c "ArgumentParser(add_help=False)" robot_follow/robot_follow_app.py
    Expected: 1.

    Step E — `--help` smoke (the install_smoke test does parser.prog byte-identical check; this confirms the consolidated parser still recognises every flag):
      python -m pytest robot_follow/tests/test_install_smoke.py -v
      drone-follow --help > /tmp/help_after.txt 2>&1
      wc -l /tmp/help_after.txt   # Sanity: non-trivially many lines
      grep -E "^\s+--(webui|openhd|reid-threshold|tracker|update-interval)" /tmp/help_after.txt
    Expected: all flag names appear.

    Step F — full pytest suite remains green:
      python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py

    Implementation note for the next task: leave the `decision = decide_branches(...)` wiring for Task 2 — this task ONLY consolidates the pre-parser. The mutual-exclusion check at lines 222-224 stays AS-IS for now (we'll route it through decide_branches in Task 2).
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && grep -c "ArgumentParser(add_help=False)" robot_follow/robot_follow_app.py | grep -q "^1$" && python -m pytest robot_follow/tests/test_install_smoke.py -v && drone-follow --help > /dev/null 2>&amp;1</automated>
  </verify>
  <done>
    Exactly one `ArgumentParser(add_help=False)` in `robot_follow_app.py`. All downstream references use the unified `pre_args` namespace. `drone-follow --help` parses without error; install_smoke tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add decide_branches helper, remove 3 implicit-display dups + 1 duplicate mutex, strip xfail markers (CLEAN-15)</name>
  <files>
    robot_follow/pipeline_adapter/vision_branches.py,
    robot_follow/robot_follow_app.py,
    robot_follow/pipeline_adapter/hailo_drone_detection_manager.py,
    robot_follow/tests/test_vision_branches.py
  </files>
  <action>
    Step A — add `BranchDecision` dataclass and `decide_branches` helper to `robot_follow/pipeline_adapter/vision_branches.py`. Insert near the top of the module (after imports, before existing top-level functions). Use the implementation from the <interfaces> block above.

      Imports required (add if not already at top of file):
        from dataclasses import dataclass

    Step B — wire `decide_branches` into `robot_follow/robot_follow_app.py`:
      1. Add `from robot_follow.pipeline_adapter.vision_branches import decide_branches` to the imports at the top of the file (or to the existing `from robot_follow.pipeline_adapter.vision_branches import ...` line if one exists).
      2. After the single pre-parser from Task 1 produces `pre_args`, call:
           decision = decide_branches(
               openhd=pre_args.openhd,
               webui=pre_args.webui,
               display=pre_args.display,
               record=pre_args.record,
           )
           pre_args.display = decision.display
           pre_args.record_branch_enabled = decision.record_branch_enabled
      3. REMOVE the old implicit-display rule at lines 226-227 (`if not ui_pre_args.openhd and not ui_pre_args.webui: ui_pre_args.display = True`). It's replaced by `pre_args.display = decision.display` above.
      4. REMOVE the old mutex check at lines 222-224 — `decide_branches` raises ValueError inside on the same condition. KEEP a guard if you want to catch the ValueError and re-raise as `argparse.ArgumentError` for a cleaner CLI error; otherwise the bare exception is fine.
      5. REMOVE the old `record_branch_enabled = ui_pre_args.record or ui_pre_args.webui or ui_pre_args.openhd` at lines 240-244 — replace usages with `pre_args.record_branch_enabled` (or `decision.record_branch_enabled` if the variable is in scope; both work).
      6. REMOVE the duplicate implicit-display at lines 330-331 (after full parse): `if not getattr(args, "openhd", False) and not getattr(args, "webui", False): args.display = True`. Replace with `args.display = decision.display` (since `decision` is captured in scope).
      7. REMOVE the duplicate mutex check at lines 325-326 entirely — already handled by decide_branches.

    Step C — remove the third implicit-display site in `robot_follow/pipeline_adapter/hailo_drone_detection_manager.py:1166-1167`:
      CURRENT (inside `_build_pipeline`, reading `self.options_menu`):
        if not openhd and not webui:
            display = True
      TARGET: DELETE these two lines. By the time `_build_pipeline` runs, `args.display` (a.k.a. `self.options_menu.display`) is authoritative from the pre-parser writeback.

      Update the surrounding code if it uses local `display` / `openhd` / `webui` variables: ensure all reads go to `self.options_menu.display` etc.

    Step D — strip xfail markers from `robot_follow/tests/test_vision_branches.py`:
      Remove every line starting with `@pytest.mark.xfail(...)` AND remove the `XFAIL_REASON` module-level constant (or leave it as documentation; either works). After stripping, the lazy import shim `_decide()` can be replaced with a module-level import:
        from robot_follow.pipeline_adapter.vision_branches import decide_branches
      And the test bodies that called `_decide()(...)` should now call `decide_branches(...)` directly. (You may leave the `_decide()` helper as-is — both styles work; cleaner to inline the import now that the symbol exists.)

    Step E — run the test file in isolation; ALL 21 tests must pass:
      python -m pytest robot_follow/tests/test_vision_branches.py -v
    Expected: 21 passed, 0 failed, 0 xfail.

    Step F — sanity grep for implicit-display rule duplication:
      grep -rnE "not (\w+\.)?openhd and not (\w+\.)?webui" robot_follow/ --include='*.py' --exclude-dir=__pycache__
    Expected: AT MOST 1 line (inside `decide_branches`). If you see 2+, find and remove the stragglers.

    Step G — full pytest suite green:
      python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && python -c "from robot_follow.pipeline_adapter.vision_branches import decide_branches, BranchDecision; d = decide_branches(openhd=False, webui=True, display=False, record=False); assert d.display is False and d.webui is True and d.record_branch_enabled is True; print('OK')" && python -m pytest robot_follow/tests/test_vision_branches.py -v && python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py</automated>
  </verify>
  <done>
    `decide_branches` exists in `vision_branches.py` and is callable. `test_vision_branches.py` reports 21 passed, 0 xfail. Implicit-display rule appears in exactly one place (the helper body). Duplicate mutex check gone. Full pytest suite green.
  </done>
</task>

</tasks>

<verification>
1. Per-task automated verifies above.
2. Quick suite: `python -m pytest robot_follow/tests/test_install_smoke.py robot_follow/tests/test_shared_state.py robot_follow/tests/test_config_persistence.py robot_follow/tests/test_velocity_command_shape.py robot_follow/tests/test_vision_branches.py -x` — green.
3. Full suite: `python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py` — green; `test_vision_branches.py` now reports passed (not xfail).
4. `drone-follow --help` parses and lists every flag from the original 3 pre-parsers.
</verification>

<success_criteria>
- CLEAN-12: One `ArgumentParser(add_help=False)` in `robot_follow_app.py`; install_smoke passes; full suite green.
- CLEAN-15: `decide_branches` exported from `vision_branches`; 21 unit tests in `test_vision_branches.py` pass (xfail markers stripped); implicit-display rule grep returns ≤ 1 hit.
- ROADMAP success criterion 5 partially closed: "Branch-decision tree is defined in one place in vision_branches; implicit-display rule appears exactly once." (Phase gate manual smoke will confirm full end-to-end behaviour.)
</success_criteria>

<output>
Create `.planning/phases/02-cleanup/02-05-SUMMARY.md` documenting:
- Pre-parser consolidation diff stat
- `decide_branches` signature + BranchDecision shape
- All 4 duplication-removal sites (line numbers before/after)
- `test_vision_branches.py` xpass count
- pytest counts before/after
</output>
