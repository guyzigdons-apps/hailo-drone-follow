---
phase: 02-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - robot_follow/drone_api/mavsdk_drone.py
  - robot_follow/drone_api/__init__.py
  - robot_follow/robot_follow_app.py
autonomous: true
requirements:
  - CLEAN-11
  - CLEAN-13
must_haves:
  truths:
    - "Single `_reap_mavsdk_server()` helper exists in `robot_follow/drone_api/mavsdk_drone.py`; both call sites (DetachedMavsdkServer.__enter__ and robot_follow_app.py finally-block) call it instead of inlining the `pkill ... mavsdk_server` invocation."
    - "Single position-stream subscription: `_telemetry_position_task` and `_telemetry_altitude_task` are merged into one task (Shape A); both `telemetry_cache` and `altitude_cache` are written from the same async-for loop on `drone.telemetry.position()`."
    - "SITL drone follow still works under `--takeoff-landing` (altitude-hold P-loop receives `altitude_cache['m']`)."
  artifacts:
    - path: "robot_follow/drone_api/mavsdk_drone.py"
      provides: "Single _reap_mavsdk_server() helper; single _telemetry_position_task that writes both dict caches"
      contains: "_reap_mavsdk_server"
    - path: "robot_follow/robot_follow_app.py"
      provides: "finally-block reaper calls the helper instead of inlining pkill"
      contains: "_reap_mavsdk_server"
  key_links:
    - from: "robot_follow/robot_follow_app.py:448 (finally block)"
      to: "robot_follow/drone_api/mavsdk_drone.py _reap_mavsdk_server"
      via: "import from robot_follow.drone_api"
      pattern: "_reap_mavsdk_server\\("
    - from: "robot_follow/drone_api/mavsdk_drone.py:225 (DetachedMavsdkServer.__enter__)"
      to: "robot_follow/drone_api/mavsdk_drone.py _reap_mavsdk_server"
      via: "in-module call"
      pattern: "_reap_mavsdk_server\\("
---

<objective>
Wave 2A — Two duplication merges, both in `mavsdk_drone.py`: CLEAN-11 (unify the `pkill ... mavsdk_server` reaper into a single helper called from both DetachedMavsdkServer.__enter__ and robot_follow_app.py finally) and CLEAN-13 (merge `_telemetry_altitude_task` + `_telemetry_position_task` into a single position-stream subscriber — **Shape A** per RESEARCH § CLEAN-13: keep both dicts, write both from one task; defer Shape B cache-consolidation to Phase 3).

Purpose: Both touch `mavsdk_drone.py`. CLEAN-13 is the bigger lifecycle change (one task spawned eagerly instead of two with different lifecycles). CLEAN-11 is a clean extraction. Land them sequentially within this plan.

depends_on: 02-03 — Plan 02-03 already touches mavsdk_drone.py at lines 47 and 642-665 (different blocks from these edits) but depends_on ordering keeps the file's history linear and avoids merge conflicts.
Output: One helper, one merged task. Drone behaviour unchanged; SITL smoke still works.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cleanup/02-RESEARCH.md
@.planning/phases/02-cleanup/02-VALIDATION.md
@robot_follow/drone_api/mavsdk_drone.py
@robot_follow/drone_api/__init__.py
@robot_follow/robot_follow_app.py

<interfaces>
CLEAN-11 — extract `_reap_mavsdk_server` helper to `robot_follow/drone_api/mavsdk_drone.py`:

  Helper body (per RESEARCH § CLEAN-11):
    def _reap_mavsdk_server() -> None:
        """Kill any straggler mavsdk_server processes scoped to current uid."""
        try:
            subprocess.run(
                ["pkill", "-9", "-u", str(os.getuid()), "-f", "mavsdk_server"],
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                timeout=3,
            )
        except (OSError, subprocess.TimeoutExpired):
            pass

  Replace BOTH call sites with `_reap_mavsdk_server()`:
    1. mavsdk_drone.py:225 — inside DetachedMavsdkServer.__enter__. Keep the `time.sleep(0.3)` after the call (it's specific to the __enter__ flow before starting a new server).
    2. robot_follow_app.py:448 — inside the `finally` block when the drone thread fails to join. Import via `from robot_follow.drone_api.mavsdk_drone import _reap_mavsdk_server` (or via `robot_follow/drone_api/__init__.py` re-export — see Task 1 step C).

CLEAN-13 — merge two telemetry tasks (Shape A from RESEARCH):

  CURRENT (mavsdk_drone.py:345-353 + :369-381):
    async def _telemetry_altitude_task(drone, altitude_cache, shutdown):
        async for position in drone.telemetry.position():
            if shutdown.is_set(): return
            altitude_cache["m"] = position.relative_altitude_m

    async def _telemetry_position_task(drone, telemetry_cache, shutdown):
        async for pos in drone.telemetry.position():
            if shutdown.is_set(): return
            telemetry_cache["lat"] = pos.latitude_deg
            telemetry_cache["lon"] = pos.longitude_deg
            telemetry_cache["abs_alt"] = pos.absolute_altitude_m
            telemetry_cache["rel_alt"] = pos.relative_altitude_m

  TARGET (Shape A — minimal blast radius; keep both dicts):
    async def _telemetry_position_task(drone, telemetry_cache, altitude_cache, shutdown):
        async for pos in drone.telemetry.position():
            if shutdown.is_set(): return
            telemetry_cache["lat"] = pos.latitude_deg
            telemetry_cache["lon"] = pos.longitude_deg
            telemetry_cache["abs_alt"] = pos.absolute_altitude_m
            telemetry_cache["rel_alt"] = pos.relative_altitude_m
            altitude_cache["m"] = pos.relative_altitude_m

  Spawn-site changes:
    - mavsdk_drone.py:709 — current `_telemetry_position_task(drone, telemetry_cache, shutdown)` spawn becomes `_telemetry_position_task(drone, telemetry_cache, altitude_cache, shutdown)` and is the ONLY task spawned for position data.
    - mavsdk_drone.py:718 (`_telemetry_altitude_task` spawn inside `_start_altitude_telemetry`): REMOVE this spawn. `_start_altitude_telemetry` (around lines 730-744) becomes either a no-op or simply ensures `altitude_cache` exists in the dict-tracking structure if any. Read the function and decide; if it does nothing useful after removal, delete it. If it still mediates a lifecycle gate (e.g. only-when-takeoff-landing), keep the function but make its body a pass / a single guarantor that `altitude_cache` was prepared upstream.

  Consumers (NO CHANGE):
    - altitude_cache["m"] at mavsdk_drone.py:487, :528 (live_control_loop altitude-hold P-loop)
    - telemetry_cache at :392, :401-405 (telemetry log task)

  Behavioural note: this means `altitude_cache` is populated EAGERLY (from process start, not gated on `live_control_loop` startup). RESEARCH § CLEAN-13 confirms this is safe — altitude-hold only runs inside `live_control_loop`, which gates on its own conditions; populating the cache earlier is harmless.

  Out of scope: Shape B (drop altitude_cache entirely and have live_control_loop read telemetry_cache["rel_alt"]) — defer to Phase 3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract _reap_mavsdk_server helper and replace both call sites (CLEAN-11)</name>
  <files>
    robot_follow/drone_api/mavsdk_drone.py,
    robot_follow/drone_api/__init__.py,
    robot_follow/robot_follow_app.py
  </files>
  <action>
    Step A — add the helper near the top of `robot_follow/drone_api/mavsdk_drone.py` (after imports, near other module-level utilities):

      def _reap_mavsdk_server() -> None:
          """Kill any straggler mavsdk_server processes scoped to current uid."""
          try:
              subprocess.run(
                  ["pkill", "-9", "-u", str(os.getuid()), "-f", "mavsdk_server"],
                  stdout=subprocess.DEVNULL,
                  stderr=subprocess.DEVNULL,
                  timeout=3,
              )
          except (OSError, subprocess.TimeoutExpired):
              pass

    Confirm `subprocess`, `os` are already imported (they are — used elsewhere in this file). If not, add the import.

    Step B — replace call site 1 at `mavsdk_drone.py:225` (inside `DetachedMavsdkServer.__enter__`):
      CURRENT:
        try:
            subprocess.run(["pkill", "-9", "-u", str(os.getuid()), "-f", "mavsdk_server"],
                           stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, timeout=3)
        except (OSError, subprocess.TimeoutExpired):
            pass
      TARGET:
        _reap_mavsdk_server()
      KEEP the `time.sleep(0.3)` line that follows — it's load-bearing for the reap-then-respawn race per RESEARCH § Out-of-Scope.

    Step C — re-export the helper for external use. Edit `robot_follow/drone_api/__init__.py` and add:
      from .mavsdk_drone import _reap_mavsdk_server

    The leading underscore signals "internal" but it's the standardised name from research; we can rename to `reap_mavsdk_server` (no underscore) if the planner prefers a public name. Decision: keep the leading underscore — this is a tactical helper, not a public API. Add to `__all__` if `__init__.py` uses one; otherwise the bare import is enough.

    Step D — replace call site 2 at `robot_follow/robot_follow_app.py:448` (inside the finally block when drone thread fails to join):
      CURRENT (similar pkill invocation, possibly wrapped in try/except):
        try:
            subprocess.run(["pkill", "-9", "-u", str(os.getuid()), "-f", "mavsdk_server"], ...)
        except (OSError, subprocess.TimeoutExpired):
            pass
      TARGET:
        from robot_follow.drone_api.mavsdk_drone import _reap_mavsdk_server  # (or move to top-of-file imports)
        _reap_mavsdk_server()

    Prefer top-of-file import. If `robot_follow_app.py` already imports from `robot_follow.drone_api.mavsdk_drone`, append `_reap_mavsdk_server` to that import line; otherwise add a new import line. If there are no other uses of `subprocess` or `os.getuid()` in robot_follow_app.py after this edit, you MAY remove those imports — but only if grep confirms zero remaining uses; otherwise keep them.

    Step E — sanity grep across the package: the literal `pkill.*mavsdk_server` should appear in exactly ONE place (the helper body):
      grep -rnE 'pkill.*mavsdk_server' robot_follow/ --include='*.py' --exclude-dir=__pycache__
    Expected: 1 line (inside `_reap_mavsdk_server`).

    Step F — smoke import:
      python -c "from robot_follow.drone_api.mavsdk_drone import _reap_mavsdk_server; print('OK')"
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && grep -rnE 'pkill.*mavsdk_server' robot_follow/ --include='*.py' --exclude-dir=__pycache__ | wc -l | grep -q "^1$" && python -c "from robot_follow.drone_api.mavsdk_drone import _reap_mavsdk_server; print('OK')" && python -m pytest robot_follow/tests/test_install_smoke.py -v</automated>
  </verify>
  <done>
    Helper exists in mavsdk_drone.py. Both call sites use the helper. Whole-tree grep for the pkill literal returns exactly 1 match (the helper body). Smoke import succeeds. Full pytest suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Merge telemetry tasks into single position-stream subscriber, Shape A (CLEAN-13)</name>
  <files>robot_follow/drone_api/mavsdk_drone.py</files>
  <action>
    Step A — rewrite `_telemetry_position_task` to accept both caches (around lines 369-381):
      TARGET:
        async def _telemetry_position_task(drone, telemetry_cache: dict, altitude_cache: dict,
                                           shutdown: asyncio.Event) -> None:
            async for pos in drone.telemetry.position():
                if shutdown.is_set():
                    return
                telemetry_cache["lat"] = pos.latitude_deg
                telemetry_cache["lon"] = pos.longitude_deg
                telemetry_cache["abs_alt"] = pos.absolute_altitude_m
                telemetry_cache["rel_alt"] = pos.relative_altitude_m
                altitude_cache["m"] = pos.relative_altitude_m

    Step B — DELETE `_telemetry_altitude_task` (lines ~345-353). Search for any remaining references with:
      grep -n "_telemetry_altitude_task" robot_follow/drone_api/mavsdk_drone.py
    Expected after edit: 0.

    Step C — update spawn site at line ~709. The current code spawns `_telemetry_position_task(drone, telemetry_cache, shutdown)`; change it to pass `altitude_cache` too:
      asyncio.create_task(_telemetry_position_task(drone, telemetry_cache, altitude_cache, shutdown))

    Ensure `altitude_cache` is initialised in scope at this point (it likely already is — search above the spawn site for `altitude_cache = {}` or similar; if it's only initialised inside `_start_altitude_telemetry`, hoist that init to before the position-task spawn).

    Step D — fix or remove `_start_altitude_telemetry` (around lines 730-744). Read its body. Cases:
      - If its only side effects were spawning `_telemetry_altitude_task` and returning the task handle: it now does nothing useful — DELETE the function AND its single caller (likely inside `live_control_loop` startup). All callers should rely on `altitude_cache` being populated by the position-task instead.
      - If it ALSO initialises `altitude_cache` (creates the dict): hoist that init upstream (before the position-task spawn at line ~709), then delete the function.
      - If it has a lifecycle gate ("only spawn altitude telemetry when --takeoff-landing is set"): remove the gate — the data is now always-on; consumers gate on their own conditions per RESEARCH note.

    Document the choice in the task summary.

    Step E — verify single position-subscription via grep:
      grep -c "drone.telemetry.position()" robot_follow/drone_api/mavsdk_drone.py
    Expected: 1 line.

    Step F — smoke import + signature inspect:
      python -c "import inspect; from robot_follow.drone_api.mavsdk_drone import _telemetry_position_task; sig = inspect.signature(_telemetry_position_task); assert 'altitude_cache' in sig.parameters and 'telemetry_cache' in sig.parameters; print('OK')"

    Step G — if SITL is available on this host: smoke that the full drone path still works:
      drone-follow --input udp://0.0.0.0:5600 --takeoff-landing --yaw-only &
      sleep 30; kill %1 2>/dev/null
    (Skip this if SITL not installed — the post-merge full pytest suite + altitude_cache populated-eagerly behaviour is good enough for plan close; phase gate manual smoke covers `--input usb --webui --yaw-only`.)
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && grep -c "drone.telemetry.position()" robot_follow/drone_api/mavsdk_drone.py | grep -q "^1$" && grep -c "_telemetry_altitude_task" robot_follow/drone_api/mavsdk_drone.py | grep -q "^0$" && python -c "import inspect; from robot_follow.drone_api.mavsdk_drone import _telemetry_position_task; sig = inspect.signature(_telemetry_position_task); assert 'altitude_cache' in sig.parameters and 'telemetry_cache' in sig.parameters; print('OK')" && python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py</automated>
  </verify>
  <done>
    `drone.telemetry.position()` appears exactly once. `_telemetry_altitude_task` symbol removed. `_telemetry_position_task` accepts both `telemetry_cache` and `altitude_cache`. Full pytest suite green.
  </done>
</task>

</tasks>

<verification>
1. Per-task automated verifies.
2. Full suite: `python -m pytest robot_follow/tests -x --ignore=robot_follow/tests/test_sim_worlds.py` — green.
3. (Manual, deferred to phase gate) `drone-follow --input usb --webui --yaw-only` boots and serves the web UI — confirms _reap_mavsdk_server doesn't break the startup path (no mavsdk_server is started on `--input usb` paths, but the import chain must be clean).
</verification>

<success_criteria>
- CLEAN-11: Single `_reap_mavsdk_server` helper; whole-package grep for `pkill.*mavsdk_server` returns 1 line.
- CLEAN-13: Single `drone.telemetry.position()` subscription; `_telemetry_altitude_task` symbol gone; `_telemetry_position_task` writes both dict caches.
- Full pytest suite green (xfail tests from 02-00 still expected-fail).
- altitude_cache populated eagerly (verified by code inspection of spawn site).
</success_criteria>

<output>
Create `.planning/phases/02-cleanup/02-04-SUMMARY.md` documenting:
- Helper extraction (location + caller updates)
- Telemetry merge decision (which Shape A flavour: delete _start_altitude_telemetry vs keep as init-only)
- Spawn-site change at mavsdk_drone.py:~709
- pytest counts and post-edit grep gates
</output>
