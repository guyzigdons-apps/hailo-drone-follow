---
phase: 01-rename
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - drone_follow/
  - drone_follow/drone_follow_app.py
  - pyproject.toml
  - install.sh
  - scripts/install_air.sh
  - scripts/bench_reid_callback.py
  - mafat/run_bench.py
  - mafat/tiling_record.py
  - configs/overlay_style.yaml
  - df_params.json
  - .gitignore
  - README.md
  - CLAUDE.md
  - TROUBLESHOOTING.md
  - PARAMETERS.md
  - SETUP_GUIDE.md
  - RESOLUTION_CONTROL.md
  - TEST_PLAN.md
  - docs/tracking-reid-algorithm.md
  - docs/calibration-flight-guide.md
  - docs/control-architecture.md
  - docs/design-review.md
  - .claude/memory/MEMORY.md
  - .claude/memory/tracking_callback_risks.md
  - .claude/memory/webui_build.md
  - .claude/skills/drone-follow-dev/SKILL.md
autonomous: true
requirements: [RENAME-01, RENAME-02, RENAME-03, RENAME-04, RENAME-05]
must_haves:
  truths:
    - "pip show robot-follow succeeds; pip show drone-follow returns nothing (success criterion 1)"
    - "robot-follow --help and drone-follow --help produce byte-identical output (success criterion 2)"
    - "scripts/start_air.sh syntax-checks clean; boot service unit file unchanged on disk (success criterion 3)"
    - "No 'from drone_follow' or 'import drone_follow' in source tree outside whitelist (success criterion 4)"
    - "Single atomic git commit lands the rename (locked CONTEXT decision)"
    - "git log --follow on any renamed file shows pre-rename history"
  artifacts:
    - path: "robot_follow/"
      provides: "Renamed package directory (was drone_follow/)"
      contains: "robot_follow_app.py"
    - path: "robot_follow/robot_follow_app.py"
      provides: "Main entry point (was drone_follow_app.py)"
      contains: "def main"
    - path: "pyproject.toml"
      provides: "Package metadata: name=robot-follow, both console scripts, packages.include=robot_follow*"
      contains: "name = \"robot-follow\""
    - path: "install.sh"
      provides: "Idempotent pip-uninstall-drone-follow step inserted before pip install -e ."
      contains: "pip uninstall drone-follow -y"
    - path: "robot_follow/tests/test_install_smoke.py"
      provides: "All 10 smoke tests PASS (no skips) post-rename, carried into robot_follow/tests/ via git mv"
  key_links:
    - from: "console scripts (bin/robot-follow, bin/drone-follow)"
      to: "robot_follow.robot_follow_app:main"
      via: "[project.scripts] in pyproject.toml + pip install -e ."
      pattern: "robot_follow\\.robot_follow_app:main"
    - from: "all .py files under robot_follow/"
      to: "robot_follow.* import paths"
      via: "sed-rewritten import statements"
      pattern: "from robot_follow"
    - from: "install.sh"
      to: "robot_follow/ui build location"
      via: "pushd $SCRIPT_DIR/robot_follow/ui"
      pattern: "robot_follow/ui"
---

<objective>
Land the atomic rename `drone_follow` -> `robot_follow` in a single buildable commit. Every commit on the branch must stay buildable (git bisect must survive); every internal import rewritten; pyproject.toml declares BOTH console scripts pointing at the same `main()`; the boot service unit file MUST NOT be touched on disk.

Purpose: This is the milestone-shaping commit for v1.1. Phase 2 (cleanup) and Phase 3 (abstraction) cannot start until the package name reflects the abstraction goal. CONTEXT locks this as ONE commit so the alias-vs-rename split is invisible to deployed units re-running `./install.sh` and to `git bisect`.

Output: A repository where `pip show robot-follow` works, `pip show drone-follow` returns nothing, both `robot-follow --help` and `drone-follow --help` produce byte-identical output, the smoke test from Plan 01-01 PASSES all 10 assertions with zero skips, and the grep-gate returns zero matches outside the docs whitelist.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rename/01-CONTEXT.md
@.planning/phases/01-rename/01-RESEARCH.md
@.planning/phases/01-rename/01-VALIDATION.md
@.planning/phases/01-rename/01-01-SUMMARY.md
@pyproject.toml
@install.sh
@setup_env.sh

<interfaces>
**Phase 1 success criteria (from ROADMAP.md):**
1. `pip show robot-follow` shows renamed package; `pip show drone-follow` returns nothing.
2. `drone-follow --help` and `robot-follow --help` produce identical output.
3. `scripts/start_air.sh` on a fresh checkout runs without path errors; boot service unit file unchanged on disk.
4. All documentation examples using `drone-follow` still work via the alias; no internal import of `drone_follow` remains in source tree.

**Locked from CONTEXT.md:**
- Single atomic commit (git bisect survives every intermediate state).
- `git mv drone_follow robot_follow` (history preserved via `git log --follow`).
- `drone_follow_app.py` ALSO renamed -> `robot_follow_app.py`.
- `pyproject.toml [project] name = "robot-follow"` (single distribution).
- `[project.scripts]`: BOTH `robot-follow` and `drone-follow` map to `robot_follow.robot_follow_app:main`.
- `install.sh` runs `pip uninstall drone-follow -y` BEFORE `pip install -e .` (idempotent).
- Boot service files (`scripts/boot/drone-follow-boot.{service,sh}`, `scripts/boot/install.sh`, `scripts/boot/uninstall.sh`, `~/Desktop/drone-follow.conf`) PRESERVED VERBATIM (RENAME-04).
- `system/drone-network-mode.sh` `DRONE_SERVICE="drone-follow.service"` PRESERVED (user systemd unit; per RESEARCH Open Question 1).
- `.claude/skills/drone-follow-dev/` directory + trigger phrases NOT renamed (out of scope; user's local Claude config).
- `run_drone.sh`, `df_params.json`, `scripts/start_air.sh` FILENAMES preserved (renamed in Phase 3).
- `reid_analysis` stays in `pyproject.toml packages.include`.

**Planner decisions (from RESEARCH Open Questions):**
- Q1 (`system/drone-network-mode.sh`): leave verbatim.
- Q2 (`scripts/bench_reid_callback.py`): rewrite imports (Phase 2 deletes; keeps grep-gate clean).
- Q3 (`drone_follow/ui/package.json` "name"): LEAVE.
- Q4 (`drone_follow/ui/index.html` `<title>Drone Follow UI</title>`): LEAVE (alias rationale).
- Q5 (`docs/superpowers/plans/*.md`): LEAVE (historical; whitelist).
- Q6 (`pyproject.toml` version): bump `"0.1.0"` -> `"1.1.0.dev0"` (PEP 440).
- Q7 (whitespace in `[project.scripts]`): `# alias for boot service` comment on the drone-follow line.
- Q8 (`mafat/*.py` comments): rewrite (mechanical).
- Q9 (`test_sim_worlds.py` RECORDINGS_DIR + `robot_follow_app.py` recordings default): rewrite together.

**Inventory from RESEARCH:**
- 48 `from drone_follow` / `import drone_follow` import lines across 19 .py files.
- 8 non-Python path-string lines across 6 files (pyproject.toml, install.sh, scripts/install_air.sh x2, configs/overlay_style.yaml, df_params.json, .gitignore x2).
- 23 .md files with `drone-follow`/`drone_follow` references outside `.planning/`.
- 6 stale `__pycache__` dirs inside `drone_follow/` (MUST delete pre-verification per Pitfall 1).

**Console-script shim regeneration requirement (Pitfall 2):**
The shim at `hailo-apps/venv_hailo_apps/bin/drone-follow` currently embeds `from drone_follow.drone_follow_app import main`. `pip install -e .` ALONE does NOT regenerate it (the distribution name changes). MUST run `pip uninstall drone-follow -y && pip install -e .` for both `bin/drone-follow` and `bin/robot-follow` to exist with the correct import path.

**Grep-gate whitelist (RENAME-05):**
The pre-commit grep gate uses `git grep` with a whitelist:
- `.planning/` (planning artefacts; expected mentions)
- `README.md`, `CLAUDE.md`, `TROUBLESHOOTING.md` (intentional alias note + a small number of troubleshooting refs)
- `docs/superpowers/plans/` (historical plan records — see Open Question 5)
- `scripts/boot/` (RENAME-04 preserves these verbatim)
- `.claude/skills/drone-follow-dev/` (skill dir + trigger phrases not renamed)
- `system/` (per Open Question 1)
- `MILESTONES.md` (v1.0 historical record)

After filtering through this whitelist, the grep must return zero matches.

**Phase 2/3 boundary (DO NOT touch in this plan):**
- Dead code (`sim/world_loader.py`, `scripts/bench_reid_callback.py` deletion, `--vfov`, etc.) -> Phase 2.
- `drone_follow/drone_api/mavsdk_drone.py` location -> Phase 3 moves to `robot_follow/robot_api/adapters/mavsdk_drone.py`.
- `run_drone()` function name -> Phase 3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directory + rewrite Python imports + pyproject + non-doc paths</name>
  <files>drone_follow/ (entire tree -> robot_follow/), drone_follow/drone_follow_app.py (-> robot_follow/robot_follow_app.py), pyproject.toml, configs/overlay_style.yaml, df_params.json, .gitignore, mafat/run_bench.py, mafat/tiling_record.py, scripts/bench_reid_callback.py</files>
  <action>
    Perform these steps IN ORDER inside a single working-tree state (no commit yet — Task 3 commits everything):

    **Step 1: Verify clean starting state.**
    Run `git status --short`. Expect only the planning artifacts (`.planning/`) and Plan 01-01's test_install_smoke.py changes to be modified. If unrelated files are dirty, STOP and ask.

    **Step 2: Move the package directory and main module.**

    Run the two git-mv commands (`git mv drone_follow robot_follow` then `git mv robot_follow/drone_follow_app.py robot_follow/robot_follow_app.py`). Verify with `git status --short | head` that the moves are recorded as renames.

    **Step 3: Rewrite Python imports across the repo.**

    Apply `sed -i` to every `.py` file under the repo EXCLUDING the `hailo-apps/` and `sim/PX4-Autopilot/` submodules and `.git/`. The sed expression uses 8 `-e` clauses:
    - `s/\bfrom drone_follow\b/from robot_follow/g`
    - `s/\bimport drone_follow\b/import robot_follow/g`
    - `s/\bdrone_follow\.drone_follow_app\b/robot_follow.robot_follow_app/g`
    - `s/\bdrone_follow\.follow_api\b/robot_follow.follow_api/g`
    - `s/\bdrone_follow\.drone_api\b/robot_follow.drone_api/g`
    - `s/\bdrone_follow\.pipeline_adapter\b/robot_follow.pipeline_adapter/g`
    - `s/\bdrone_follow\.servers\b/robot_follow.servers/g`
    - `s/\bdrone_follow\.pipeline_defaults\b/robot_follow.pipeline_defaults/g`

    Patterns 3-8 catch deep path strings (e.g. `import_module("drone_follow.follow_api.types")`) that the simple `from drone_follow` patterns don't.

    This rewrite also touches `scripts/bench_reid_callback.py` (per planner decision Q2 — keeps grep-gate clean; Phase 2 deletes the file).

    Find command shape: `find . -type f -name '*.py' -not -path './hailo-apps/*' -not -path './sim/PX4-Autopilot/*' -not -path './.git/*' -exec sed -i <expressions> {} +`.

    **Step 4: Rewrite Python string-literal path references.**

    Mechanical text rewrites in .py files:
    - `robot_follow/robot_follow_app.py` (around line 101): the recordings default path literal `"drone_follow/recordings"` -> `"robot_follow/recordings"`. Locate with `grep -n 'drone_follow.*recordings' robot_follow/robot_follow_app.py` first.
    - `robot_follow/tests/test_sim_worlds.py` (line ~28): `RECORDINGS_DIR = REPO_ROOT / "drone_follow" / "recordings"` -> `"robot_follow"`. Also rewrite the other 3 `drone_follow` mentions in this file (4 total per RESEARCH inventory).
    - `robot_follow/tests/conftest.py`: 2 comment mentions ("drone_follow package") -> "robot_follow package".
    - `mafat/run_bench.py` and `mafat/tiling_record.py`: 1 comment mention each (`drone-follow`/`drone_follow`) -> `robot-follow`/`robot_follow`.

    Verify with `grep -RnE 'drone_follow/(recordings|pipeline_adapter)|"drone_follow"' robot_follow mafat scripts`. Expect zero output.

    **Step 5: Strip the forward-compat skip guards from `robot_follow/tests/test_install_smoke.py`.**

    The file was written with `pytest.skip("pre-rename tree...")` guards in Plan 01-01 (because the tree was pre-rename then). Now post-rename, those skips are dead code that would silently mask regressions. Edit the file to:
    - Remove the `_robot_follow_installed()` helper.
    - Remove every `pytest.skip("pre-rename tree...")` call site. Each test now asserts unconditionally.
    - Keep all 10 test function names and assertion bodies intact.
    - Update the module docstring to describe the post-rename canonical state (no mention of "forward-compatible" or "skip pre-rename").
    - If `pytest` is no longer imported (no remaining skip calls), drop the `import pytest` line.

    **Step 6: Rewrite pyproject.toml in full.**

    Replace the file contents verbatim with this TOML:
    - `[project]` block: `name = "robot-follow"`, `version = "1.1.0.dev0"`, `description = "Autonomous robot-follow using Hailo AI tiling detection (drone + rover)"`, `requires-python = ">=3.10"`, `dependencies = ["mavsdk", "matplotlib", "numpy", "scipy>=1.11"]`.
    - The unchanged hailo-apps comment block.
    - `[build-system] requires = ["setuptools>=68", "wheel"], build-backend = "setuptools.build_meta"`.
    - `[tool.setuptools.packages.find] include = ["robot_follow*", "reid_analysis*"], exclude = ["configs", "configs.*"]`.
    - `[project.scripts]` with TWO entries: `robot-follow = "robot_follow.robot_follow_app:main"` followed by a comment line `# alias for boot service + muscle memory (preserved permanently; see RENAME-02)` and `drone-follow = "robot_follow.robot_follow_app:main"`.

    **Step 7: Rewrite `configs/overlay_style.yaml` line 4 comment** — `drone_follow/pipeline_adapter/vision_branches.py` -> `robot_follow/pipeline_adapter/vision_branches.py`.

    **Step 8: Rewrite `df_params.json`** — `drone_follow/recordings/*.mp4` path in JSON description (line ~409) -> `robot_follow/recordings/*.mp4`. Also rewrite the other 2 `drone-follow`/`drone_follow` text mentions per RESEARCH inventory (3 total) to `robot-follow`/`robot_follow`. CONTEXT preserves the FILENAME `df_params.json`; only internal text changes.

    **Step 9: Rewrite `.gitignore`** — lines 14-15: `drone_follow/ui/node_modules/` -> `robot_follow/ui/node_modules/`, `drone_follow/ui/build/` -> `robot_follow/ui/build/`.

    **Step 10: Clean stale bytecode (Pitfall 1).**

    Run `find . -name __pycache__ -type d -prune -exec rm -rf {} +` followed by `find . -name '*.pyc' -delete`. These files are gitignored but live on disk and can mask the rename via stale .pyc resolution.

    **Step 11: Sanity grep gate (intermediate — full gate runs in Task 3).**

    Run `git grep -nE 'from drone_follow|import drone_follow' -- '*.py'`. Expect zero output. If non-empty, the sed missed something — investigate before continuing to Task 2.

    Do NOT commit yet. Task 3 commits the entire atomic rename.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && test -d robot_follow && test -f robot_follow/robot_follow_app.py && test ! -d drone_follow && grep -Fq 'name = "robot-follow"' pyproject.toml && grep -Fq 'robot-follow = "robot_follow.robot_follow_app:main"' pyproject.toml && grep -Fq 'drone-follow = "robot_follow.robot_follow_app:main"' pyproject.toml && grep -Fq 'version = "1.1.0.dev0"' pyproject.toml && grep -Fq 'robot_follow/ui/node_modules' .gitignore && [ -z "$(git grep -nE 'from drone_follow|import drone_follow' -- '*.py' 2>/dev/null)" ]</automated>
  </verify>
  <done>
    - `robot_follow/` directory exists; `drone_follow/` does not.
    - `robot_follow/robot_follow_app.py` exists; `drone_follow_app.py` does not.
    - pyproject.toml declares `name = "robot-follow"`, version `1.1.0.dev0`, `include = ["robot_follow*", "reid_analysis*"]`, and BOTH console scripts (`robot-follow` + `drone-follow`) mapped to `robot_follow.robot_follow_app:main`.
    - Zero `from drone_follow` / `import drone_follow` lines in any `.py` file under the repo (excluding submodules).
    - `configs/overlay_style.yaml`, `df_params.json`, `.gitignore` updated to `robot_follow/...` paths.
    - `mafat/run_bench.py`, `mafat/tiling_record.py`, `scripts/bench_reid_callback.py` updated.
    - `robot_follow/tests/test_install_smoke.py` no longer contains `pytest.skip("pre-rename...")` calls; all 10 tests assert unconditionally.
    - Stale `__pycache__` removed (Pitfall 1).
    - Working tree dirty, NOT committed yet (Task 3 commits everything).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite shell scripts + docs + .claude memory</name>
  <files>install.sh, scripts/install_air.sh, README.md, CLAUDE.md, TROUBLESHOOTING.md, PARAMETERS.md, SETUP_GUIDE.md, RESOLUTION_CONTROL.md, TEST_PLAN.md, docs/tracking-reid-algorithm.md, docs/calibration-flight-guide.md, docs/control-architecture.md, docs/design-review.md, .claude/memory/MEMORY.md, .claude/memory/tracking_callback_risks.md, .claude/memory/webui_build.md, .claude/skills/drone-follow-dev/SKILL.md</files>
  <action>
    Continue editing the SAME working tree as Task 1 — do not commit yet.

    **Step 1: Update `install.sh`.**

    Two changes:
    - Line ~120: rewrite `pushd "$SCRIPT_DIR/drone_follow/ui" >/dev/null` -> `pushd "$SCRIPT_DIR/robot_follow/ui" >/dev/null`.
    - Insert a new step BEFORE the `pip install -e $SCRIPT_DIR` line (currently around line 88). The new block:

      ```
      # Idempotent: remove any prior installation of the old 'drone-follow' distribution
      # so pip metadata + the old `drone-follow` console-script shim don't linger after
      # the v1.1 rename. No-op on fresh installs. Runs as the invoking user (we're inside
      # the activated venv from above).
      echo "==> Removing any prior 'drone-follow' distribution (legacy name)"
      pip uninstall drone-follow -y >/dev/null 2>&1 || true
      ```

      The `|| true` is mandatory under `set -euo pipefail`. Verify the surrounding `set -e` discipline by reading lines 1-30 of install.sh.

    - Optional (planner discretion): banner text on L8 and L134 may be rewritten to "robot-follow". Keep ONE intentional `drone-follow` mention in install.sh comments to document the alias.

    **Step 2: Update `scripts/install_air.sh`.**

    Lines 322-323: chown paths.
    - `chown_back "${APP_ROOT}/drone_follow/ui/node_modules"` -> `chown_back "${APP_ROOT}/robot_follow/ui/node_modules"`.
    - `chown_back "${APP_ROOT}/drone_follow/ui/build"` -> `chown_back "${APP_ROOT}/robot_follow/ui/build"`.

    Leave all `drone-follow` (CLI alias) mentions verbatim — they work unchanged.

    Comments in this file referencing the package directory by name (e.g. "drone-follow's UI build location") may be rewritten if clearly path-related; CLI/banner mentions stay.

    **Step 3: Rewrite Tier 1 user-facing docs (CLI examples + path refs).**

    For each file, rewrite EVERY `drone-follow --...` CLI invocation example to `robot-follow --...`, and every `drone_follow/path/...` Python path reference to `robot_follow/path/...`:
    - `README.md` (31 refs) — ALSO add a one-line alias note near the top: a paragraph or admonition explaining that `drone-follow` remains a permanent console-script alias for `robot-follow` (boot-service / muscle-memory compat). Wording is planner's discretion; suggested form: "Note: `drone-follow` is preserved as a permanent console-script alias for `robot-follow` (so the boot service and existing deployments keep working unchanged)." Place it near the "Running" or "Quickstart" section.
    - `CLAUDE.md` (40 refs) — same: add the alias note near the top (after the Project Overview section). Rewrite the architecture file-path bullets (`drone_follow/follow_api/`, `drone_follow/drone_api/mavsdk_drone.py`, etc.) to `robot_follow/...`. CLI examples in the "Running" section get rewritten to `robot-follow`.
    - `TROUBLESHOOTING.md` (32 refs) — rewrite CLI examples and the pytest path on L317. CLI references that document the legacy boot-service behavior may keep `drone-follow` (intentional, alias-related).
    - `PARAMETERS.md` (16 refs) — Python invocation example on L204 (`from drone_follow.follow_api.config import ControllerConfig` -> `from robot_follow.follow_api.config import ControllerConfig`).
    - `SETUP_GUIDE.md` (67 refs — largest doc surface). L31 architecture pointer; L526 `~/hailo-drone-follow/drone_follow/recordings/...`.
    - `RESOLUTION_CONTROL.md` (14 refs) — L81 / L206 `drone_follow/pipeline_adapter/...` path refs.
    - `TEST_PLAN.md` (1 trivial ref).

    **Step 4: Rewrite Tier 2 implementation docs (path refs + CLI examples).**

    - `docs/tracking-reid-algorithm.md` (4 refs) — `drone_follow/pipeline_adapter/...` paths.
    - `docs/calibration-flight-guide.md` (3 refs) — mixed.
    - `docs/control-architecture.md` (1 ref) — trivial.
    - `docs/design-review.md` (24 refs) — has `drone_follow_app.py` mentions that ALSO become `robot_follow_app.py`.

    **DO NOT touch:**
    - `docs/superpowers/plans/*.md` (3 files, 213 total refs) — historical plan documents. Leave verbatim per planner decision Q5; grep-gate whitelists them.

    **Step 5: Update `.claude/memory/` and `.claude/skills/drone-follow-dev/SKILL.md` text refs.**

    Per CONTEXT and RESEARCH:
    - `.claude/memory/MEMORY.md`: keep "Drone-Follow Memory Index" title (sub-project brand). Keep the path ref to `../skills/drone-follow-dev/` (skill dir name preserved). NO changes needed unless a path string inside the package is mentioned.
    - `.claude/memory/openhd_pairing.md`: contains historical path examples (`<repo>/community/apps/hailo_drone_follow/...`) — leave verbatim (historical).
    - `.claude/memory/tracking_callback_risks.md`: path refs at L12-14 (`drone_follow/pipeline_adapter/*`) -> `robot_follow/pipeline_adapter/*`. L56 has a diff example using a historical path string — keep verbatim (it's part of a quoted diff).
    - `.claude/memory/webui_build.md`: 6 `drone_follow/ui/...` path refs -> `robot_follow/ui/...`.
    - `.claude/skills/drone-follow-dev/SKILL.md`: skill dir + trigger phrases stay (out of scope). ONE path ref at L76 (`cd drone_follow/ui && ...`) -> `cd robot_follow/ui && ...`. Other mentions are the skill name itself — keep.
    - `.claude/skills/safe-pull-and-rollback/SKILL.md`: 2 mentions, both project-name (not path strings) — keep verbatim.

    **Step 6: Run the full grep-gate.**

    Apply the whitelist regex and verify the gate is clean. The expected command (write it to a temp script `/tmp/phase1_grep_gate.sh` for repeatable verification):

    ```
    #!/bin/bash
    set -e
    HITS=$(git grep -nE 'drone_follow|from drone_follow|import drone_follow' \
      | grep -vE '^\.planning/' \
      | grep -vE '^README\.md' \
      | grep -vE '^CLAUDE\.md' \
      | grep -vE '^TROUBLESHOOTING\.md' \
      | grep -vE '^docs/superpowers/plans/' \
      | grep -vE '^scripts/boot/' \
      | grep -vE '^\.claude/skills/drone-follow-dev/' \
      | grep -vE '^\.claude/memory/openhd_pairing\.md' \
      | grep -vE '^system/' \
      | grep -vE '^MILESTONES?\.md' \
      | grep -vE '^\.claude/memory/MEMORY\.md' \
      || true)
    if [ -n "$HITS" ]; then
      echo "GREP GATE FAILED — unexpected drone_follow mentions:"
      echo "$HITS"
      exit 1
    fi
    echo "GREP GATE PASSED"
    ```

    Run it. If it fails, investigate each hit. Acceptable additions to the whitelist are: documented boot-service references in TROUBLESHOOTING.md (already whitelisted), the alias paragraph in README.md and CLAUDE.md (whitelisted). UNACCEPTABLE: any remaining `from drone_follow` / `import drone_follow` (those must be zero — should already be caught by Task 1 Step 11). Any `drone_follow/path/...` in a Tier-1/Tier-2 doc (those must be 100% rewritten).

    Do NOT commit yet. Task 3 commits the entire atomic rename.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && grep -Fq 'pip uninstall drone-follow -y' install.sh && grep -Fq 'robot_follow/ui' install.sh && grep -Fq 'robot_follow/ui/node_modules' scripts/install_air.sh && grep -Fq 'robot_follow/ui/build' scripts/install_air.sh && [ -z "$(git grep -nE 'drone_follow/(pipeline_adapter|follow_api|drone_api|servers|tests|ui|recordings)' -- 'README.md' 'CLAUDE.md' 'PARAMETERS.md' 'SETUP_GUIDE.md' 'RESOLUTION_CONTROL.md' 'TEST_PLAN.md' 'docs/tracking-reid-algorithm.md' 'docs/calibration-flight-guide.md' 'docs/control-architecture.md' 'docs/design-review.md' '.claude/memory/tracking_callback_risks.md' '.claude/memory/webui_build.md' 2>/dev/null)" ]</automated>
  </verify>
  <done>
    - `install.sh` contains the `pip uninstall drone-follow -y` step before `pip install -e .`, and the UI pushd path is `robot_follow/ui`.
    - `scripts/install_air.sh` chown lines reference `robot_follow/ui/{node_modules,build}`.
    - All Tier 1 docs (README.md, CLAUDE.md, TROUBLESHOOTING.md, PARAMETERS.md, SETUP_GUIDE.md, RESOLUTION_CONTROL.md, TEST_PLAN.md) have their CLI examples + path refs rewritten; README.md and CLAUDE.md have a one-line alias note near the top.
    - All Tier 2 docs (`docs/tracking-reid-algorithm.md`, `docs/calibration-flight-guide.md`, `docs/control-architecture.md`, `docs/design-review.md`) have path refs rewritten.
    - `.claude/memory/tracking_callback_risks.md` and `.claude/memory/webui_build.md` updated; SKILL.md has its one path-line at L76 updated.
    - `docs/superpowers/plans/*.md`, `scripts/boot/*`, `system/*`, `.claude/skills/drone-follow-dev/` (except SKILL.md L76) untouched.
    - Grep gate passes: no source-tree path refs to `drone_follow/*` outside whitelist.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reinstall, run full Phase 1 verification gate, commit atomic rename</name>
  <files>(no new file edits — this task runs verification + git commit)</files>
  <action>
    All file edits are complete from Tasks 1 and 2. Now reinstall, run the full verification gate, and commit as ONE atomic commit.

    **Step 1: Re-source venv to ensure pip operates inside the right interpreter.**

    Run `source setup_env.sh`. This activates `./hailo-apps/venv_hailo_apps` and exports `PYTHONPATH`/`HAILO_APPS_PATH`. Verify with `which pip` (should point inside `hailo-apps/venv_hailo_apps/bin/`).

    **Step 2: Run pip uninstall + reinstall to regenerate console-script shims.**

    Order matters (per RESEARCH Pitfall 2 + Pitfall 3):
    - `pip uninstall drone-follow -y` (removes legacy distribution metadata and the old `bin/drone-follow` shim that embeds `from drone_follow.drone_follow_app import main`).
    - `pip install -e .` (installs the new `robot-follow` distribution; creates `bin/robot-follow` and `bin/drone-follow` BOTH embedding `from robot_follow.robot_follow_app import main`).

    If pip emits warnings about cached entry points, run `pip cache purge` and retry (only if the next-step verify fails).

    **Step 3: Verify pip metadata exclusivity (success criterion 1).**

    - `pip show robot-follow` should exit 0 and show metadata.
    - `pip show drone-follow` should exit non-zero (no metadata).

    If either fails, STOP and investigate before committing.

    **Step 4: Verify console-script shims regenerated.**

    Cat both shims and verify their import lines:
    - `cat $(which robot-follow)` should show `from robot_follow.robot_follow_app import main`.
    - `cat $(which drone-follow)` should show `from robot_follow.robot_follow_app import main` (NOT the old `drone_follow` path).

    **Step 5: Verify --help identity (success criterion 2).**

    Run `diff <(robot-follow --help 2>&1) <(drone-follow --help 2>&1)`. Expect zero output (byte-identical).

    **Step 6: Verify imports cleanly.**

    `python -c 'import robot_follow; import robot_follow.follow_api.config; import robot_follow.follow_api.controller; import robot_follow.follow_api.state; import robot_follow.follow_api.types'`. Expect exit 0.

    Negative assertion: `python -c 'import drone_follow' 2>/dev/null`. Expect exit non-zero (ModuleNotFoundError).

    **Step 7: Run the smoke test gate.**

    `pytest robot_follow/tests/test_install_smoke.py -v`. Expect 10 PASSED, 0 SKIPPED, 0 FAILED.

    **Step 8: Run the grep gate one more time (defense in depth).**

    Re-run the whitelist-filtered grep from Task 2 Step 6. Expect "GREP GATE PASSED".

    **Step 9: Verify boot service files preserved verbatim (success criterion 3 partial).**

    - `git diff scripts/boot/` — expect zero diff.
    - `git diff system/` — expect zero diff (per planner decision Q1).
    - `grep -q 'drone-follow-boot' scripts/boot/drone-follow-boot.service` — must succeed (file untouched).
    - `grep -q 'drone-follow.conf' scripts/boot/install.sh` — must succeed (file untouched).

    **Step 10: Syntax-check the start scripts (success criterion 3, headless verifiable).**

    `bash -n scripts/start_air.sh` and `bash -n scripts/install_air.sh`. Expect exit 0 for both.

    **Step 11: Stage everything and commit.**

    Use a HEREDOC commit message (multi-line, descriptive). Stage only the files in this plan's `files_modified` list plus the rename's git-mv records. Suggested message:

    ```
    refactor(01): rename drone_follow -> robot_follow (v1.1 milestone shaper)

    Atomic mechanical rename:
    - git mv drone_follow robot_follow + drone_follow_app.py -> robot_follow_app.py
    - 48 internal imports rewritten across 19 .py files (sed pass)
    - pyproject: name=robot-follow, version=1.1.0.dev0, both console scripts
      (robot-follow primary + drone-follow alias) map to
      robot_follow.robot_follow_app:main
    - install.sh: idempotent `pip uninstall drone-follow -y` before `pip install -e .`
    - Tier 1/2 docs: CLI examples + path refs rewritten to robot-follow / robot_follow
    - Alias note added near top of README.md and CLAUDE.md
    - .claude/memory/{tracking_callback_risks,webui_build}.md + SKILL.md L76 updated

    Preserved verbatim (RENAME-04 spirit):
    - scripts/boot/drone-follow-boot.{service,sh} + ~/Desktop/drone-follow.conf
    - system/drone-network-mode.sh + system/install.sh (user-level systemd unit name)
    - .claude/skills/drone-follow-dev/ (skill dir + trigger phrases)
    - run_drone.sh, df_params.json, scripts/{start,install}_air.sh filenames
    - docs/superpowers/plans/*.md (historical plan records)

    Verification gate (all passing):
    - pip show robot-follow OK; pip show drone-follow returns nothing
    - diff <(robot-follow --help) <(drone-follow --help) empty
    - pytest robot_follow/tests/test_install_smoke.py: 10 passed
    - git grep gate (whitelist-filtered): zero hits

    Phase 2 (cleanup) and Phase 3 (abstraction) move drone_api/ -> robot_api/
    and the run_drone() function name; not in scope here.

    Refs: RENAME-01, RENAME-02, RENAME-03, RENAME-04, RENAME-05
    ```

    Use `node ~/.claude/get-shit-done/bin/gsd-tools.cjs commit "$MESSAGE" --files <list>` if the GSD tooling is wired, or fall back to `git commit -m "$(cat <<'EOF' ... EOF)"` using HEREDOC syntax.

    Stage with: `git add -A` is acceptable here because the working tree is already curated by Tasks 1 + 2 — no stray files should exist. If `git status` shows unexpected paths, fall back to explicit `git add <path>` per file.

    **Step 12: Post-commit verification (don't push yet).**

    - `git log --oneline -1` — confirm the commit landed.
    - `git log --follow robot_follow/follow_api/config.py | head -20` — confirm history follows the rename (should show pre-rename commits).
    - `git status` — clean working tree.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && source setup_env.sh && pip show robot-follow >/dev/null 2>&1 && ! pip show drone-follow >/dev/null 2>&1 && command -v robot-follow >/dev/null && command -v drone-follow >/dev/null && diff <(robot-follow --help 2>&1) <(drone-follow --help 2>&1) && python -c 'import robot_follow; import robot_follow.follow_api.config; import robot_follow.follow_api.controller; import robot_follow.follow_api.state; import robot_follow.follow_api.types' && ! python -c 'import drone_follow' 2>/dev/null && pytest robot_follow/tests/test_install_smoke.py -x && bash -n scripts/start_air.sh && bash -n scripts/install_air.sh && [ -z "$(git diff scripts/boot/)" ] && [ -z "$(git diff system/)" ]</automated>
  </verify>
  <done>
    - `pip show robot-follow` exits 0; `pip show drone-follow` exits non-zero (success criterion 1).
    - Both `robot-follow` and `drone-follow` are on PATH; their shims both embed `from robot_follow.robot_follow_app import main`.
    - `robot-follow --help` and `drone-follow --help` produce byte-identical output (success criterion 2).
    - `pytest robot_follow/tests/test_install_smoke.py` returns 10 passed, 0 skipped, 0 failed.
    - `bash -n scripts/start_air.sh` and `bash -n scripts/install_air.sh` exit 0 (syntax clean — success criterion 3 partial).
    - `git diff scripts/boot/` and `git diff system/` are empty (RENAME-04 + planner decision Q1).
    - The grep gate (whitelist-filtered) returns zero matches (success criterion 4).
    - Exactly ONE new commit lands on `feature/rover-support` (atomic rename per CONTEXT).
    - `git log --follow robot_follow/follow_api/config.py` shows pre-rename history.
  </done>
</task>

</tasks>

<verification>
**Full Phase 1 verification gate (post-Task 3 commit):**

Per VALIDATION.md § Quick suite (pre-commit, < 30 s, no Hailo HW):

1. **Flush stale bytecode:**
   - `find . -name __pycache__ -type d -prune -exec rm -rf {} +`
   - `find . -name '*.pyc' -delete`

2. **New import path works:**
   - `python -c 'import robot_follow; import robot_follow.follow_api.config; import robot_follow.follow_api.controller; import robot_follow.follow_api.state; import robot_follow.follow_api.types'`

3. **Old import path raises:**
   - `! python -c 'import drone_follow' 2>/dev/null`

4. **Both console scripts on PATH, byte-identical --help:**
   - `command -v robot-follow && command -v drone-follow`
   - `diff <(robot-follow --help 2>&1) <(drone-follow --help 2>&1)` (zero output)

5. **pip metadata exclusive:**
   - `pip show robot-follow >/dev/null`
   - `! pip show drone-follow >/dev/null 2>&1`

6. **Grep gate (whitelist-limited):**
   - Per Task 2 Step 6 script.

7. **Smoke test:**
   - `pytest robot_follow/tests/test_install_smoke.py -x` (10 passed).

Per VALIDATION.md § Per wave merge (pre-push, Hailo-capable host):
- `pytest robot_follow/tests/ -x --ignore=robot_follow/tests/test_sim_worlds.py`

(Plan 03 covers manual dev-box verifications: `./install.sh --skip-apps --skip-hefs --skip-ui` clean re-run and `bash -n scripts/start_air.sh` on a fresh checkout.)
</verification>

<success_criteria>
- **RENAME-01**: Package dir `drone_follow/` renamed to `robot_follow/`; all internal imports updated. Verified by grep gate + import test.
- **RENAME-02**: `pyproject.toml` name is `robot-follow`; both console scripts work; `pip show` metadata exclusive. Verified by `pip show` + `diff <(...help) <(...help)`.
- **RENAME-03**: `setup_env.sh`, `install.sh`, `scripts/start_air.sh` updated; existing functionality preserved. Verified by `bash -n` syntax checks + `setup_env.sh` re-source.
- **RENAME-04**: Boot service files + `~/Desktop/drone-follow.conf` config name preserved. Verified by `git diff scripts/boot/` empty.
- **RENAME-05**: README, CLAUDE.md, TROUBLESHOOTING.md, docs/*.md, .claude/memory/*.md updated with examples valid via alias. Verified by whitelist-filtered grep gate.
- **Atomic-commit discipline**: Exactly one commit lands the rename. Verified by `git log --oneline -1` showing the commit + `git status` clean.
</success_criteria>

<output>
After completion, create `.planning/phases/01-rename/01-02-SUMMARY.md` documenting:
- The atomic commit hash + message
- Inventory of files actually changed (vs files_modified prediction)
- Grep-gate whitelist hits accepted (alias mentions count by file)
- All 7 quick-suite steps green
- Smoke test result (10 passed / 0 skipped / 0 failed)
- Boot service file diff (must be empty)
- Phase 2 handoff notes (any cleanup hooks observed during the rewrite)
</output>
