---
phase: 01-rename
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - drone_follow/tests/test_install_smoke.py
autonomous: true
requirements: [RENAME-01, RENAME-02]
must_haves:
  truths:
    - "Smoke test asserts new-name imports work AND old-name imports fail"
    - "Smoke test asserts both console scripts on PATH"
    - "Smoke test asserts byte-identical --help output"
    - "Smoke test asserts pip metadata exclusive to robot-follow"
  artifacts:
    - path: "drone_follow/tests/test_install_smoke.py"
      provides: "Phase 1 verification gate; renamed-package smoke test"
      min_lines: 90
      contains: "test_robot_follow_package_imports"
  key_links:
    - from: "drone_follow/tests/test_install_smoke.py"
      to: "robot-follow / drone-follow console scripts"
      via: "subprocess.run + shutil.which"
      pattern: "shutil\\.which\\(.(robot|drone)-follow.\\)"
    - from: "drone_follow/tests/test_install_smoke.py"
      to: "pip metadata"
      via: "subprocess.run(['pip', 'show', ...])"
      pattern: "pip.*show.*robot-follow"
---

<objective>
Land the post-rename smoke test BEFORE the rename commit. This file becomes the verification gate for Wave 1 (the rename itself) — when Wave 1 lands, this test must pass on the renamed tree, and any drift trips immediately.

Purpose: Decouple test-infrastructure changes from the atomic rename commit so the test is available to run on Wave 1's working tree without circular-dependency drama (the rename commit would otherwise need to both create-and-pass the test in one step).

Output: Updated `drone_follow/tests/test_install_smoke.py` that probes for BOTH `robot_follow` (preferred) AND `drone_follow` (legacy) with `pytest.skip` semantics so it does not break the current tree but ASSERTS the post-rename contract once `robot_follow` exists. This is a single forward-compatible commit.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rename/01-CONTEXT.md
@.planning/phases/01-rename/01-RESEARCH.md
@.planning/phases/01-rename/01-VALIDATION.md
@drone_follow/tests/test_install_smoke.py
@drone_follow/tests/conftest.py

<interfaces>
<!-- Current test file's structure (replace, do not append) -->

Current file: drone_follow/tests/test_install_smoke.py (56 lines):

```python
import os
import shutil
import subprocess
from importlib import import_module


def test_drone_follow_package_imports():
    mod = import_module("drone_follow")
    assert mod is not None


def test_drone_follow_follow_api_imports():
    for name in (
        "drone_follow.follow_api.types",
        "drone_follow.follow_api.config",
        "drone_follow.follow_api.controller",
        "drone_follow.follow_api.state",
    ):
        m = import_module(name)
        assert m is not None, name


def test_drone_follow_help_exits_zero():
    drone_follow_bin = shutil.which("drone-follow")
    assert drone_follow_bin, (...)
    proc = subprocess.run([drone_follow_bin, "--help"], ...)
    assert proc.returncode == 0, (...)
    assert b"--input" in proc.stdout or b"--input" in proc.stderr
```

Discovery context:
- pytest 9.0.2 at `/home/guyz/.local/bin/pytest` (system, not venv)
- conftest.py adds repo root to `sys.path` via `..` traversal — survives rename
- Phase 1 success criteria (from ROADMAP):
  1. `pip show robot-follow` succeeds; `pip show drone-follow` returns nothing
  2. `drone-follow --help` and `robot-follow --help` produce identical output
  3. `scripts/start_air.sh` runs without path errors; boot service unchanged
  4. No `from drone_follow` / `import drone_follow` anywhere in source

Forward-compatibility constraint: this commit lands BEFORE the rename. On the current tree (pre-rename), `robot_follow` does not exist yet. The test must:
- Detect which package name is installed (try `robot_follow` first, fall back to `drone_follow` for legacy)
- Use `pytest.skip` cleanly when post-rename assertions are not yet applicable on the pre-rename tree
- Assert the FULL post-rename contract once `robot_follow` is importable
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite test_install_smoke.py as forward-compatible Phase 1 gate</name>
  <files>drone_follow/tests/test_install_smoke.py</files>
  <behavior>
    The rewritten test file must define these tests, each with the documented behavior:

    1. `test_robot_follow_package_imports` — PASSES if `import robot_follow` succeeds; SKIPS with reason "pre-rename tree" if `robot_follow` is not yet installed AND `drone_follow` IS installed.
    2. `test_robot_follow_follow_api_imports` — PASSES if all of `robot_follow.follow_api.{types,config,controller,state}` import; SKIPS pre-rename.
    3. `test_drone_follow_import_raises_after_rename` — PASSES if `robot_follow` is importable AND `drone_follow` raises `ModuleNotFoundError`; SKIPS pre-rename. This is the negative assertion that the old import path is removed.
    4. `test_robot_follow_console_script_on_path` — PASSES if `shutil.which("robot-follow")` returns non-None; SKIPS pre-rename.
    5. `test_drone_follow_console_script_alias_on_path` — PASSES if `shutil.which("drone-follow")` returns non-None. This is REQUIRED both pre- and post-rename (legacy already provides it; post-rename alias preserves it). NO skip.
    6. `test_robot_follow_help_exits_zero` — `robot-follow --help` exits 0 and outputs `--input` in stdout/stderr; SKIPS pre-rename.
    7. `test_drone_follow_help_exits_zero` — `drone-follow --help` exits 0 and outputs `--input` in stdout/stderr. REQUIRED both pre- and post-rename. NO skip (replaces the existing test, same name).
    8. `test_help_outputs_byte_identical` — diff `robot-follow --help` against `drone-follow --help` (stdout+stderr concatenated); they MUST be byte-identical post-rename. SKIPS pre-rename.
    9. `test_pip_show_robot_follow_succeeds` — `pip show robot-follow` exits 0. SKIPS pre-rename.
    10. `test_pip_show_drone_follow_returns_nothing` — `pip show drone-follow` exits non-zero. SKIPS pre-rename (legacy distribution still installed).

    Detection helper (module-level): a function `_robot_follow_installed() -> bool` that returns `True` iff `importlib.util.find_spec("robot_follow") is not None`. Tests that should skip pre-rename call `pytest.skip("pre-rename tree; robot_follow not yet installed")` when this returns False.

    The legacy `test_drone_follow_package_imports` and `test_drone_follow_follow_api_imports` are REMOVED (replaced by their robot_follow equivalents — those are the new gates). The legacy `test_drone_follow_help_exits_zero` is KEPT (the alias must always work) but its docstring updated to mention the alias.

    Module docstring rewritten to describe: "Tier-1 install smoke test for the robot-follow package (formerly drone-follow). Forward-compatible: skips robot_follow assertions on the pre-rename tree, enforces them once robot_follow is installed. The `drone-follow` console script is asserted to always work (legacy pre-rename, alias post-rename)."
  </behavior>
  <action>
    Replace the entire contents of `drone_follow/tests/test_install_smoke.py` with the rewrite described in &lt;behavior&gt;.

    Implementation notes:
    - Use `importlib.util.find_spec("robot_follow")` (not `importlib.import_module` in a try/except) for detection — `find_spec` doesn't side-effect-import.
    - For `pytest.skip` to work at test-function granularity, import `pytest` at module top.
    - For the byte-identical assertion, run both via `subprocess.run(..., capture_output=True, timeout=30, env={**os.environ})` and concatenate `stdout + stderr` from each before comparing. Provide the diff in the assertion message on failure (use `difflib.unified_diff` for human-readable output).
    - For pip metadata tests: `subprocess.run([sys.executable, "-m", "pip", "show", "robot-follow"], capture_output=True, timeout=15)`. Use `sys.executable -m pip` not bare `pip` to bind to the test interpreter's venv.
    - Preserve the existing `env={**os.environ}` pattern (pytest may otherwise strip PATH).
    - Keep file under ~150 lines; each test is a small focused function.
    - Verify the file is still discoverable by pytest (function names start with `test_`, no class needed).

    Do NOT rename the file itself in this commit (`git mv` of the tests dir is Wave 1's job — the `drone_follow/` parent rename carries the file).

    NOTE on the existing test name `test_drone_follow_help_exits_zero`: keep this exact name (Step 7 above) — its body is unchanged in shape (still tests `drone-follow --help`), so it works pre- and post-rename without skip logic. New tests are pure additions; only the two `drone_follow`-import tests are replaced by `robot_follow` equivalents.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow &amp;&amp; source setup_env.sh &amp;&amp; pytest drone_follow/tests/test_install_smoke.py -v</automated>

    Expected: All "skip pre-rename" tests SKIP with the documented reason. The 2 always-required tests (`test_drone_follow_console_script_alias_on_path`, `test_drone_follow_help_exits_zero`) PASS. No FAILURES.

    Also run: `python -c "import ast; ast.parse(open('drone_follow/tests/test_install_smoke.py').read())"` (syntax check, no execution).
  </verify>
  <done>
    - `drone_follow/tests/test_install_smoke.py` contains the 10 tests listed in &lt;behavior&gt; (renamed/added/preserved per the spec).
    - On the current (pre-rename) tree, `pytest drone_follow/tests/test_install_smoke.py` returns exit 0 with skips logged for the post-rename-only assertions and the 2 always-on tests passing.
    - File header docstring describes the forward-compatibility design.
    - No `from drone_follow` imports introduced (the tests use string-based detection only).
    - `pytest --collect-only drone_follow/tests/test_install_smoke.py` lists all 10 tests.
  </done>
</task>

</tasks>

<verification>
**Phase 1 Wave 0 gate (single command):**

```bash
source setup_env.sh
pytest drone_follow/tests/test_install_smoke.py -v
```

Expected on pre-rename tree:
- 2 PASSED (alias + legacy console-script tests)
- 8 SKIPPED ("pre-rename tree; robot_follow not yet installed")
- 0 FAILED

After Wave 1 lands (smoke test runs against renamed tree):
- 10 PASSED
- 0 SKIPPED
- 0 FAILED
</verification>

<success_criteria>
- File `drone_follow/tests/test_install_smoke.py` updated with 10-test rewrite per Task 1's behavior block.
- Pre-rename test execution: exit 0 with documented skips.
- No syntax errors; pytest collects all 10 tests.
- Commit message: `test(01): forward-compatible install smoke test for rename gate`
</success_criteria>

<output>
After completion, create `.planning/phases/01-rename/01-01-SUMMARY.md` documenting:
- The 10 tests added/kept/replaced
- The detection helper pattern (`_robot_follow_installed`)
- How Wave 1 will exercise this test post-rename
</output>
