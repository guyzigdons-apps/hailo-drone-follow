---
phase: 01-rename
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified: []
autonomous: false
requirements: [RENAME-03, RENAME-04]
must_haves:
  truths:
    - "install.sh re-runs cleanly on a dev box without re-doing the submodule installer"
    - "Operator confirms scripts/start_air.sh would resolve all paths on a fresh checkout (headless syntax + path-exists checks)"
    - "Boot service unit file on disk (if deployed) still ExecStarts a valid path post-rename"
  artifacts:
    - path: ".planning/phases/01-rename/01-03-SUMMARY.md"
      provides: "Phase 1 manual-verification log; gates phase completion"
      contains: "Manual verification"
  key_links:
    - from: "operator-run install.sh"
      to: "venv site-packages metadata + console-script shims"
      via: "pip uninstall + pip install -e ."
      pattern: "pip install -e"
---

<objective>
Document and execute the manual verifications that cannot run in a pure pytest gate: a clean re-run of `./install.sh --skip-apps --skip-hefs --skip-ui` on a dev box (confirms the idempotent uninstall step lands cleanly on a machine that has BOTH old and new distributions historically), plus operator confirmation that `scripts/start_air.sh` and the boot-service path resolution survive the rename.

Purpose: VALIDATION.md flags four manual-only verifications under § Manual-Only Verifications. They are real Phase 1 success criteria (RENAME-03 + RENAME-04) and must be witnessed before Phase 1 is marked complete — otherwise `/gsd:verify-work` cannot honestly call Phase 1 done. Without this plan they get lost in the gap between "smoke test passed" and "actually shipped on a deployed unit."

Output: A SUMMARY.md log proving each manual check was performed and PASSED, plus the operator's resume signal.
</objective>

<execution_context>
@/home/guyz/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rename/01-CONTEXT.md
@.planning/phases/01-rename/01-VALIDATION.md
@.planning/phases/01-rename/01-02-SUMMARY.md
@scripts/start_air.sh
@scripts/install_air.sh
@install.sh

<interfaces>
**Manual-only items from VALIDATION.md § Manual-Only Verifications:**

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `setup_env.sh` clean source | RENAME-03 | Shell-only, not pytestable in-process | `bash -c 'source setup_env.sh && python -c "import robot_follow"'` returns 0 |
| `scripts/start_air.sh` runs without path errors | RENAME-03 | Needs OpenHD + camera; only path-resolution verifiable headlessly | `bash -n scripts/start_air.sh` + `test -d robot_follow/ui/build` (post-install) |
| `./install.sh` idempotent re-run | RENAME-03 | Touches `/usr/local/hailo/resources/` + submodule installer; can't run in CI | One-time dev-box re-run with `--skip-apps --skip-hefs --skip-ui` |
| Boot service file unchanged on disk | RENAME-04 | Deployed `/etc/systemd/system/drone-follow-boot.service` is per-host state | `systemctl status drone-follow-boot` shows the unit; verify `ExecStart` path on a target host |

**Pre-conditions (Plan 01-02 must already have landed):**
- `git status` clean on `feature/rover-support`.
- Atomic rename commit exists.
- `pytest robot_follow/tests/test_install_smoke.py` passes 10/10.
- Working dev-box venv has `robot-follow` installed and `drone-follow` is no longer in pip metadata.

**Operator interaction:**
This plan is NOT autonomous (`autonomous: false`). Task 2 is a checkpoint that pauses for the user to either confirm the install.sh re-run output or describe failures. Task 1 is fully automated (syntax checks + path-exists checks).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated path/syntax checks for start_air.sh and install_air.sh</name>
  <files>(no edits — read-only verification)</files>
  <action>
    Run the headless-verifiable subset of the manual checks. These don't need OpenHD or the camera; they verify path strings and shell syntax only.

    **Step 1: `setup_env.sh` clean source.**

    Run `bash -c 'source setup_env.sh && python -c "import robot_follow"'`. Expect exit 0.

    Then run `bash -c 'source setup_env.sh && python -c "import robot_follow.follow_api.config; import robot_follow.follow_api.controller; import robot_follow.follow_api.state; import robot_follow.follow_api.types"'`. Expect exit 0.

    Then negative-assert: `bash -c 'source setup_env.sh && python -c "import drone_follow"' 2>/dev/null`. Expect exit non-zero.

    **Step 2: Syntax-check `scripts/start_air.sh` and `scripts/install_air.sh`.**

    Run `bash -n scripts/start_air.sh`. Expect exit 0.
    Run `bash -n scripts/install_air.sh`. Expect exit 0.
    Run `bash -n install.sh`. Expect exit 0 (this also exercises the new `pip uninstall drone-follow -y` insertion as a syntax-only check).

    **Step 3: Path-exists check on the renamed UI build location.**

    `scripts/install_air.sh` and `scripts/start_air.sh` reference `${APP_ROOT}/robot_follow/ui/build`. After the rename, on a dev box that has run `./install.sh` post-rename, this directory should exist. If it doesn't (because `--skip-ui` was passed historically), check `robot_follow/ui/` itself exists.

    Run: `test -d robot_follow/ui && echo "ui dir present"`.

    If the dev box has run the full install: `test -d robot_follow/ui/build && echo "ui build present"`. If not, log "ui build not present — depends on --skip-ui in the install.sh re-run; Task 2 will exercise this".

    **Step 4: Boot service preservation check.**

    - `git diff HEAD~1 scripts/boot/` should be empty (the rename commit must not have touched boot files).
    - `test -f scripts/boot/drone-follow-boot.service && grep -Fq 'drone-follow-boot' scripts/boot/drone-follow-boot.service` — must succeed.
    - `grep -Fq 'drone-follow.conf' scripts/boot/install.sh` — must succeed.
    - `git diff HEAD~1 system/` should be empty (per planner decision Q1 — leaving `drone-follow.service` user unit verbatim).

    **Step 5: Console-script shim sanity (re-verify post-rename install state).**

    - `cat $(which robot-follow) | head -5` — should embed `from robot_follow.robot_follow_app import main`.
    - `cat $(which drone-follow) | head -5` — should embed the SAME import (alias points at the same target).

    Record all results in `.planning/phases/01-rename/01-03-SUMMARY.md` as a checklist. Each item PASSED or FAILED with the output text.
  </action>
  <verify>
    <automated>cd /home/guyz/code/guyz/hailo-drone-follow && bash -c 'source setup_env.sh && python -c "import robot_follow; import robot_follow.follow_api.config; import robot_follow.follow_api.controller; import robot_follow.follow_api.state; import robot_follow.follow_api.types"' && bash -n scripts/start_air.sh && bash -n scripts/install_air.sh && bash -n install.sh && test -d robot_follow/ui && [ -z "$(git diff HEAD~1 scripts/boot/ 2>/dev/null)" ] && [ -z "$(git diff HEAD~1 system/ 2>/dev/null)" ] && grep -Fq 'drone-follow-boot' scripts/boot/drone-follow-boot.service && grep -Fq 'drone-follow.conf' scripts/boot/install.sh && cat "$(which robot-follow)" | grep -Fq 'from robot_follow.robot_follow_app import main' && cat "$(which drone-follow)" | grep -Fq 'from robot_follow.robot_follow_app import main'</automated>
  </verify>
  <done>
    - `setup_env.sh` sources cleanly; `robot_follow` imports succeed; `drone_follow` import fails (negative assert).
    - Three shell-syntax checks (`bash -n`) all exit 0.
    - `robot_follow/ui/` exists; build subdirectory presence noted (depends on `--skip-ui`).
    - `git diff HEAD~1 scripts/boot/` and `git diff HEAD~1 system/` both empty (RENAME-04 + Q1).
    - Both console-script shims embed `from robot_follow.robot_follow_app import main`.
    - All findings logged to `.planning/phases/01-rename/01-03-SUMMARY.md` as a Task 1 checklist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator confirms install.sh idempotent re-run + boot service state</name>
  <files>.planning/phases/01-rename/01-03-SUMMARY.md</files>
  <action>
    This is a blocking checkpoint. Pause execution and present the three manual verifications to the operator. Do NOT proceed until the operator types a resume signal.

    **What's been built (context for the operator):**

    The atomic rename commit (Plan 01-02) has landed; all automated gates from Plan 01-02 and Plan 01-03 Task 1 are green. What remains is the one verification that ONLY makes sense on a dev box with a venv that historically had `drone-follow` installed: a fresh re-run of `./install.sh --skip-apps --skip-hefs --skip-ui` to confirm the idempotent `pip uninstall drone-follow -y` step lands cleanly and the editable reinstall produces the expected shim state.

    If the dev box this plan runs on is the SAME machine where Plan 01-02 was developed, the venv already has the post-rename state. Re-running install.sh re-exercises the uninstall + reinstall path and verifies it stays clean on a second invocation (idempotency check). It also catches any cached entry-point or package-lock drift introduced by the rename.

    This task also documents the boot-service-on-disk check, which depends on whether a deployed RPi is reachable from the operator's machine.

    **How to verify (operator instructions — present these verbatim):**

    **Verification A: install.sh idempotent re-run on a dev box.**

    1. Open a fresh terminal in the repo root.
    2. Run: `./install.sh --skip-apps --skip-hefs --skip-ui`
    3. Expected behaviour (per RESEARCH inventory of install.sh modifications):
       - Banner: `==> Removing any prior 'drone-follow' distribution (legacy name)` (the new step from Plan 01-02 Task 2).
       - Either silent success (no `drone-follow` distribution to remove) OR `Found existing installation: drone-follow ...; Uninstalling drone-follow-...; Successfully uninstalled`.
       - Then `==> [3/5] pip install -e $SCRIPT_DIR` (or similar — depends on install.sh's step numbering after the insertion).
       - Editable install reports `Successfully installed robot-follow-1.1.0.dev0`.
       - Final banner: success-equivalent message (planner discretion on exact wording).
    4. Post-run sanity:
       - `pip show robot-follow` exits 0.
       - `pip show drone-follow` exits non-zero.
       - `robot-follow --help` and `drone-follow --help` both exit 0 and produce byte-identical output.
       - `cat $(which drone-follow) | head -5` shows `from robot_follow.robot_follow_app import main`.

    **Verification B: boot-service on disk (if a deployed RPi is reachable).**

    On the deployed RPi (e.g. `ssh hailo@<rpi-host>`):
    1. `systemctl status drone-follow-boot` — confirm the unit file is loaded.
    2. `cat /etc/systemd/system/drone-follow-boot.service | grep ExecStart` — confirm `ExecStart` points to `/home/hailo/hailo-drone-follow/scripts/boot/drone-follow-boot.sh` (or equivalent — should be UNCHANGED post-rename).
    3. `cat ~/Desktop/drone-follow.conf` — file exists, contains `ENABLED=true|false` and optionally `MODE=stream|shm`.

    If no deployed RPi is currently reachable, mark Verification B as "deferred to next field deployment" in the SUMMARY and note that the boot service unit-file ExecStart path is path-shape (not path-name) dependent per RESEARCH Pitfall 6 — it survives the rename mechanically.

    **Verification C: end-to-end smoke (optional, only if camera + Hailo HW available).**

    On a Hailo-capable host:
    1. `pytest robot_follow/tests/ -x --ignore=robot_follow/tests/test_sim_worlds.py` (full test suite excluding sim).
    2. Expect all PASSED.

    This is RESEARCH § Verification Architecture's "pre-push (Hailo-capable host)" tier. If the dev box has Hailo HW, run it; if not, document as deferred until next sync to a Hailo-equipped machine.

    **After running all applicable verifications:**

    Update `.planning/phases/01-rename/01-03-SUMMARY.md` with the results of each. Mark each as PASSED / FAILED / DEFERRED with operator notes. Append the operator's terminal output for Verification A as a fenced code block.

    **Resume signal:**

    - Type "approved" if all applicable verifications PASSED.
    - Type "approved (B deferred)" or "approved (C deferred)" if specific verifications were deferred (no RPi access, no Hailo HW) — note the deferral reason in the SUMMARY.
    - Describe issues if anything FAILED.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-rename/01-03-SUMMARY.md && grep -qE 'Verification A.*(PASSED|FAILED|DEFERRED)' .planning/phases/01-rename/01-03-SUMMARY.md && grep -qE 'Verification B.*(PASSED|FAILED|DEFERRED)' .planning/phases/01-rename/01-03-SUMMARY.md && grep -qE 'Verification C.*(PASSED|FAILED|DEFERRED)' .planning/phases/01-rename/01-03-SUMMARY.md</automated>
  </verify>
  <done>
    - Operator has run `./install.sh --skip-apps --skip-hefs --skip-ui` and confirmed expected output (Verification A: PASSED or FAILED + reason).
    - Boot service on disk verified or explicitly DEFERRED with reason (Verification B).
    - Full pytest suite run on Hailo host or explicitly DEFERRED with reason (Verification C).
    - `.planning/phases/01-rename/01-03-SUMMARY.md` updated with PASSED/FAILED/DEFERRED for each of A, B, C plus operator notes.
    - Operator's resume signal received ("approved" or specific deferral note).
  </done>
</task>

</tasks>

<verification>
**Plan 03 completion criteria:**

1. Task 1 automated checks all PASS (verify block).
2. Task 2 checkpoint receives operator "approved" signal.
3. `.planning/phases/01-rename/01-03-SUMMARY.md` exists and documents each manual verification with PASSED / FAILED / DEFERRED + operator notes.

**Phase 1 overall completion (post-Plan-03):**
- All 4 ROADMAP success criteria verified:
  1. `pip show robot-follow` works; `pip show drone-follow` returns nothing. (Plan 01-02 Task 3)
  2. `drone-follow --help` and `robot-follow --help` byte-identical. (Plan 01-02 Task 3)
  3. `scripts/start_air.sh` syntax-clean + boot service unit file unchanged. (Plan 01-03 Task 1)
  4. No `from drone_follow` / `import drone_follow` outside whitelist. (Plan 01-02 grep gate + Plan 01-03 SUMMARY)

Phase 1 is now eligible for `/gsd:verify-work`.
</verification>

<success_criteria>
- Task 1 automated checks all green (see `<automated>` block).
- Task 2 operator confirms install.sh idempotent re-run produces the expected output (Verification A PASSED, B/C optional or deferred).
- `.planning/phases/01-rename/01-03-SUMMARY.md` written with explicit PASS/FAIL/DEFER for each manual verification.
- Phase 1 ready for `/gsd:verify-work`.
</success_criteria>

<output>
After completion, ensure `.planning/phases/01-rename/01-03-SUMMARY.md` documents:
- Task 1 checklist (each automated check + output)
- Verification A result (install.sh re-run) — PASSED/FAILED + the operator's terminal-output paste
- Verification B result (boot service on disk) — PASSED/DEFERRED + reason
- Verification C result (full pytest suite) — PASSED/DEFERRED + reason
- Phase 1 final sign-off statement: "All 4 ROADMAP success criteria for Phase 1 verified."
</output>
